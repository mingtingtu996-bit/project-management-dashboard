# Visual Check Artifacts Moved

UI screenshot artifacts were moved to:

`project-ui/artifacts/visual-check/`

New UI implementation artifacts should go under `project-ui/artifacts/` or `project-ui/reports/`.
