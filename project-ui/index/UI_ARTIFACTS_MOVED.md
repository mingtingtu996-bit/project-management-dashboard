# UI Artifacts Moved

UI/Figma generated artifacts have been consolidated under:

`project-ui/artifacts/`

The migration ledger is:

`project-ui/index/moved-files.json`

New UI evidence should not be written back to `artifacts/uiux-*` or `artifacts/figma-design-data`.
