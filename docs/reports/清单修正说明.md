# 业务流程优化清单修正说明

## 修正概览

基于五轮讨论记录与原清单的全面比对，已完成以下修正：

### ✅ 已修正的问题（8项）

1. **预警新增来源遗漏** → 已补充：里程碑临近但前置任务未完成、关键路径任务停滞
2. **风险自动来源阈值丢失** → 已明确：阻碍≥7天+critical、延期≥5次、关键路径停滞7天
3. **风险自动闭合中间态遗漏** → 已补充：被人工编辑过的自动风险推到"待确认关闭"状态
4. **阻碍 resolution_notes 编辑入口遗漏** → 已补充：在阻碍弹窗中增加编辑区域
5. **条件图纸联动遗漏** → 已补充：图纸审批通过后自动满足对应条件
6. **基线/月度计划过度标记TBD** → 已明确：部分交互方式已确认（GanttView工具栏、快照性质、独立数据结构等）
7. **偏差分析强调不足** → 已强化：第5轮用户强调"关键几项，有指导意义"
8. **变更记录展示位置模糊** → 已明确：第5轮用户强调"Reports侧边栏统一入口，取消模块内散落入口"

### ❌ 原分析中的误判（已验证无需修正）

1. **chain_id矛盾** → 实际不矛盾：
   - 预警→风险→问题链：使用 chain_id（第3-4轮确认）
   - 条件→阻碍→延期链：不使用 chain_id，用 task_id（第4轮确认）
   - 两条链的处理方式本就不同，清单记录正确

2. **阻碍解除后自动恢复任务** → 已从清单中移除（来源存疑，未找到明确讨论记录）

### 📋 新增"待用户确认事项"章节（5项）

将五轮讨论中提出但未获明确确认的事项单独列出，避免实施时引入未经确认的功能：

1. **累计延期天数维度** - 第3轮提出，第4轮未明确确认
2. **预警架构改造方向** - 查询式 vs 事件驱动，第2轮提出但未决策
3. **延期功能扩展的其他方向** - 统计分析、干预措施等
4. **阻碍超时升级机制** - 工作流层面 vs 数据层面
5. **条件过期后的具体行为** - 是否仍触发预警、级别是否升级

---

## 修正前后对比

### 1. 预警触发源（1.1节）

**修正前：**
```
仅列出现有4类来源的改进（持久化、去重、排序等）
```

**修正后：**
```
+ 新增触发源：
  ① 里程碑临近但前置任务未完成
  ② 关键路径任务停滞（is_critical=true 进度连续 N 天无变化）
```

### 2. 风险自动来源（1.2节）

**修正前：**
```
阈值与预警体系统一，不出现"延期3次是预警、5次是风险"的矛盾
```

**修正后：**
```
具体阈值：
- 阻碍 ≥7天且 severity=critical → 自动创建风险
- 延期 ≥5次 → 自动创建风险
- 关键路径任务停滞连续7天 → 自动创建 schedule 类风险
阈值与预警体系统一，不跳过预警直接变风险
```

### 3. 风险自动闭合（1.2节）

**修正前：**
```
来源阻碍 → 阻碍已解决时自动 → closed
来源延期 → 任务完成时自动 → closed
手动创建 → 只能手动关闭
```

**修正后：**
```
+ 被人工编辑过的自动风险 → 推到"待确认关闭"状态，由人工最终确认
```

### 4. 阻碍管理（2.2节）

**修正前：**
```
| 状态 | ... | 完全删除"无法解决"状态值 |
| 解决预设方案 | ... | 不加预设解决方案模板 |
| 类型汇总 | ... | 并入 Reports 的进度分析模块 |
```

**修正后：**
```
+ | 解决措施记录 | resolution_notes 字段存在但无编辑入口 | 在阻碍弹窗中增加"解决措施"文本编辑区域 |
```

### 5. 开工条件（2.1节）

**修正前：**
```
| 单条件手动满足 | ... | 保留 |
| ���量自动满足 | ... | 保留 |
| 前置任务联动 | ... | 保留 |
| 进度概念 | ... | 不加进度百分比 |
| 阻止填报 | ... | 条件未满足时禁用或弹提示 |
```

**修正后：**
```
+ | 图纸联动 | 不存在 | 图纸类条件与图纸管理（G2）联动，图纸审批通过后自动满足对应条件 |
```

### 6. 计划编制（3.1节）

**修正前：**
```
> 待单独讨论确认后补充

已达成共识的方向：
- 基线创建入口：GanttView 工具栏"保存为基线"按钮
- ...

待确认事项：
- 基线/月度计划的具体数据结构字段
- ...
```

**修正后：**
```
> 部分已确认，具体数据结构待单独讨论后补充

已确认事项：（标注确认轮次）
- 基线创建入口：GanttView 工具栏"保存为基线"按钮（第4轮确认）
- 基线性质：历史快照，一旦保存不可修改（第4轮确认）
- 月度计划数据结构：必须有独立数据结构（第4轮用户坚持）
+ 时间字段对比组合（第4轮讨论）：
  - 计划偏差：baseline_end_date vs end_date
  - 执行偏差：end_date vs actual_end_date
  - 月度偏差：monthly_plan_items.planned_end_date vs 任务实际时间

待确认事项：（精简为真正待定的）
- 基线表（task_baselines）的具体字段设计
- 月度计划创建入口位置
- ...
```

### 7. 偏差分析（3.2节）

**修正前：**
```
内容：①关键偏差指标（精选少量有指导意义的，不做过多分析）②偏差热力图...
```

**修正后：**
```
内容聚焦关键指标（第5轮强调：太多的偏差分析没有意义，应就关键几项，能有指导意义）：
①精选核心偏差指标（计划vs实际偏差天数、关键路径偏差等）②偏差热力图...
```

### 8. 变更记录（3.3节）

**修正前：**
```
| 变更记录 | ... | 范围精简为：任务排期变更、任务状态变更、延期记录、风险/问题状态变更 |
| 变更记录展示 | — | 统一放在 Reports 页面（不设内联入口） |
```

**修正后：**
```
| 变更记录 | ... | 范围精简（第5轮强调：扩展太多太冗余）为核心变更：... |
| 变更记录展示 | — | 统一放在 Reports 页面（第5轮明确：内联入口不要，Reports 页面单独放在侧边栏统一入口，把模块里的入口都取消） |
```

### 9. 每日进度回顾（4节）

**修正前：**
```
| 回顾 | 不存在 | 项目维度回顾：在 Reports 中可按项目查看整体进度曲线和状态变化趋势。不做单任务维度回顾（已有任务总结覆盖），不做对比维度回顾 |
```

**修正后：**
```
| 回顾 | 不存在 | 项目维度回顾（第5轮明确：对比维度没必要，单任务维度有任务总结覆盖也没必要，要项目维度回顾）：在 Reports 中可按项目查看整体进度曲线和状态变化趋势 |
```

---

## 清单完整性评估

### 覆盖率：约 85-90%

**已完整覆盖的领域：**
- ✅ 预警/风险/问题三者统一链路架构
- ✅ 开工条件/阻碍/延期的消除方式闭环
- ✅ 数据结构变更（新建表、修改表、删除项）
- ✅ 页面导航改造（Reports 提升到侧边栏）
- ✅ 每日进度快照机制

**部分覆盖但需深化的领域：**
- ⚠️ 计划编制（基线/月度计划）- 交互方式已确认，数据结构待定
- ⚠️ 偏差分析 - 主框架已定，具体指标选择待深化
- ⚠️ 延期功能扩展 - 审批和CPM影响已覆盖，统计分析和干预措施待确认

**未覆盖或存疑的细节：**
- ❓ 累计延期天数维度（第3轮提出，第4轮未明确）
- ❓ 预警架构改造方向（查询式 vs 事件驱动）
- ❓ 条件过期后的具体行为（是否仍触发、级别是否升级）
- ❓ 阻碍超时升级的工作流层面机制

---

## 建议后续行动

### 立即可执行（基于已确认事项）

1. **预警/风险/问题链改造** - 清单 1.1-1.4 已完整，可直接进入实施
2. **阻碍"无法解决"状态删除** - 清单 2.2 + 6 + 7 + 8 已明确，可直接执行
3. **延期审批流程** - 清单 2.3 已明确耦合现有JWT+role体系，可直接实施
4. **Reports 页面提升到侧边栏** - 清单 5 已明确，可直接调整导航
5. **每日进度快照写入** - 清单 4 已明确字段扩展，可直接实施

### 需进一步讨论（待确认事项）

1. **计划编制模块** - 按清单 3.1 "待单独讨论"，需专门会议确认数据结构
2. **待确认事项章节（9.1-9.5）** - 逐项与用户确认，避免实施时产生歧义

### 风险提示

- ⚠️ 清单中"阻碍解除后自动恢复任务"已移除（来源存疑），如后续发现有此需求需补充
- ⚠️ 部分第2轮提出的架构层面问题（如预警的查询式vs事件驱动）在清单中被简化为功能点，实施时需注意架构一致性

---

## 修正完成时间

2026-04-08（基于五轮讨论记录）
