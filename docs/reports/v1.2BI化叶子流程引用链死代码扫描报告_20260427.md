# v1.2 BI 叶子流程引用链与死代码扫描报告

扫描时间：2026-04-27

扫描范围：
- `client/src/components/DashboardHealthCard.tsx`
- `client/src/lib/healthScore.ts`
- `client/src/lib/healthAdapter.ts`
- `client/src/lib/dashboardCalculations.ts`
- `client/src/pages/CompanyCockpit.tsx`

## 结论

1. `DashboardHealthCard.tsx` 仍然是活引用，且仍然直接依赖 `healthScore.ts`。
2. `healthScore.ts` 目前仍是活模块，不能在本轮直接删除。
3. `dashboardCalculations.ts` 在业务代码层面已经没有外部引用，属于死代码候选。
4. `healthAdapter.ts` 在业务代码层面已经没有外部引用，属于死代码候选。
5. `CompanyCockpit.tsx` 仍在前端自行聚合健康度相关指标，说明本轮还不能把健康度链路一次性删空，只能先按流程拆分和收口。

## 引用链

- `[DashboardHealthCard.tsx](c:/Users/jjj64/WorkBuddy/20260318232610/client/src/components/DashboardHealthCard.tsx#L6)` 直接从 `healthScore.ts` 导入 `getHealthLevel` 和 `HealthDetails`。
- `[healthAdapter.ts](c:/Users/jjj64/WorkBuddy/20260318232610/client/src/lib/healthAdapter.ts#L10)` 直接从 `healthScore.ts` 导入健康度计算函数。
- `[dashboardCalculations.ts](c:/Users/jjj64/WorkBuddy/20260318232610/client/src/lib/dashboardCalculations.ts#L2)` 直接从 `healthScore.ts` 导入健康度计算函数。
- `[CompanyCockpit.tsx](c:/Users/jjj64/WorkBuddy/20260318232610/client/src/pages/CompanyCockpit.tsx#L279)` 仍在前端直接汇总 `healthScore`、`attentionRequired` 和 `overdue` 口径。

## 死代码判断

### `dashboardCalculations.ts`

- 业务级搜索 `rg -l dashboardCalculations client/src` 只命中 `[healthScore.ts](c:/Users/jjj64/WorkBuddy/20260318232610/client/src/lib/healthScore.ts)` 内部注释，不存在外部业务导入。
- 结论：可按 BI 流程进入删除候选，但必须先处理所有下游引用迁移。

### `healthAdapter.ts`

- 业务级搜索 `rg -l healthAdapter client/src` 未返回业务引用。
- 结论：可按 BI 流程进入删除候选，但必须先完成前端健康度链路收口。

### `healthScore.ts`

- 该文件目前仍被 `[DashboardHealthCard.tsx](c:/Users/jjj64/WorkBuddy/20260318232610/client/src/components/DashboardHealthCard.tsx#L6)` 直接引用。
- 结论：暂不删除，等待后续健康度展示层迁移完成。

## 当前执行含义

本轮 `16.1` 的扫描结果说明：

- 先处理前端消费者迁移，再删除 `dashboardCalculations.ts` 和 `healthAdapter.ts`。
- `healthScore.ts` 不能提前删，因为 `DashboardHealthCard.tsx` 仍在使用。
- `CompanyCockpit.tsx` 的前端聚合逻辑仍然是后续收口的关键阻塞点。
