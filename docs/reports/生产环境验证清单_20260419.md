# 生产环境验证清单

> 生成时间：2026-04-19  
> 目的：确保前后端代码完整实现后，网页端无功能缺陷  
> 测试范围：200+ 需求全覆盖

---

## 一、自动化测试（优先执行）

### 1.1 单元测试 & 集成测试

**现有测试覆盖：476 个测试文件**

```bash
# 后端测试（必须全部通过）
cd server
npm test

# 前端测试（必须全部通过）
cd client
npm test

# 关键测试文件验证：
# - server/src/__tests__/acceptanceFlowService.test.ts
# - server/src/__tests__/baselineGovernanceService.test.ts
# - server/src/__tests__/certificate-work-items.test.ts
# - server/src/__tests__/constructionDrawings-currentVersion.test.ts
# - server/src/__tests__/warning-chain.test.ts
# - server/src/__tests__/issues-domain.test.ts
# - client/src/pages/__tests__/BaselinePage.test.tsx
# - client/src/pages/__tests__/AcceptanceTimeline.test.tsx
# - client/src/pages/__tests__/Dashboard.test.tsx
# - client/src/pages/__tests__/GanttView.test.tsx
```

**预期结果：**
- ✅ 所有测试通过（0 failed）
- ⚠️ 若有失败，记录失败用例并修复

---

### 1.2 TypeScript 类型检查

```bash
# 前端类型检查
cd client
npx tsc --noEmit

# 后端类型检查
cd server
npx tsc --noEmit
```

**预期结果：**
- ✅ 0 errors
- ⚠️ 若有类型错误，必须修复（类型错误会导致运行时崩溃）

---

### 1.3 构建验证

```bash
# 前端构建
cd client
npm run build

# 后端构建
cd server
npm run build
```

**预期结果：**
- ✅ 构建成功，无错误
- ✅ 生成 dist/ 目录

---

## 二、端到端功能测试（E2E）

### 2.1 预警/风险/问题链（§零-§一）

**测试路径：** `/projects/:id/risks`

#### 测试用例 E2E-1：预警确认与升级

1. **前置条件：** 创建一个任务，设置计划完成日期为昨天
2. **操作步骤：**
   - 打开风险管理页面
   - 验证"预警"tab 中出现延期预警
   - 点击预警卡片，打开详情
   - 点击"知悉"按钮
   - 等待 3 天后（或手动修改数据库 `created_at`），验证预警自动升级为风险
3. **预期结果：**
   - ✅ 预警出现在列表中
   - ✅ 知悉后状态变为"已知悉"
   - ✅ 3 天后自动升级为风险，`chain_id` 一致
   - ✅ 风险 `source_type = 'warning_auto_escalated'`

#### 测试用例 E2E-2：风险转问题

1. **前置条件：** 存在一个 `status='identified'` 的风险
2. **操作步骤：**
   - 打开风险管理页面，切换到"风险"tab
   - 点击风险卡片右上角"转为问题"按钮
   - 填写问题描述，确认创建
3. **预期结果：**
   - ✅ 问题创建成功，出现在"问题"tab
   - ✅ 问题 `source_type = 'risk_converted'`
   - ✅ 问题 `chain_id` 与风险一致
   - ✅ 风险 `linked_issue_id` 指向新问题

#### 测试用例 E2E-3：手动创建风险/问题

1. **操作步骤：**
   - 点击"新建风险"按钮
   - 填写标题、严重程度、描述
   - 确认创建
   - 重复操作创建问题
2. **预期结果：**
   - ✅ 风险/问题创建成功
   - ✅ `source_type = 'manual'`
   - ✅ 优先级分数正确计算

---

### 2.2 四层时间线（§三）

**测试路径：** `/projects/:id/planning/baseline` 和 `/projects/:id/planning/monthly`

#### 测试用例 E2E-4：项目基线确认

1. **前置条件：** 项目无基线
2. **操作步骤：**
   - 打开基线页面
   - 验证显示"建立基线"入口态
   - 点击"建立基线"
   - 在树形编辑器中调整任务
   - 点击"确认基线"
3. **预期结果：**
   - ✅ 基线版本创建成功
   - ✅ 页面切换到"查看态"
   - ✅ 显示基线版本号、确认时间
   - ✅ 任务快照保存到 `task_baseline_items`

#### 测试用例 E2E-5：月度计划编制

1. **前置条件：** 已有确认的基线
2. **操作步骤：**
   - 打开月度计划页面
   - 选择月份（如 2026-05）
   - 点击"编制本月计划"
   - 调整任务计划日期
   - 点击"确认月度计划"
3. **预期结果：**
   - ✅ 月度计划版本创建成功
   - ✅ 状态从 `draft` 变为 `confirmed`
   - ✅ 计划项保存到 `monthly_plan_items`

#### 测试用例 E2E-6：结转池与偏差分析

1. **前置条件：** 已有确认的月度计划，部分任务实际完成日期晚于计划
2. **操作步骤：**
   - 打开偏差分析页面（Reports 或 Dashboard）
   - 查看偏差散点图
   - 查看结转池候选任务
3. **预期结果：**
   - ✅ 散点图显示偏差任务
   - ✅ 结转池显示未完成任务
   - ✅ 偏差原因可填写

---

### 2.3 前期证照（12.x）

**测试路径：** `/projects/:id/pre-milestones`

#### 测试用例 E2E-7：四证推进看板

1. **操作步骤：**
   - 打开前期证照页面
   - 验证四证行（土地证、用地规划、工程规划、施工许可）全部显示
   - Hover 某个证件卡片，验证共享事项高亮
   - 点击证件卡片，打开详情抽屉
2. **预期结果：**
   - ✅ 四证行固定顺序显示
   - ✅ Hover 时共享事项高亮，非关联证件弱化
   - ✅ 详情抽屉显示依赖矩阵、办理台账、状态记录

#### 测试用例 E2E-8：共享事项依赖

1. **前置条件：** 已创建共享事项（如"规划审批"）
2. **操作步骤：**
   - 在详情抽屉中查看依赖矩阵
   - 验证矩阵单元格显示"已满足"/"待办"/"阻塞"
   - 点击单元格，验证联动高亮
3. **预期结果：**
   - ✅ 矩阵正确显示依赖关系
   - ✅ 强依赖/软依赖标签显示
   - ✅ 点击单元格时行列高亮

#### 测试用例 E2E-9：升级为风险/问题

1. **操作步骤：**
   - 在台账中选择一个阻塞的工作项
   - 点击"升级为问题"按钮
   - 填写问题描述，确认
2. **预期结果：**
   - ✅ 问题创建成功
   - ✅ `source_entity_type = 'certificate_work_item'`
   - ✅ `source_entity_id` 指向工作项 ID

---

### 2.4 施工图纸（DW.x）

**测试路径：** `/projects/:id/drawings`

#### 测试用例 E2E-10：图纸包管理

1. **操作步骤：**
   - 打开施工图纸页面
   - 点击"新建图纸包"
   - 填写包名称、专业类型、用途、送审要求
   - 确认创建
   - 点击图纸包卡片，打开详情抽屉
2. **预期结果：**
   - ✅ 图纸包创建成功
   - ✅ 按专业分组显示
   - ✅ 详情抽屉显示应有项清单、版本记录、关联影响

#### 测试用例 E2E-11：版本变更

1. **前置条件：** 已有图纸包
2. **操作步骤：**
   - 在详情抽屉中点击"查看版本窗口"
   - 点击"新增版本"
   - 填写版本号、变更原因
   - 设置为当前有效版
3. **预期结果：**
   - ✅ 版本创建成功
   - ✅ 版本时间轴显示
   - ✅ 当前有效版标记正确
   - ✅ 影响对象列表显示关联任务/验收

#### 测试用例 E2E-12：齐套检测

1. **前置条件：** 图纸包有应有项清单
2. **操作步骤：**
   - 查看图纸准备度总览
   - 验证"缺漏图纸包"数量
   - 点击"缺漏视图"
3. **预期结果：**
   - ✅ 缺漏数量正确计算
   - ✅ 缺漏视图仅显示有缺漏的图纸包
   - ✅ 齐套度百分比正确

---

### 2.5 验收流程轴（13.x）

**测试路径：** `/projects/:id/acceptance`

#### 测试用例 E2E-13：流程板布局

1. **操作步骤：**
   - 打开验收流程轴页面
   - 验证默认为流程板视图（非力导图）
   - 切换时间尺度（月/双周/周）
   - 验证节点按阶段泳道排列
2. **预期结果：**
   - ✅ 流程板为主视图
   - ✅ 五个阶段泳道显示
   - ✅ 时间尺度切换后布局重算
   - ✅ 依赖箭头从左到右

#### 测试用例 E2E-14：状态推进守卫

1. **前置条件：** 验收项状态为 `draft`，有未满足的前置项
2. **操作步骤：**
   - 点击验收项，打开详情抽屉
   - 尝试将状态改为 `ready_to_submit`（待申报）
3. **预期结果：**
   - ✅ 状态 select 被 disabled
   - ✅ 显示提示："前置项未满足，无法进入待申报"
   - ✅ 满足前置项后，状态 select 可用

#### 测试用例 E2E-15：批量操作

1. **操作步骤：**
   - 切换到台账视图
   - 勾选多个验收项
   - 点击"批量调整阶段"
   - 选择目标阶段，确认
2. **预期结果：**
   - ✅ 批量调整成功
   - ✅ 所有选中项阶段更新
   - ✅ 流程板布局自动重算

---

### 2.6 关键路径（11.x）

**测试路径：** `/projects/:id/gantt`

#### 测试用例 E2E-16：关键路径弹窗

1. **操作步骤：**
   - 打开甘特图页面
   - 点击顶部"查看关键路径"按钮
   - 验证弹窗显示主链 DAG 图
   - 验证备选链折叠/展开
2. **预期结果：**
   - ✅ 弹窗宽度 1152px（max-w-6xl）
   - ✅ 节点尺寸 180×64px
   - ✅ 主链红色边框，关注琥珀虚线，插链橙色
   - ✅ 备选链默认折叠

#### 测试用例 E2E-17：手动覆盖

1. **操作步骤：**
   - 在关键路径弹窗中，点击某个非关键任务
   - 点击"标记为关注"
   - 验证任务节点变为琥珀色
   - 点击"插到主链前面"
   - 验证任务插入主链
2. **预期结果：**
   - ✅ 手动关注任务显示琥珀色
   - ✅ 手动插链任务显示橙色
   - ✅ 覆盖记录保存到 `task_critical_overrides`
   - ✅ GanttView 中任务行显示关键路径标记

#### 测试用例 E2E-18：行级快捷操作

1. **操作步骤：**
   - 在 GanttView 任务行 hover
   - 验证出现"插到主链前面"/"插到主链后面"按钮
   - 点击按钮，验证覆盖创建
2. **预期结果：**
   - ✅ 行级按钮可见
   - ✅ 点击后覆盖创建成功
   - ✅ 关键路径弹窗中立即反映变化

---

### 2.7 Dashboard 集成（14.x）

**测试路径：** `/projects/:id/dashboard`

#### 测试用例 E2E-19：四大区块

1. **操作步骤：**
   - 打开 Dashboard
   - 验证四大区块：项目健康度、执行概况、关键节点、偏差信号
   - 点击"偏差信号"卡片，展开详情
2. **预期结果：**
   - ✅ 四大区块全部显示
   - ✅ 偏差信号显示延期任务数、最大偏差天数
   - ✅ 展开后显示 Top 5 偏差任务列表

#### 测试用例 E2E-20：今日现场

1. **操作步骤：**
   - 验证"今日现场"四个卡片：今日到期任务、今日预警、今日变更、今日新增风险/问题
   - 点击卡片，验证跳转
2. **预期结果：**
   - ✅ 四个卡片数据正确
   - ✅ 今日到期任务按 `planned_end_date` 筛选
   - ✅ 今日预警按 `created_at` 当日筛选
   - ✅ 点击卡片跳转到对应页面

---

## 三、浏览器兼容性测试

### 3.1 目标浏览器

- ✅ Chrome 120+ (主要)
- ✅ Edge 120+
- ✅ Firefox 120+
- ⚠️ Safari 17+ (次要，可能有 CSS 兼容问题)

### 3.2 测试项

- ✅ 页面布局正常
- ✅ 交互功能正常（点击、hover、拖拽）
- ✅ 弹窗/抽屉正常打开关闭
- ✅ 表单提交正常
- ✅ 图表渲染正常（关键路径 DAG、偏差散点图）

---

## 四、性能测试

### 4.1 页面加载性能

**测试工具：** Chrome DevTools Lighthouse

**测试页面：**
- Dashboard
- GanttView (100+ 任务)
- AcceptanceTimeline (50+ 验收项)
- Drawings (50+ 图纸包)

**预期指标：**
- ✅ FCP (First Contentful Paint) < 1.5s
- ✅ LCP (Largest Contentful Paint) < 2.5s
- ✅ TTI (Time to Interactive) < 3.5s
- ✅ CLS (Cumulative Layout Shift) < 0.1

### 4.2 大数据量测试

**测试场景：**
- 500+ 任务的甘特图
- 200+ 验收项的流程板
- 100+ 图纸包的看板

**预期结果：**
- ✅ 页面不卡顿（60fps）
- ✅ 滚动流畅
- ✅ 筛选/排序响应 < 500ms

---

## 五、安全测试

### 5.1 权限验证

**测试用例：**
1. 以 `viewer` 角色登录，验证无编辑权限
2. 以 `editor` 角色登录，验证有编辑权限但无删除权限
3. 以 `admin` 角色登录，验证有完整权限

**预期结果：**
- ✅ 权限控制正确
- ✅ 无权限操作被阻止（前端 + 后端双重验证）

### 5.2 XSS 防护

**测试用例：**
1. 在任务标题中输入 `<script>alert('XSS')</script>`
2. 保存后刷新页面
3. 验证脚本未执行

**预期结果：**
- ✅ 输入被转义，脚本未执行

---

## 六、错误处理测试

### 6.1 网络错误

**测试用例：**
1. 断开网络
2. 尝试提交表单
3. 验证错误提示

**预期结果：**
- ✅ 显示友好错误提示："网络连接失败，请检查网络"
- ✅ 不崩溃，可重试

### 6.2 后端错误

**测试用例：**
1. 模拟后端返回 500 错误
2. 验证前端错误处理

**预期结果：**
- ✅ 显示错误提示："服务器错误，请稍后重试"
- ✅ 不崩溃，可返回

### 6.3 数据校验

**测试用例：**
1. 提交空表单
2. 提交无效日期（结束日期早于开始日期）
3. 验证校验提示

**预期结果：**
- ✅ 显示字段级错误提示
- ✅ 阻止提交

---

## 七、回归测试清单

### 7.1 核心流程回归

每次发布前必须执行：

- [ ] E2E-1: 预警确认与升级
- [ ] E2E-4: 项目基线确认
- [ ] E2E-5: 月度计划编制
- [ ] E2E-7: 四证推进看板
- [ ] E2E-10: 图纸包管理
- [ ] E2E-13: 流程板布局
- [ ] E2E-16: 关键路径弹窗
- [ ] E2E-19: Dashboard 四大区块

### 7.2 关键交互回归

- [ ] 所有弹窗/抽屉正常打开关闭
- [ ] 所有表单提交成功
- [ ] 所有筛选/排序功能正常
- [ ] 所有导航跳转正常

---

## 八、验收标准

### 8.1 必须通过（P0）

- ✅ 所有单元测试通过
- ✅ TypeScript 类型检查通过
- ✅ 构建成功
- ✅ 核心流程 E2E 测试全部通过（E2E-1, 4, 5, 7, 10, 13, 16, 19）
- ✅ 无 P0 级 bug（崩溃、数据丢失、权限绕过）

### 8.2 建议通过（P1）

- ✅ 浏览器兼容性测试通过
- ✅ 性能指标达标
- ✅ 无 P1 级 bug（功能不可用、严重交互问题）

### 8.3 可接受（P2）

- ⚠️ 存在 P2 级 bug（视觉偏差、边缘场景问题），但不影响主流程
- ⚠️ Safari 兼容性问题（可后续修复）

---

## 九、测试执行记录模板

```markdown
## 测试执行记录

**测试日期：** 2026-04-XX  
**测试人员：** XXX  
**测试环境：** 生产环境 / 测试环境

### 自动化测试结果

- [ ] 后端单元测试：通过 / 失败（X 个用例失败）
- [ ] 前端单元测试：通过 / 失败（X 个用例失败）
- [ ] TypeScript 类型检查：通过 / 失败（X 个错误）
- [ ] 构建验证：通过 / 失败

### E2E 测试结果

- [ ] E2E-1: 通过 / 失败 / 未测试
- [ ] E2E-4: 通过 / 失败 / 未测试
- [ ] E2E-5: 通过 / 失败 / 未测试
- [ ] E2E-7: 通过 / 失败 / 未测试
- [ ] E2E-10: 通过 / 失败 / 未测试
- [ ] E2E-13: 通过 / 失败 / 未测试
- [ ] E2E-16: 通过 / 失败 / 未测试
- [ ] E2E-19: 通过 / 失败 / 未测试

### 发现的问题

| 编号 | 严重程度 | 问题描述 | 复现步骤 | 状态 |
|------|---------|---------|---------|------|
| BUG-001 | P0 | XXX | XXX | 待修复 |

### 验收结论

- [ ] 通过验收，可发布生产
- [ ] 不通过，需修复 P0/P1 问题后重新测试
```

---

## 十、快速验证脚本

```bash
#!/bin/bash
# 快速验证脚本 - 执行所有自动化测试

echo "=== 开始验证 ==="

echo "1. 后端类型检查..."
cd server && npx tsc --noEmit || exit 1

echo "2. 前端类型检查..."
cd ../client && npx tsc --noEmit || exit 1

echo "3. 后端单元测试..."
cd ../server && npm test || exit 1

echo "4. 前端单元测试..."
cd ../client && npm test || exit 1

echo "5. 后端构建..."
cd ../server && npm run build || exit 1

echo "6. 前端构建..."
cd ../client && npm run build || exit 1

echo "=== 验证完成 ==="
echo "✅ 所有自动化测试通过，可进行 E2E 测试"
```

---

**总结：**

1. **优先执行自动化测试**（单元测试 + 类型检查 + 构建），确保代码质量
2. **核心流程 E2E 测试**（8 个关键用例），覆盖 200+ 需求的主流程
3. **浏览器兼容性 + 性能测试**，确保用户体验
4. **安全 + 错误处理测试**，确保系统健壮性

**预计测试时间：**
- 自动化测试：30 分钟
- E2E 测试：2-3 小时
- 兼容性 + 性能测试：1 小时
- **总计：4-5 小时**

执行完以上测试后，可确保网页端无重大功能缺陷。
