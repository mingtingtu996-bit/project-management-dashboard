# 前期证照测试与验收清单_20260409

> 适用范围：`证照管理 / 前期证照`
> 导航口径说明：侧边栏父级最终显示名统一为 `专项管理`；本文中保留的 `证照管理` 仅用于既有专项命名和技术语境。


## 1. 页面结构验收

- 页面标题、摘要、看板、台账、抽屉四层结构齐全
- `PreMilestones` 不再承载施工图纸主视图
- 共享事项条带可见

## 2. 数据口径验收

- 四证类型映射正确
- `审批部门` 字段展示统一
- 状态中文映射正确
- 共享事项可同时关联多个证件

## 3. 交互验收

- 点击证件行可打开抽屉
- 点击共享事项可联动高亮证件行
- `新增办理事项` 可正常创建
- `升级为问题` / `升级为风险` 可成功并刷新标记

## 4. 联动验收

- 临期事项可进入预警
- 问题和风险联动成功后，详情抽屉可看到关联摘要

## 5. 回归验收

- 旧 `pre-milestones` 数据仍可被页面正确读取
- 当前项目切换后摘要与台账同步刷新

## 6. 建议测试文件

- `src/pages/__tests__/PreMilestonesBoard.test.tsx`
- `src/pages/__tests__/PreMilestonesLedger.test.tsx`
- `server/src/__tests__/preMilestoneBoardService.test.ts`
