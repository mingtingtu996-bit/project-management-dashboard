# 验收流程轴测试与验收清单_20260409

> 适用范围：`证照管理 / 验收时间轴`
> 导航口径说明：侧边栏父级最终显示名统一为 `专项管理`；本文中保留的 `证照管理` 仅用于既有专项命名和技术语境。


## 1. 页面结构验收

- `AcceptanceTimeline` 已从关系实验页收口为标准系统页
- 流程板、台账、抽屉三层结构齐全
- `ForceDirectedGraph` 不再作为主承载

## 2. 数据口径验收

- `catalog / plan / dependency / requirement / record` 五层结构可读
- 旧 `acceptance_plans` 数据可被兼容映射
- `planned_finish_date`、`phase_code`、`acceptance_name` 显示正确

## 3. 交互验收

- 点击卡片可打开抽屉
- 流程板拖拽后自动吸附
- 台账批量编辑可用
- `新增记录` 可成功写入

## 4. 联动验收

- 任务、图纸、前期证照可作为前置条件展示
- 临期、逾期、阻塞可进入预警或升级为问题

## 5. 回归验收

- 原有验收数据不丢失
- 旧接口兼容窗口内仍可读
- 当前项目切换后流程板和台账同步刷新

## 6. 建议测试文件

- `src/pages/__tests__/AcceptanceTimelineFlowBoard.test.tsx`
- `src/pages/__tests__/AcceptanceTimelineLedger.test.tsx`
- `server/src/__tests__/acceptanceFlowService.test.ts`
- `server/src/__tests__/acceptanceRequirements.test.ts`
