# v1.4.22.5 可学习工期资产 live 自升级闭环专项方案

> 本文件承接 v1.4.22.4 的收口结论。
> v1.4.22.4 证明“工期精度闭环边界、核心 runtime 校准、事实锁定、验收矩阵已收口”。
> v1.4.22.5 专门证明“所有可学习的工期预测与计划网络资产，是否真的进入 live 自学习 / 自升级闭环”。

## 0. 结论口径

允许达成的目标口径：

> 所有可学习的工期预测与计划网络资产，都已进入 live 自学习 / 自升级闭环；事实与承诺层保持锁定，只作为样本、验收和回放依据。

禁止达成或宣传的口径：

> 所有工期相关资产都已经 live 自学习、自我升级。

禁止原因：actual start/end、completion status、baseline、monthly plan、progress snapshot、用户确认依赖、用户确认关键路径等是事实或承诺，不是学习系统的 runtime writer。

## 1. 与 v1.4.22.3 / v1.4.22.4 的关系

| 文件 | 职责 | 不能替代 |
| --- | --- | --- |
| v1.4.22.3 | 治理控制面：资产登记、release-exit、rollback、冲突清理、发布锚点 | 不能证明数字更准 |
| v1.4.22.4 | 精度执行面：prediction event、actual outcome、MAE / Bias / overcompensation、runtime consumption、事实锁定验收 | 不能证明所有可学习资产都已 live 消费 |
| v1.4.22.5 | live 自升级闭环：逐项判定可学习资产是否具备五项闭环条件 | 不能把事实层改造成学习 writer |

代码边界：

- `durationPrecisionGovernanceBoundaryService.ts`：区分 v1.4.22.3 governance control、v1.4.22.4 precision learning、business fact lock。
- `durationLiveLearningClosureService.ts`：区分可学习资产、计划网络资产、事实锁定资产，并判定 live 自学习声明是否成立。

## 2. 五项强制闭环条件

每个可学习工期资产要被声明为 live 自学习，必须同时满足以下五项条件：

| 条件 | 代码字段 | 验收证据 |
| --- | --- | --- |
| 资产分级强约束 | `assetClass / canLearn / factLocked / selfLearningPublishAllowed / runtimeWriterRequired / publishGateRequired` | `durationLiveLearningClosureService.ts` 登记，测试覆盖标准工序 seed、专项工序 seed、依赖、关键路径、事实层 |
| prediction / outcome 事件 | `predictionEventRecorded / actualOutcomeEventRecorded` | 每次预测有输入、资产版本、输出影响；完工后有 actual outcome 与样本健康 |
| global / industry / company / project 分层学习 | `tieredLearningPolicyRegistered / enabledLearningScopes` | 冷启动可回退到全局/行业，公司和项目样本成熟后逐层覆盖 |
| runtime consumer 消费 published / canary artifact | `runtimeConsumerUsesPublishedArtifact` | 业务预测、剩余工期、赶工、依赖/WBS/关键路径消费者不再只看 report-only |
| 发布门禁与回滚闭环 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` | v1.4.22.3 release-exit 通过，v1.4.22.4 精度指标证明 MAE/Bias/过度补偿受控 |

任一条件缺失，只能说 `not_ready`、`candidate`、`evidence-only` 或 `shadow/report-only`，不得说 live 自学习。

## 3. 资产分级

| 资产族 | 资产键 | 分类 | 是否可学习 | runtime 声明边界 |
| --- | --- | --- | --- | --- |
| base 工期与冷启动 | `base_duration_benchmark`、`duration_cold_start_baseline` | `learnable_duration_prediction` | 是 | 可以在五项齐备后参与 runtime 预测，不写回事实 |
| 残差与置信度 | `forecast_residual_overlay`、`forecast_confidence_weight` | `learnable_duration_prediction` | 是 | published / canary 才能影响 runtime，shadow 只能留证据 |
| 标准工序 seed | `standard_work_duration_seed` | `learnable_duration_prediction` | 是 | replay 只能产候选，seed rewrite 需要专门 writer |
| 专项工序 seed / WBS / 依赖 / 关键路径 | `special_work_duration_seed`、`wbs_reference_days`、`dependency_rule_candidate`、`critical_path_rule_candidate` | `learnable_plan_network` | 是 | 先产网络 prediction/outcome，再通过 release-exit 与专项 writer |
| baseline / monthly / actual / progress | `baseline_commitment`、`monthly_plan_commitment`、`actual_duration_outcome`、`progress_snapshot` | `business_fact_lock` | 否 | 只做样本、验收、回放依据，不允许自学习改写 |

## 4. 分层学习规则

live 自升级不是单一公司桶学习，必须按以下顺序收敛：

1. `global`：全局默认基线，托底冷启动。
2. `industry`：行业/业态/项目类型基线，解决小账号长尾冻结。
3. `company`：公司级经验，样本达到门槛后覆盖上层。
4. `project`：项目内高频工序或局部节奏 overlay，样本足够时短周期校准。

下层样本不足时必须 shrink 到上层，不得用少量项目样本直接覆盖公司或全局经验。

## 5. 验收矩阵

| 验收项 | 通过条件 | 失败时口径 |
| --- | --- | --- |
| 单资产 live 自学习 | `evaluateDurationLiveLearningAsset(...).status = live_self_learning_ready` | `not_ready` |
| 全组合口径 | 所有可学习资产 ready，所有事实资产 `fact_locked_closed` | `not_ready_for_live_self_learning_claim` |
| 事实层保护 | 所有事实资产 `selfLearningPublishAllowed=false` | 阻断并返回 `duration_fact_auto_rewrite_blocked` |
| 禁止宣传语 | `prohibitedClaim = all_duration_assets_are_live_self_learning` 永远存在 | 不允许删除 |
| 正确宣传语 | `allowedClaim = all_learnable_duration_prediction_and_network_assets_are_live_self_learning;facts_and_commitments_remain_locked` | 未满足前不得使用 |

## 6. 实施顺序

1. 用 `durationLiveLearningClosureService.ts` 作为统一判定口，先拒绝错误宣传和事实层改写。
2. 给各可学习资产补 prediction event / actual outcome event。
3. 给冷启动、标准工序、专项工序、依赖/WBS/关键路径补 global / industry / company / project 分层策略。
4. 让 runtime consumer 逐个消费 published / canary artifact，并记录 consumer evidence。
5. 将 v1.4.22.3 release-exit、impact monitoring、rollback target 和 v1.4.22.4 accuracy metrics 绑定为统一发布门禁。
6. 每个资产族通过后，逐项从 `candidate/evidence-only` 升级为 `live_self_learning_ready`。

## 7. 当前状态

本文件创建时，代码已具备口径判定与阻断能力：

- `durationLiveLearningClosureService.ts` 已登记可学习资产和事实锁定资产；
- `durationLiveLearningClosureService.test.ts` 已覆盖资产分级、五项闭环、事实锁定、组合口径和缺口阻断；
- 这不等于所有业务 consumer 已经 live 消费所有资产，只说明系统已有统一判定口，后续实现必须以该判定口为准。

因此，当前可声明：

> live 自升级口径的代码级判定口已建立。

当前不可声明：

> 所有可学习工期资产已经全部 live 自学习。

### 7.1 第一批 A 类资产 manifest

已落地补充：`durationLiveLearningClosureService.ts` 新增 `duration_prediction_core_a` manifest，覆盖以下四个核心 A 类资产：

| 资产 | 当前 manifest 结论 | 已有锚点 | 主要缺口 |
| --- | --- | --- | --- |
| `base_duration_benchmark` | `manifest_not_ready` | `durationSuggestionService.ts`、`duration_prediction_events`、`durationExperienceService.ts`、`projectProductivityCalibrationService.ts`、`baseDurationBenchmarkLiveLearningEvidenceService.ts`、参数 runtime consumption / release execution | 四层样本覆盖与 runtime publication evidence bridge 已补；仍需要真实生产 evidence override 证明 global / industry / company / project 样本均已成熟，并把 impact monitoring 绑定到 base duration runtime publication |
| `duration_cold_start_baseline` | `manifest_not_ready` | `algorithmAssetColdStartBaselineService.ts`、`durationSuggestionService.ts`、`duration_prediction_events` | 需要补 cold-start actual outcome，对接 company/project shrinkage，并通过 release-exit / rollback |
| `forecast_residual_overlay` | `manifest_not_ready` | `algorithmAssetForecastResidualOverlayService.ts`、`forecastScopedRuntimeLiveLearningEvidenceService.ts`、`taskDurationForecastService.ts`、`projectRemainingDurationForecastService.ts`、`durationAlgorithmAccuracyService.ts`、release execution | company/project 受控 scope exception 判定与 evidence bridge 已补；仍需要真实 production override 证明 approved exception、runtime publication、impact monitoring 与 accuracy 全部齐备 |
| `forecast_confidence_weight` | `manifest_not_ready` | `algorithmAssetLearnableParameterRuntimeConsumptionService.ts`、`forecastScopedRuntimeLiveLearningEvidenceService.ts`、`taskDurationForecastService.ts`、`durationAlgorithmAccuracyService.ts`、release execution | company/project 受控 scope exception 判定与 evidence bridge 已补；仍需要真实 production override 证明 approved exception、runtime publication、impact monitoring 与 accuracy 全部齐备 |

代码验收：

- `listDurationLiveLearningManifests('duration_prediction_core_a')` 必须返回上述四个资产；
- `evaluateDurationLiveLearningManifest('duration_prediction_core_a')` 当前必须返回 `manifest_not_ready`，且 `prohibitedClaim=all_duration_assets_are_live_self_learning`；
- 第一批未 ready 前，允许声明只能是 `first_batch_manifest_established_not_ready_for_live_self_learning_claim`。

范围归一化补充：`resolveDurationLearningScopeCoverage(...)` 已把 runtime 层常见命名归一到 v1.4.22.5 的四层学习口径：

| runtime / baseline 命名 | v1.4.22.5 归一层级 |
| --- | --- |
| `system` | `global` |
| `industry_baseline` | `industry` |
| `segment_baseline` | `industry` |
| `company` | `company` |
| `project` | `project` |

未知 scope 必须进入 `unknownScopes`，不得被默认当作已覆盖。

### 7.2 第二批 B 类计划网络 manifest

已落地补充：`durationLiveLearningClosureService.ts` 新增 `plan_network_core_b` manifest，覆盖以下五个计划网络资产：

| 资产 | 当前 manifest 结论 | 已有锚点 | 主要缺口 |
| --- | --- | --- | --- |
| `standard_work_duration_seed` | `manifest_not_ready` | `standardWorkDurationSeedReplayService.ts`、`evaluateStandardWorkDurationSeedLiveLearningEvidence(...)`、`standardWorkDurationSeedReplayCandidateBridgeService.ts`、`buildStandardWorkDurationSeedPublicationReadiness(...)`、`durationSuggestionService.ts`、`duration_prediction_events`、`durationExperienceService.ts` | live evidence 门禁与 approved candidate publication readiness 包已补；仍需要真实 seed version 发布执行与 runtime consumer 证据，并把 replay accuracy 绑定到 release-exit / rollback |
| `special_work_duration_seed` | `manifest_not_ready` | `wbsTemplateGenerationService.ts`、`wbsTemplateCandidateEventService.ts`、`evaluateSpecialWorkDurationSeedLiveLearningEvidence(...)`、`buildSpecialWorkDurationSeedPublicationReadiness(...)` | live evidence 门禁与 approved candidate publication readiness 包已补；仍需要真实专项 seed version 发布执行与 runtime consumer 证据，并把用户 outcome / template feedback 绑定到 release-exit / rollback |
| `wbs_reference_days` | `manifest_not_ready` | `wbsTemplateGoldenBenchmarkReplayService.ts`、`wbsTemplateGoldenBenchmarkGateService.ts`、`evaluateWbsReferenceDaysLiveLearningEvidence(...)`、`buildWbsReferenceDaysPublicationReadiness(...)`、`durationAccuracyReplayAcceptanceService.ts` | live evidence 门禁与 approved candidate publication readiness 包已补；仍需要真实 WBS reference days version 发布执行与 runtime consumer 证据，并避免 feedback 静默改 seed |
| `dependency_rule_candidate` | `manifest_not_ready` | `constructionDependencyReplayCalibrationService.ts`、`evaluateConstructionDependencyRuleCandidateLiveLearningEvidence(...)`、`buildConstructionDependencyRulePublicationReadiness(...)`、`wbsTemplateGenerationService.ts`、`scheduleAccelerationService.ts` | live evidence 门禁与 approved candidate publication readiness 包已补；仍需要真实 dependency rule version 发布执行与 runtime consumer 证据，并把实际前后置兑现 outcome 绑定到 release-exit / rollback |
| `critical_path_rule_candidate` | `manifest_not_ready` | `projectCriticalPathService.ts`、`evaluateCriticalPathRuleCandidateLiveLearningEvidence(...)`、`criticalPathRulePublicationReadinessService.ts`、`buildCriticalPathRulePublicationReadiness(...)`、`projectRemainingDurationForecastService.ts`、`scheduleAccelerationRuntimeService.ts` | live evidence 门禁与 approved candidate publication readiness 包已补；仍需要真实 critical path rule version 发布执行与 runtime consumer 证据，并把 critical path outcome / float outcome 绑定到 release-exit / rollback |

代码验收：

- `listDurationLiveLearningManifests('plan_network_core_b')` 必须返回上述五个资产；
- `evaluateDurationLiveLearningManifest('plan_network_core_b')` 当前必须返回 `manifest_not_ready`；
- 第二批未 ready 前，允许声明只能是 `batch_manifest_established_not_ready_for_live_self_learning_claim`。

### 7.3 执行 gate 与下一步推荐资产

已落地补充：`durationLiveLearningClosureService.ts` 新增 `evaluateDurationLiveLearningExecutionPlan(...)`，把 A/B 两批 manifest 的缺口统一归入五个可执行 gate：

| gate | 作用 | 失败时必须做的事 |
| --- | --- | --- |
| `prediction_and_outcome_events` | 检查每次 runtime prediction 是否有 prediction event，是否有 actual outcome / network outcome | 补预测事件和实际结果事件 |
| `tiered_learning_scope` | 检查 global / industry / company / project 四层学习或明确例外 | 补分层策略，低样本层 shrink 到上层 |
| `runtime_consumer_publication` | 检查 consumer 是否真的消费 published / canary artifact | 补 runtime consumer，candidate / shadow 仍只作证据 |
| `release_monitoring_rollback` | 检查 release-exit、impact monitoring、rollback target | 补 v1.4.22.3 发布门禁和回滚绑定 |
| `accuracy_metrics` | 检查 MAE / Bias / overcompensation 是否绑定到 live artifact | 补精度指标出口 |

当前 `evaluateDurationLiveLearningExecutionPlan(['duration_prediction_core_a', 'plan_network_core_b'])` 必须返回：

- `status=execution_plan_not_ready`；
- `prohibitedClaim=all_duration_assets_are_live_self_learning`；
- `nextRecommendedAssetKeys=['duration_cold_start_baseline','standard_work_duration_seed','special_work_duration_seed']`。

补充落地：`evaluateDurationLiveLearningManifest(...)` 与 `evaluateDurationLiveLearningExecutionPlan(...)` 已支持按资产传入 runtime evidence override。默认 manifest 仍保持静态保守口径；只有当 `duration_cold_start_baseline` 传入由 `evaluateAlgorithmAssetColdStartLiveLearningEvidence(...)` 生成的完整 evidence 时，统一执行计划才允许把它从阻塞资产中移除，并把下一批推荐推进为 `standard_work_duration_seed / special_work_duration_seed / wbs_reference_days`。

因此，后续执行顺序固定为：

1. 先补 `duration_cold_start_baseline` 的 actual outcome、company/project shrinkage、release / rollback；
2. 再补 `standard_work_duration_seed` 的 seed publication writer、seed version lineage、runtime consumer 与 replay accuracy release-exit；当前先落 live evidence 门禁，不把 replay 报告误判为 live 发布；
3. 再补 `special_work_duration_seed` 的专项 seed publication writer、seed version lineage、runtime consumer 与 template feedback accuracy release-exit；当前先落 live evidence 门禁，不把 template candidate event 误判为 live 发布。

### 7.4 `duration_cold_start_baseline` live evidence 入口

已落地补充：`algorithmAssetColdStartBaselineService.ts` 新增 `evaluateAlgorithmAssetColdStartLiveLearningEvidence(...)`，把冷启动 runtime decision 转换为 v1.4.22.5 可验收证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| actual outcome | `actualOutcomeRecorded=true` 且 `actualSampleHealth=accepted` |
| 分层学习 | runtime source 至少包含 `system_seed` 与 `industry_baseline / segment_baseline`，company accepted sample 达到 override 门槛，project accepted sample 达到 overlay 门槛 |
| runtime 消费 | runtime decision 为 `shared_baseline_reference` 或 `company_override`，且 `runtimeConsumable=true`；生产桥接还必须存在合法 `runtime_consumer_observations` 观测行 |
| 发布安全 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady` 均为 true |
| 精度指标 | `accuracyMetricsAvailable=true` |

返回结果：

- 满足全部条件时：`status=cold_start_live_learning_ready`；
- 任一条件缺失时：`status=cold_start_live_learning_not_ready`，并返回缺口原因，例如 `accepted_actual_outcome_required`、`company_scope_samples_required_for_shrinkage`、`release_exit_required`。

统一闭环补充：该入口的 `liveLearningEvidence` 已可作为 `duration_cold_start_baseline` 的 runtime evidence override 传入 `durationLiveLearningClosureService.ts`。传入后只影响 manifest / execution plan 的 ready/not-ready 判定和 next recommended asset 排序，不改静态 manifest 默认值。

边界：该入口只生成 live-learning evidence，不写 actual、不写 company override、不写 shared baseline。事实与发布仍由各自业务链路和 v1.4.22.3 release-exit 管。

生产证据桥接补充：`algorithmAssetColdStartBaselineService.ts` 新增 `buildAlgorithmAssetColdStartLiveLearningEvidenceFromProductionRows(...)`，把 canonical production source rows / typed production evidence records 汇总成 `duration_cold_start_baseline` 的 `liveLearningEvidence` override。该 bridge 从 `duration_experience_samples` 读取 accepted outcome 与 `company / project` 样本 scope，从 runtime publication / impact monitoring / rollback / accuracy source rows 读取发布安全与精度证据，并要求 `runtime_consumer_observations` 存在合法 consumer 观测行且观测的 publication key 与发布证据指向同一个 runtime publication 后才承认 `runtimeConsumerUsesPublishedArtifact=true`，再复用 `evaluateAlgorithmAssetColdStartLiveLearningEvidence(...)` 判定。它只把生产证据转成 completion audit 可消费的 evidence，不写 shared baseline、不写 company override、不写任务事实；生产行不足、缺 runtime consumer observation 或发布/观测 publication 不一致时 cold-start 仍保持 `cold_start_live_learning_not_ready`。

### 7.5 `standard_work_duration_seed` live evidence / publication writer gate

已落地补充：`standardWorkDurationSeedReplayService.ts` 新增 `evaluateStandardWorkDurationSeedLiveLearningEvidence(...)`，把标准工序 seed replay report 转换为 v1.4.22.5 可验收证据。

二次补充：`standardWorkDurationSeedReplayCandidateBridgeService.ts` 新增 `buildStandardWorkDurationSeedPublicationReadiness(...)`，把 report-only replay、candidate-only bridge、approved candidate、seed version id、runtime publication key、runtime consumer observation、rollback target、release-exit 与 accuracy evidence 合成为一个 approved publication readiness package；只有 consumer observation 观测到的 `algorithm_seed_versions:*` 与 runtime publication key 完全一致时，才可把 `runtimeConsumerUsesPublishedArtifact` 判为 true。该包可把 `liveLearningEvidence` 交给 `durationLiveLearningClosureService.ts` 的 runtime evidence override，但自身不写 seed、不发布 version。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| replay prediction / outcome | replay report 存在、`evaluatedCodeCount>0`，且外部确认 `actualOutcomeEventRecorded=true` |
| 候选可发布性 | 存在 trusted replay 或 review / missing-seed candidate，且 `approvedReplayCandidateRecorded=true` |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| replay 安全边界 | replay 保持 `report_only`，所有 queue item 保持 `seedWritePolicy=never_write_seed_from_replay` |
| 专项 writer 与 lineage | `seedPublicationWriterReady=true`，`seedVersionLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，不能只停留在 candidate / report-only |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true |

返回结果：

- 满足全部条件时：`status=standard_work_seed_live_learning_ready`；
- 任一条件缺失时：`status=standard_work_seed_live_learning_not_ready`，并返回缺口原因，例如 `approved_replay_candidate_required`、`seed_publication_writer_required`、`seed_version_lineage_required`、`runtime_consumer_publication_required`、`global_industry_company_project_learning_scopes_required`。

边界：这些入口只证明“标准工序 seed 已具备可发布 live-learning evidence / readiness package”。它们不直接写 `algorithm_seed_versions`，不改系统 seed，不绕过 replay candidate review，不把 `standardWorkDurationSeedReplayService` 的 report-only 阶段升级成 runtime writer。真正 runtime 生效仍必须通过专门 seed publication writer、seed version lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

生产证据源补充：`durationLiveLearningProductionEvidenceGateService.ts` 与 `durationLiveLearningProductionEvidenceReaderService.ts` 已把 `algorithm_seed_versions` 纳入 canonical production source plan，只把 `seed_type='standard_work_duration'`、`status='active'`、`is_current=true`、且 `published_at` 非空的 seed version 转成 `standard_work_duration_seed` 的 `publication_execution` 证据。该 source 只证明标准工序 seed version 已发布，不替代 replay approval、runtime consumer observation、impact monitoring、rollback drill 或 accuracy evidence。

生产 readiness bridge 补充：`standardWorkDurationSeedReplayCandidateBridgeService.ts` 新增 `buildStandardWorkDurationSeedPublicationReadinessFromProductionRows(...)`，把 canonical production source rows / typed production evidence records 汇总成标准工序 seed 的 publication readiness package。该 bridge 从 `algorithm_seed_versions` 读取当前 active published seed version，从 `duration_experience_samples` 读取生产 outcome evidence，从 `runtime_consumer_observations` 读取 runtime consumer evidence 且要求 consumer 观测到的 `algorithm_seed_versions:*` 与发布 seed version 完全一致，并从 release / accuracy source rows 读取 impact monitoring、rollback drill 与 accuracy evidence；再复用 `evaluateStandardWorkDurationSeedLiveLearningEvidence(...)` 输出 `liveLearningEvidence` override。它只把生产证据转成 completion audit 可消费的 readiness，不写 `algorithm_seed_versions`、不写 seed record、不写 task / baseline / monthly plan；且不允许用 seed version 发布行替代 runtime consumer observation，也不允许用 A seed version 的发布证据与 B seed version 的观测证据拼成 live 闭环。

### 7.6 `special_work_duration_seed` live evidence / publication writer gate

已落地补充：`wbsTemplateCandidateEventService.ts` 新增 `evaluateSpecialWorkDurationSeedLiveLearningEvidence(...)`，把专项工序 seed / 模板候选的 generated / retained / rejected / pending outcome 转换为 v1.4.22.5 可验收证据。

二次补充：`wbsTemplateCandidateEventService.ts` 新增 `buildSpecialWorkDurationSeedPublicationReadiness(...)`，把 candidate-only 模板事件、approved candidate event、专项 seed version id、runtime publication key、rollback target、release-exit 与 accuracy evidence 合成为一个 approved publication readiness package。该包可把 `liveLearningEvidence` 交给 `durationLiveLearningClosureService.ts` 的 runtime evidence override，但自身不写模板、不写任务、不发布 seed version。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| network prediction | `networkPredictionEventRecorded=true`，且 `generatedRowCount>0` |
| 用户 outcome | `templateFeedbackOutcomeRecorded=true`，retained + rejected 覆盖全部 generated rows，`pendingRowCount=0` |
| 候选可发布性 | `approvedSpecialSeedCandidateRecorded=true`，不能只停留在 template candidate event |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| candidate 安全边界 | `governedCandidateOnlyBoundaryPreserved=true`；候选事件仍保持 candidate-only，不直接写 runtime |
| 专项 writer 与 lineage | `specialSeedPublicationWriterReady=true`，`seedVersionLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，模板生成 / 工期消费者消费 published / canary artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true |

返回结果：

- 满足全部条件时：`status=special_work_seed_live_learning_ready`；
- 任一条件缺失时：`status=special_work_seed_live_learning_not_ready`，并返回缺口原因，例如 `network_prediction_event_required`、`network_outcome_event_required`、`all_candidate_rows_must_have_user_outcome`、`special_seed_publication_writer_required`、`runtime_consumer_publication_required`。

边界：这些入口只证明“专项工序 seed 已具备可发布 live-learning evidence / readiness package”。它们不直接修改 `wbs_templates`、任务、baseline、monthly plan 或 `algorithm_seed_versions`，不把用户确认的计划网络事实改成学习 writer。真正 runtime 生效仍必须通过专项 seed publication writer、seed version lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

生产 readiness bridge 补充：`wbsTemplateCandidateEventService.ts` 新增 `buildSpecialWorkDurationSeedPublicationReadinessFromProductionRows(...)`，把 canonical production source rows / typed production evidence records 汇总成专项工序 seed 的 publication readiness package。该 bridge 从 `wbs_template_runtime_publications` 读取 `asset_kind='special_work_duration_seed'` 且 `runtime_publication_status='runtime_published'` 的 seed version 与 runtime publication key，从 `runtime_consumer_observations` 读取模板生成 / 工期消费者的 runtime consumption evidence，并要求 consumer observation 的 `publication_key` 与同一次 `wbs_template_runtime:*` 发布证据完全一致；从 `wbs_template_runtime_events` 或 publication row payload 读取 impact monitoring / rollback drill，并从 accuracy source rows 读取精度证据；模板用户 outcome 以 `network_outcomes:` 类型的 production evidence record 承接。它只把生产证据转成 completion audit 可消费的 readiness，不写 `wbs_templates`、不写任务、不写 baseline / monthly plan、不发布 runtime artifact，也不允许用 runtime publication row 冒充 runtime consumer observation，或用 A 专项 seed 发布证据与 B 专项 seed 观测证据拼成 live 闭环。

### 7.7 `wbs_reference_days` live evidence / benchmark gate

已落地补充：`wbsTemplateGoldenBenchmarkGateService.ts` 新增 `evaluateWbsReferenceDaysLiveLearningEvidence(...)`，把 WBS golden benchmark runtime gate 与 reference-days 发布证据合成为 v1.4.22.5 可验收证据。

二次补充：`wbsTemplateGoldenBenchmarkGateService.ts` 新增 `buildWbsReferenceDaysPublicationReadiness(...)`，把通过的 golden benchmark gate、template reference-days outcome、approved reference-days candidate、reference-days version id、runtime publication key、runtime consumer observation ref、consumer observed publication key、rollback target、release-exit 与 accuracy evidence 合成为一个 approved publication readiness package；只有 consumer observed publication key 与 runtime publication key 完全一致时，才可把 `runtimeConsumerUsesPublishedArtifact` 判为 true。该包可把 `liveLearningEvidence` 交给 `durationLiveLearningClosureService.ts` 的 runtime evidence override，但自身不写模板、不写任务、不发布 reference-days version，也不得用 runtime publication key 单独冒充 runtime consumer 证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| benchmark replay | `evaluateWbsTemplateGoldenBenchmarkRunGate(...).status=pass`，13 个真实项目场景通过 coverage、deep coverage、duration deviation、dependency pass、row-count、duration output evidence |
| template reference-days outcome | `templateReferenceDaysOutcomeRecorded=true`，必须有 template feedback / actual schedule outcome，不只看 synthetic replay |
| 候选可发布性 | `approvedReferenceDaysCandidateRecorded=true`，不能只停留在 benchmark finding |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| 专项 writer 与 lineage | `referenceDaysPublicationWriterReady=true`，`referenceDaysLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，WBS 生成 / 剩余工期消费者消费 published / canary reference-days artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true，且 accuracy 必须建立在通过的 benchmark gate 上 |

返回结果：

- 满足全部条件时：`status=wbs_reference_days_live_learning_ready`；
- 任一条件缺失时：`status=wbs_reference_days_live_learning_not_ready`，并返回缺口原因，例如 `wbs_reference_days_benchmark_gate_required`、`template_reference_days_outcome_required`、`wbs_reference_days_publication_writer_required`、`wbs_reference_days_lineage_required`。

边界：这些入口只证明“WBS reference days 已具备可发布 live-learning evidence / readiness package”。它们不直接修改模板、任务、baseline、monthly plan、依赖边或关键路径事实。golden benchmark replay 只是 release / accuracy 证据，不是 runtime writer；真正 runtime 生效仍必须通过 reference-days publication writer、lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

runtime publication writer 补充：新增 `wbsTemplateRuntimePublicationService.ts`，统一承接 `special_work_duration_seed / wbs_reference_days` 的 scoped runtime publication、consumer resolve 与 rollback。该 writer 只写 `wbs_template_runtime_publications / wbs_template_runtime_events`，不写 `wbs_templates / wbs_template_nodes / tasks / task_baselines / algorithm_seed_records / algorithm_seed_versions`；`wbs_reference_days` 的 live 声明仍必须同时具备 template reference-days outcome、runtime consumer observation、impact monitoring、rollback 和 accuracy 证据，不能用 writer 存在替代生产闭环。

生产 readiness bridge 补充：`wbsTemplateGoldenBenchmarkGateService.ts` 新增 `buildWbsReferenceDaysPublicationReadinessFromProductionRows(...)`，把 canonical production source rows / typed production evidence records 汇总成 WBS reference days 的 publication readiness package。该 bridge 从 `wbs_template_runtime_publications` 读取 `asset_kind='wbs_reference_days'` 且 `runtime_publication_status='runtime_published'` 的 reference-days version 与 runtime publication key，从 `runtime_consumer_observations` 读取 WBS 生成 / 剩余工期消费者的 runtime consumption evidence，并要求 consumer observation 的 `publication_key` 与同一次 `wbs_reference_days_runtime:*` 发布证据完全一致；从 runtime publication / event rows 读取 impact monitoring 与 rollback drill，并从 accuracy source rows 读取精度证据；template reference-days outcome 以 `network_outcomes:` 类型 production evidence record 承接。它只把生产证据转成 completion audit 可消费的 readiness，不写模板、不写任务、不写 baseline / monthly plan、不写依赖或关键路径事实，也不允许用 golden benchmark replay 或 runtime publication row 冒充真实 outcome / runtime consumer observation，或用 A reference-days 发布证据与 B reference-days 观测证据拼成 live 闭环。

### 7.8 `dependency_rule_candidate` live evidence / replay calibration gate

已落地补充：`constructionDependencyReplayCalibrationService.ts` 新增 `evaluateConstructionDependencyRuleCandidateLiveLearningEvidence(...)`，把 L3 / L4 dependency replay calibration report 转换为 v1.4.22.5 可验收证据。

二次补充：`constructionDependencyReplayCalibrationService.ts` 新增 `buildConstructionDependencyRulePublicationReadiness(...)`，把 report-only replay、L3/L4 calibration candidates、approved dependency-rule candidate、dependency rule version id、runtime publication key、rollback target、release-exit 与 accuracy evidence 合成为一个 approved publication readiness package。该包可把 `liveLearningEvidence` 交给 `durationLiveLearningClosureService.ts` 的 runtime evidence override，但自身不改 L3/L4 seed、不写 `task_dependencies`、不发布 dependency rule version。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| dependency replay prediction | `reportCode=construction_dependency_replay_calibration`，且 `inputDependencyCount>0` |
| actual dependency outcome | `dependencyOutcomeEventRecorded=true`，且 `comparableActualDateCount>0` |
| 候选可发布性 | 存在 L3 lag calibration 或 L4 conflict quarantine candidate，且 `approvedDependencyRuleCandidateRecorded=true` |
| replay 安全边界 | replay 保持 `report_only`，`seedWritePolicy=never_write_seed_from_replay`，`taskDependencyWritePolicy=never_write_task_dependencies_from_replay` |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| 专项 writer 与 lineage | `dependencyRulePublicationWriterReady=true`，`dependencyRuleLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，WBS 生成 / 赶工 / 依赖建议消费者消费 published / canary dependency artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true |

返回结果：

- 满足全部条件时：`status=dependency_rule_candidate_live_learning_ready`；
- 任一条件缺失时：`status=dependency_rule_candidate_live_learning_not_ready`，并返回缺口原因，例如 `dependency_replay_report_required`、`dependency_actual_outcome_required`、`dependency_rule_publication_writer_required`、`dependency_rule_lineage_required`。

边界：这些入口只证明“依赖规则候选已具备可发布 live-learning evidence / readiness package”。它们不直接改 L3 / L4 seed、不写 `task_dependencies`、不改用户确认依赖、baseline、monthly plan 或关键路径事实。真正 runtime 生效仍必须通过 dependency rule publication writer、lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

runtime publication writer 补充：新增 `constructionDependencyRuleRuntimePublicationService.ts`，承接 dependency rule 的 runtime publication、consumer resolve 与 rollback。该 writer 只写 `construction_dependency_rule_runtime_publications / construction_dependency_rule_runtime_events`，不写 `task_dependencies / algorithm_seed_records / algorithm_seed_versions`；`dependency_rule_candidate` 的 live 声明仍必须由实际前后置兑现 outcome、consumer observation、impact monitoring、rollback 和 accuracy 共同证明，不能把 replay report 或 publication row 当成 outcome。

生产 readiness bridge 补充：`constructionDependencyReplayCalibrationService.ts` 新增 `buildConstructionDependencyRulePublicationReadinessFromProductionRows(...)`，把 canonical production source rows / typed production evidence records 汇总成依赖规则候选的 publication readiness package。该 bridge 从 `construction_dependency_rule_runtime_publications` 读取 `dependency_rule_lineage.assetType='dependency_rule_candidate'` 且 `runtime_publication_status='runtime_published'` 的 dependency rule version 与 runtime publication key，从 `runtime_consumer_observations` 读取 WBS 生成 / 赶工消费者的 runtime consumption evidence，并要求 consumer observation 的 `publication_key` 与同一次 `dependency_rule_runtime:*` 发布证据完全一致；从 runtime publication / event rows 读取 impact monitoring 与 rollback drill，并从 accuracy source rows 读取精度证据；实际前后置兑现 outcome 以 `network_outcomes:` 类型 production evidence record 承接。它只把生产证据转成 completion audit 可消费的 readiness，不改 L3 / L4 seed、不写 `task_dependencies`、不写 baseline / monthly plan、不改用户确认依赖边或关键路径事实，也不允许用 dependency runtime publication row 冒充实际 outcome / runtime consumer observation，或用 A dependency rule 发布证据与 B dependency rule 观测证据拼成 live 闭环。

### 7.9 `critical_path_rule_candidate` live evidence / CPM projection gate

已落地补充：`projectCriticalPathService.ts` 新增 `evaluateCriticalPathRuleCandidateLiveLearningEvidence(...)`，把 CPM critical path snapshot、network lineage、float projection 与关键路径规则候选发布证据合成为 v1.4.22.5 可验收证据。

二次补充：新增 `criticalPathRulePublicationReadinessService.ts` 与 `buildCriticalPathRulePublicationReadiness(...)`，把 CPM snapshot、critical path outcome、approved critical-path-rule candidate、critical path rule version id、runtime publication key、rollback target、release-exit 与 accuracy evidence 合成为一个 approved publication readiness package。该包可把 `liveLearningEvidence` 交给 `durationLiveLearningClosureService.ts` 的 runtime evidence override，但自身不改任务日期、不改 `task_critical_overrides`、不发布 critical path rule version。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| critical path prediction | `criticalPathSnapshot.networkLineage.criticalPathInputHash / criticalSetHash` 存在，且自动关键路径 / 展示关键路径 / task snapshot 至少有一类证据 |
| float / projection evidence | snapshot 中存在关键任务集合，且 task snapshot 有可计算 `floatDays` |
| actual critical path outcome | `criticalPathOutcomeEventRecorded=true`，且 `projectDurationDays>0`；后续可接项目实际工期、关键任务 float 偏差或关键链兑现 outcome |
| 候选可发布性 | `approvedCriticalPathRuleCandidateRecorded=true`，不能只停留在 CPM snapshot 或人工关注标记 |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| 专项 writer 与 lineage | `criticalPathRulePublicationWriterReady=true`，`criticalPathRuleLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，剩余工期 / 赶工 / critical path 消费者消费 published / canary critical-path-rule artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true |

返回结果：

- 满足全部条件时：`status=critical_path_rule_candidate_live_learning_ready`；
- 任一条件缺失时：`status=critical_path_rule_candidate_live_learning_not_ready`，并返回缺口原因，例如 `critical_path_prediction_snapshot_required`、`critical_path_actual_outcome_required`、`critical_path_rule_publication_writer_required`、`critical_path_rule_lineage_required`。

边界：这些入口只证明“关键路径规则候选已具备可发布 live-learning evidence / readiness package”。它们不直接改任务日期、不改 `task_critical_overrides`、不改用户确认关键路径、baseline、monthly plan 或 progress snapshot；`is_critical / total_float_days / free_float_days / criticality_weight` 仍是 CPM runtime projection，不是学习 writer。真正 runtime 自升级仍必须通过 critical path rule publication writer、lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

生产 readiness bridge 补充：`criticalPathRulePublicationReadinessService.ts` 新增 `buildCriticalPathRulePublicationReadinessFromProductionRows(...)`，把 canonical production source rows / typed production evidence records 汇总成关键路径规则候选的 publication readiness package。该 bridge 从 `construction_dependency_rule_runtime_publications` 读取 `critical_path_rule_runtime:*` publication key 与 `runtime_publication_status='runtime_published'` 的 critical-path rule version，从 `runtime_consumer_observations` 读取剩余工期 / 赶工消费者的 runtime consumption evidence，并要求 consumer observation 的 `publication_key` 与同一次 `critical_path_rule_runtime:*` 发布证据完全一致；从 runtime publication / event rows 读取 impact monitoring 与 rollback drill，并从 accuracy source rows 读取精度证据；critical path / float outcome 以 `network_outcomes:` 类型 production evidence record 承接。它只把生产证据转成 completion audit 可消费的 readiness，不改任务日期、不改 `task_critical_overrides`、不写 baseline / monthly plan / progress snapshot，也不允许用 CPM snapshot 或 runtime publication row 冒充 actual outcome / runtime consumer observation，或用 A critical-path rule 发布证据与 B critical-path rule 观测证据拼成 live 闭环。

### 7.10 `base_duration_benchmark` live evidence / tiered sample gate

已落地补充：新增 `baseDurationBenchmarkLiveLearningEvidenceService.ts` 与 `buildBaseDurationBenchmarkLiveLearningEvidence(...)`，把 base benchmark 的 prediction event、actual outcome、global / industry / company / project 四层样本覆盖、runtime publication key、rollback target、release-exit、impact monitoring 与 accuracy evidence 合成为 v1.4.22.5 可验收证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| prediction / outcome | `predictionEventRecorded=true` 且 `actualOutcomeEventRecorded=true` |
| 四层学习范围 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| 四层样本覆盖 | normalized `acceptedSampleCounts.global / industry / company / project` 均大于 0；真实生产门槛仍由对应样本健康与发布策略控制 |
| runtime 消费 | 存在 `runtimePublicationKey` 与匹配的 `runtimeConsumerObservationRef/runtimeConsumerPublicationKey`，表明消费者消费同一次 published / canary benchmark artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTarget / accuracyMetricsAvailable` 均齐备 |

返回结果：

- 满足全部条件时：`status=base_duration_benchmark_live_learning_ready`；
- 任一条件缺失时：`status=base_duration_benchmark_live_learning_not_ready`，并返回缺口原因，例如 `base_duration_actual_outcome_required`、`base_duration_scope_sample_coverage_required`、`impact_monitoring_required`。

边界：该入口只生成 `DurationLiveLearningEvidence` 和 lineage，可作为 `durationLiveLearningClosureService.ts` 的 runtime evidence override；它不直接写 `duration_benchmarks`，不改 seed，不改任务事实，不把少量 project 样本强行升级为 company / industry / global 经验。真正 runtime 生效仍必须通过对应 benchmark publication、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

生产 evidence bridge 补充：`baseDurationBenchmarkLiveLearningEvidenceService.ts` 新增 `buildBaseDurationBenchmarkLiveLearningEvidenceFromProductionRows(...)`，把 canonical production source rows / typed production evidence records 汇总成 base duration benchmark 的 `liveLearningEvidence` override。该 bridge 从 `duration_experience_samples` 读取 `base_duration_benchmark` 的 accepted outcome，并按 `learningScope=global / industry / company / project` 统计四层样本覆盖；从 runtime publication / impact monitoring / rollback / accuracy source rows 读取发布安全与精度证据；从 `runtime_consumer_observations` 读取消费者已消费 published artifact 的证据，并要求 consumer observation 的 `publication_key` 与同一次 `duration_benchmark_runtime:*` 发布证据完全一致。它只把生产证据转成 completion audit 可消费的 evidence，不写 benchmark、不写 seed、不写任务事实，也不允许用 runtime publication row 冒充 runtime consumer observation、用 A benchmark 发布证据与 B benchmark 观测证据拼成 live 闭环，或用 project 样本填充 global / industry 桶。

### 7.11 `forecast_residual_overlay` / `forecast_confidence_weight` scope exception gate

已落地补充：`durationLiveLearningClosureService.ts` 新增受控 scope exception 判定，且只对 `forecast_residual_overlay` 与 `forecast_confidence_weight` 开放；新增 `forecastScopedRuntimeLiveLearningEvidenceService.ts` 与 `buildForecastScopedRuntimeLiveLearningEvidence(...)`，把 company/project 范围、scope exception approval、runtime publication key、rollback target、release-exit、impact monitoring 与 accuracy evidence 合成为 v1.4.22.5 可验收证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| prediction / outcome | `predictionEventRecorded=true` 且 `actualOutcomeEventRecorded=true` |
| 受控范围 | normalized scope 至少包含 `company` 与 `project`；不把缺失的 global / industry 伪造成已覆盖 |
| 例外批准 | `scopeExceptionApprovalId` 存在，且统一判定口收到 `scopeExceptionApproved=true` |
| runtime 消费 | 存在 `runtimePublicationKey` 与匹配的 `runtimeConsumerObservationRef/runtimeConsumerPublicationKey`，表明消费者消费同一次 published / canary forecast artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTarget / accuracyMetricsAvailable` 均齐备 |

返回结果：

- 满足全部条件时：`status=forecast_scoped_runtime_live_learning_ready`；
- 任一条件缺失时：`status=forecast_scoped_runtime_live_learning_not_ready`，并返回缺口原因，例如 `forecast_scope_exception_approval_required`、`company_project_scope_required`、`impact_monitoring_required`。

边界：该例外不是把 residual overlay / confidence weight 宣称为 global / industry 自学习；它只承认这两类运行期 forecast 参数在公司级与项目级范围内可 live 自学习。base benchmark、seed、WBS、依赖、关键路径仍必须满足四层 scope，不能借用该例外。该入口不改任务事实、不改 baseline/monthly plan、不写 seed。

生产 evidence bridge 补充：`forecastScopedRuntimeLiveLearningEvidenceService.ts` 新增 `buildForecastScopedRuntimeLiveLearningEvidenceFromProductionRows(...)`，把 canonical production source rows / typed production evidence records 汇总成 `forecast_residual_overlay` 与 `forecast_confidence_weight` 的 `liveLearningEvidence` override。该 bridge 从 `algorithm_learnable_parameter_runtime_publications` 读取 published / canary forecast artifact 与 release / impact monitoring / rollback 证据，从 `runtime_consumer_observations` 读取任务工期预测或项目剩余工期预测消费者已消费该 artifact 的观测证据，并要求 consumer observation 的 `publication_key` 与同一次 forecast runtime 发布证据完全一致；从 `duration_algorithm_accuracy_events` 读取 accuracy passed 证据。只有存在匹配的 runtime consumer observation 时，publication key 才能计入 `runtimeConsumerUsesPublishedArtifact=true`。它只把生产证据转成 completion audit 可消费的 evidence，不写 overlay / weight 参数、不写任务事实、不改 baseline / monthly plan，也不允许用 runtime publication row 冒充 runtime consumer observation，或用 A forecast 参数发布证据与 B forecast 参数观测证据拼成 live 闭环。

### 7.12 全资产 completion audit

已落地补充：新增 `durationLiveLearningCompletionAuditService.ts` 与 `buildDurationLiveLearningCompletionAudit(...)`，把 v1.4.22.5 的两个 manifest、统一 execution plan、全 learnable portfolio 与事实锁定资产一次性汇总成最终 claim gate。

组合补充：`durationLiveLearningCompletionAuditService.test.ts` 已验证 completion audit 可以直接消费 base benchmark、cold start、forecast residual / confidence、standard seed、special seed、WBS reference days、dependency rule、critical path rule 各 bridge 输出的 `liveLearningEvidence`，九类可学习资产全部 ready 时才允许最终口径。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| A 类 manifest | `duration_prediction_core_a` 中 `base_duration_benchmark / duration_cold_start_baseline / forecast_residual_overlay / forecast_confidence_weight` 全部 ready |
| B 类 manifest | `plan_network_core_b` 中 `standard_work_duration_seed / special_work_duration_seed / wbs_reference_days / dependency_rule_candidate / critical_path_rule_candidate` 全部 ready |
| execution plan | 五个 gate 全部 `passed`，`nextRecommendedAssetKeys=[]` |
| portfolio | 九个 learnable assets 全部 `allowedLiveLearningClaim=true` |
| fact lock | `baseline_commitment / monthly_plan_commitment / actual_duration_outcome / progress_snapshot` 均保持 `fact_locked_closed`，且没有请求事实层 rewrite |

返回结果：

- 满足全部条件时：`status=duration_live_learning_completion_ready`，唯一允许口径为 `all_learnable_duration_prediction_and_network_assets_are_live_self_learning;facts_and_commitments_remain_locked`；
- 任一 learnable asset 缺证据或任一 fact asset 请求 rewrite 时：`status=duration_live_learning_completion_not_ready`，口径降级为 `not_ready_for_live_self_learning_claim`，并继续保留禁止口径 `all_duration_assets_are_live_self_learning`。

边界：completion audit 只汇总各 evidence / readiness bridge 的输出，不替任何资产伪造 production evidence。它能证明“证据齐备时系统允许正确口径、证据缺失时系统阻断宣传”，但不能替代真实生产样本、发布执行、runtime consumer 观测和回滚演练。

### 7.13 production evidence gate

已落地补充：新增 `durationLiveLearningProductionEvidenceGateService.ts` 与 `evaluateDurationLiveLearningProductionEvidenceGate(...)`，在 completion audit 之上再加一层只读生产证据门禁。

采集补充：同一服务新增 `collectDurationLiveLearningProductionEvidenceRefs(...)`，把 typed production evidence records 归一成 gate 可消费的 refs。该 collector 只接受生产态证据状态：`accepted / weak` 样本、`published / canary` 发布、`observed` runtime consumer、`monitoring_armed / monitoring_running / monitoring_passed` 监控、`rollback_verified / rollback_executed` 回滚、`accuracy_passed` 精度；`candidate / shadow / report_only`、空 ref、或不在 source allowlist 内的 ref 会进入 `rejectedRecords`，不得被算作生产闭环。

生产行适配补充：同一服务新增 `collectDurationLiveLearningProductionEvidenceRecordsFromRows(...)`，把 `duration_experience_samples`、`duration_plan_network_outcomes`、`duration_algorithm_accuracy_events`、`algorithm_learnable_parameter_runtime_publications`、`algorithm_learnable_parameter_release_events`、`wbs_template_runtime_publications`、`wbs_template_runtime_events`、`construction_dependency_rule_runtime_publications`、`construction_dependency_rule_runtime_events` 和 `runtime_consumer_observations` 形态的生产行快照转换成 typed production evidence records。未带资产键、缺主键、未达到生产态的行进入 `rejectedRows`，不得进入最终口径。`duration_experience_samples` 只允许证明 base / cold-start / forecast / standard seed 这类 duration-outcome 资产，不得证明 `special_work_duration_seed / wbs_reference_days / dependency_rule_candidate / critical_path_rule_candidate` 的 plan-network outcome；计划网络 outcome 必须来自 `duration_plan_network_outcomes` 或受控 `network_outcomes:` evidence。

生产证据源计划补充：同一服务新增 `listDurationLiveLearningProductionEvidenceSourcePlan(...)`，把九类 learnable assets 的生产取证计划固定为按资产类型分配的 canonical source tables、required evidence kinds 和 required fields。后续 DB reader 必须按该 source plan 取行，再交给 source row adapter；不得绕过 adapter 直接拼 refs，也不得把尚未进入 adapter allowlist 的表名当作生产闭环证据。

生产源表与资产类型绑定补充：`durationLiveLearningProductionEvidenceGateService.ts` 已把 source plan 从“所有资产共用所有表”收紧为按资产类型取证。`base_duration_benchmark / duration_cold_start_baseline / forecast_residual_overlay / forecast_confidence_weight` 的发布证据只能来自 `algorithm_learnable_parameter_runtime_publications`；`standard_work_duration_seed` 的发布证据只能来自 `algorithm_seed_versions`，发布后 impact / rollback 可由 `algorithm_learnable_parameter_release_events` 证明；`special_work_duration_seed / wbs_reference_days` 必须来自 `wbs_template_runtime_publications / events`；`dependency_rule_candidate / critical_path_rule_candidate` 必须来自 `construction_dependency_rule_runtime_publications / events`。collector 对资产类型不匹配的 source row 返回 `production_source_table_not_allowed_for_asset`，防止用参数发布表冒充 seed / WBS / 依赖 / 关键路径的 live 发布。

typed production record 绑定补充：`collectDurationLiveLearningProductionEvidenceRefs(...)` 对 direct typed records 也执行同样的 publication ref allowlist。参数类资产必须指向 `algorithm_learnable_parameter_runtime_publications:` 或对应 runtime publication ref，标准工序 seed 必须指向 `algorithm_seed_versions:`，专项 / WBS 必须指向 `wbs_template_runtime:` 或 `wbs_reference_days_runtime:`，依赖 / 关键路径必须指向 `dependency_rule_runtime:` 或 `critical_path_rule_runtime:`。泛化 `release_execution:<asset>:published` 不能单独作为 seed / plan-network asset 的 production publication evidence，避免绕过 source row adapter。`runtime_consumer_observation / impact_monitoring / rollback_drill / accuracy` 四类 typed 证据还必须携带可归属到同一资产的 `publicationKey`，缺 publication provenance 或 publicationKey 与发布执行 artifact 不一致时，按对应证据缺失处理，不能进入 live claim。

typed production sample 绑定补充：`collectDurationLiveLearningProductionEvidenceRefs(...)` 对 `production_sample` 也按资产类型分流。duration-outcome 资产接受 `duration_samples:` / `duration_outcomes:`；plan-network 资产只接受 `network_outcomes:`。`durationLiveLearningProductionEvidenceReaderService.ts` 已把 `duration_plan_network_outcomes` 纳入 canonical source plan，DB reader 可直接从该表读取 `accepted / weak` 且 `writes_runtime_directly=false / writes_fact_directly=false` 的计划网络 outcome；低层 typed records 仍只允许作为 `network_outcomes:` 兼容补充。不得用普通完工工期样本、runtime publication row 或人工 release ref 冒充网络 outcome。

direct production evidence 绑定补充：`evaluateDurationLiveLearningProductionEvidenceGate(...)` 对调用方已经归一好的 `productionEvidence` refs 也执行同一套 source prefix allowlist。即使绕过 source row adapter / typed record collector 直接传入 refs，普通 `duration_samples:` 不能证明 plan-network outcome，泛化 `release_execution:*` 不能证明 seed / plan-network publication，外部表格或人工上传 ref 不能证明 accuracy。非法 ref 按对应 evidence 缺失处理，最终口径降级为 `not_ready_for_live_self_learning_claim`。

最终 claim direct record 收口补充：`buildDurationLiveLearningProductionClaimAudit(...)` 不再把调用方手工传入的全量 typed production records 当作 production claim 证据；最终 claim 优先消费 canonical source rows 转出的 production records，并且只允许外部 typed records 兼容补充 plan-network 资产的 `production_sample` / `network_outcomes:` 证据。发布、runtime consumer observation、impact monitoring、rollback、accuracy、duration-outcome 样本和 DB reader 权威路径下的 plan-network outcome 都必须来自 canonical source rows；手工 direct records 即使 ref 前缀合法，也会以 `production_evidence_direct_record_not_allowed_for_final_claim` 进入 `rejectedRecords`，不得把测试对象或人工上传表格拼成 live 自升级证明。

runtime consumer 观测源补充：新增 `204_v14225_runtime_consumer_observations.sql`，为 `runtime_consumer_observations` 建立只读生产证据表。该表只记录消费者已观测 published / canary artifact 的证据，SQL 约束固定 `writes_runtime_directly=false`、`writes_fact_directly=false`，不得被用作 seed、task、baseline 或 monthly plan writer。

runtime consumer source row 安全补充：`collectDurationLiveLearningProductionEvidenceRecordsFromRows(...)` 现在只接受 `runtime_consumer_observations` 中同时具备 `id / asset_key / publication_key / consumer_key / observation_status='observed' / writes_runtime_directly=false / writes_fact_directly=false` 的行。缺 publication、缺 consumer 或声明会写 runtime/fact 的观测行进入 `rejectedRows`，不得被转成 `runtime_consumer_observation` 生产证据。

runtime consumer publication 归属补充：`durationRuntimeConsumerObservationIntegrationService.ts` 固定了逐资产允许的 runtime publication key 前缀，`runtime_consumer_observations.publication_key` 必须能归属到该 `asset_key`。`collectDurationLiveLearningProductionEvidenceRecordsFromRows(...)`、typed production records 与 direct `productionEvidence` 均会执行同一校验；例如 `critical_path_rule_candidate` 不能用 `duration_benchmark_runtime:*` 的观测行冒充关键路径规则消费证据。

publication-lineage 一致性补充：`durationLiveLearningProductionEvidenceGateService.ts` 现在要求同一资产的 `publication_execution`、`runtime_consumer_observation`、`impact_monitoring`、`rollback_drill` 与 `accuracy` 必须指向同一个发布 artifact。source row adapter 会把 runtime consumer / monitoring / rollback / accuracy 所属的 `publication_key / source_publication_key / prediction_context.publicationKey` 保留下来；最终 production gate 会用它与发布执行 ref 比对，不允许用 A 版本发布、B 版本 consumer observation、C 版本 impact monitoring、D 版本 rollback drill、E 版本 accuracy 拼成 ready。缺少或不匹配时按对应 evidence 缺失处理，最终 claim 降级为 `duration_live_learning_production_claim_not_ready`。

publication-lineage 选择稳定性补充：`collectDurationLiveLearningProductionEvidenceRefs(...)` 对同一资产存在多个历史 publication-bound 证据时，先确定 production publication artifact，再优先选取与该 artifact 同源的 runtime consumer / impact monitoring / rollback / accuracy 证据。若只有错版本证据，则保留该错版本证据用于输出 `runtime_consumer_publication_mismatch` / 对应 evidence 缺失诊断；若同时存在旧版和当前版证据，旧版不得因读取顺序靠前而遮住当前发布版本的完整闭环证据。

runtime consumer 观测写入补充：新增 `durationRuntimeConsumerObservationService.ts` 与 `recordDurationRuntimeConsumerObservation(...)`，作为所有 runtime consumer 记录生产观测的统一入口。该入口只允许插入 `runtime_consumer_observations`，并在调用方声明 `writesRuntimeDirectly / writesFactDirectly` 时阻断写入，避免消费者为了证明 live consumption 而顺手改 runtime artifact 或业务事实；同时校验 `consumerKey` 必须属于该资产在 manifest 中声明的 runtime consumer，且 `publicationKey` 必须属于该资产允许的 published / canary / runtime_published artifact，未声明 consumer 或错资产 publication 的观测不得进入生产证据池。

runtime consumer 批量接入补充：同一服务新增 `recordDurationRuntimeConsumerObservedArtifacts(...)`，供业务 consumer 在一次预测/排程/赶工计算中把已解析的 published / canary artifact 批量记录为观测证据。该入口复用单条 recorder 的声明 consumer 校验和 no-write 约束，并拒绝 `candidate / shadow / report_only` artifact，避免业务侧为每个 consumer 临时手写 insert。

runtime consumer 接入契约补充：新增 `durationRuntimeConsumerObservationIntegrationService.ts` 与 `listDurationRuntimeConsumerObservationIntegrationContracts(...)`，从 manifest 生成逐资产 consumer 接入契约，并固定 `duration_suggestion / task_duration_forecast / remaining_duration_forecast / wbs_template_generation / schedule_acceleration / schedule_acceleration_runtime` 等 `consumerSurface`。recorder 的 consumer 声明校验与 accepted publication status 均复用该契约，避免业务服务各自维护 consumer 名称、surface 和状态 allowlist。

runtime consumer 契约 adapter 补充：`durationRuntimeConsumerObservationService.ts` 新增 `recordDurationRuntimeConsumerObservedContractArtifacts(...)`，业务 consumer 只需传入自身 `consumerKey` 与已消费 artifact，服务会按 manifest 契约解析 `consumerSurface` 并记录观测；未声明 consumer、未发布/金丝雀/runtime_published artifact 仍会被阻断。该 adapter 是后续接真实业务消费者的统一入口，不等同于所有业务消费者已经完成自动写入。

runtime consumer 接入覆盖审计补充：`durationRuntimeConsumerObservationIntegrationService.ts` 新增 `evaluateDurationRuntimeConsumerObservationIntegrationCoverage(...)`，把 manifest 声明的逐资产 consumer 契约与已接入 adapter 的 consumer 注册表逐项比对；未接入项进入 `missingContracts`，错误注册项进入 `rejectedRegistrations`。该审计用于发布前证明“哪些 consumer 已自动记录观测、哪些仍未接入”，不得用一条业务服务接入替代全部声明 consumer 的接入证明。

runtime consumer facade 补充：新增 `durationRuntimeConsumerObservationAdapterService.ts`，为 `durationSuggestionService / taskDurationForecastService / projectRemainingDurationForecastService / wbsTemplateGenerationService / scheduleAccelerationService / scheduleAccelerationRuntimeService` 提供各自命名的 `record*ConsumedArtifacts(...)` 入口。facade 只委托 contract adapter，业务侧不再手写 `consumerKey / consumerSurface / publicationStatus allowlist`；真实 live 口径仍要求对应业务 consumer 在运行路径调用 facade 并产生 `runtime_consumer_observations` 生产行。

runtime consumer facade 注册表补充：同一 facade 服务新增 `listDurationRuntimeConsumerObservationFacadeRegistrations(...)`，按业务 consumer 声明其覆盖的资产清单；`evaluateDurationRuntimeConsumerObservationIntegrationCoverage(...)` 可用该 canonical 注册表证明 adapter 层已覆盖 manifest 的 14 个逐资产 consumer 契约。该证明只覆盖“入口齐备”，不覆盖“生产运行中已调用入口并写出观测行”。

最终 production claim 接入补充：`buildDurationLiveLearningProductionClaimAudit(...)` 已纳入 `runtimeConsumerObservationIntegrationCoverage`，默认读取 canonical facade 注册表；只有 adapter/facade 覆盖 ready 且 `runtime_consumer_observations` 生产观测覆盖 ready 时，最终 claim 才可能 ready。这样把“入口齐备”和“生产运行已观测”拆成两道门，避免用 facade 存在替代真实 runtime evidence。

runtime consumer runtime-call 审计补充：新增 `durationRuntimeConsumerObservationRuntimeCallAuditService.ts` 与 `evaluateDurationRuntimeConsumerObservationRuntimeCallCoverage(...)`，要求六个 facade-backed 业务 consumer 均提供运行路径调用证据；`buildDurationLiveLearningProductionClaimAudit(...)` 已纳入 `runtimeConsumerRuntimeCallCoverage`，缺少运行路径 evidence 时即使 facade 和生产观测行齐全，也不能给最终 live 自升级 claim。该门禁专门防止“有统一入口但真实业务路径未调用”的假闭环。runtime-call evidence 还必须匹配 business path 审计表中的 canonical `runtimeEntryRef`，且该 ref 必须锚定当前源码真实导出入口；例如 `projectRemainingDurationForecastService` 只能证明 `projectRemainingDurationForecastService:buildProjectRemainingDurationForecast`，不能用同 consumer 下任意函数名冒领。

runtime consumer runtime-call 生产源补充：新增 `205_v14225_runtime_consumer_runtime_calls.sql` 与 `runtime_consumer_runtime_calls` 只读生产证据表，记录六个 facade-backed consumer 的 `consumer_key / runtime_entry_ref / call_status`。`durationLiveLearningProductionEvidenceGateService.ts` 已把该表纳入 canonical source plan，`durationLiveLearningProductionEvidenceReaderService.ts` 已从 DB 只读查询 `call_status='called'` 且 `writes_runtime_directly=false / writes_fact_directly=false` 的行，再转换为 `runtimeConsumerRuntimeCallCoverage`。source row 转 runtime-call evidence 时还必须具备生产行 `id`，并生成 `runtime_consumer_runtime_calls:<id>` evidence ref；底层 `evaluateDurationRuntimeConsumerObservationRuntimeCallCoverage(...)` 只接受带该生产 ref 的调用证据，最终 `buildDurationLiveLearningProductionClaimAudit(...)` 也只消费 canonical source rows 转出的 runtime-call evidence，不再接受调用方手工传入的 `runtimeConsumerRuntimeCallEvidence` 作为 production claim 证明。缺 id 的手工 sourceRows 或手工 override 不得冒充可追溯 runtime-call 证据。这一步把 runtime-call 门禁从“调用方手动传 evidence”推进为“生产源表可验收”，但仍不写 runtime artifact、seed、task、baseline 或 monthly plan。

plan-network outcome 生产源补充：新增 `207_v14225_plan_network_outcomes.sql` 与 `duration_plan_network_outcomes` 只读生产证据表，承接 `special_work_duration_seed / wbs_reference_days / dependency_rule_candidate / critical_path_rule_candidate` 四类可学习计划网络资产的 accepted / weak outcome。该表固定 `writes_runtime_directly=false`、`writes_fact_directly=false`，只证明网络产出被业务结果接受或形成弱样本，不写任务日期、依赖、关键路径、baseline、monthly plan 或 runtime artifact。`durationLiveLearningProductionEvidenceGateService.ts` 已把该表纳入 plan-network source plan，并把合格行转成 `network_outcomes:<id>` production sample evidence；DB reader 因此可以在没有调用方 typed `network_outcomes:` records 时，从 canonical source rows 独立补齐 plan-network outcome 证据。

第一条 plan-network outcome 业务 writer 补充：`wbsTemplateCandidateEventService:recordWbsTemplateCandidateEvent(...)` 已在 WBS 模板候选提交路径写入 `duration_plan_network_outcomes` 的 `special_work_duration_seed` outcome。候选行全部被保留/剔除并且无 pending 时写 `outcome_status='accepted'`，部分已有用户结果但仍有 pending 时只写 `weak`；写入行固定 `writes_runtime_directly=false`、`writes_fact_directly=false`，只作为 `network_outcomes:<id>` 生产样本证据，不改模板、不改任务、不改 baseline/monthly plan，也不发布 seed/runtime artifact。该 writer 只补齐专项工序 seed 的业务 outcome 证据入口。

第二条 plan-network outcome 业务 writer 补充：`wbsTemplateFeedback:collectWbsTemplateFeedback(...)` 已在 WBS reference-days 反馈路径写入 `duration_plan_network_outcomes` 的 `wbs_reference_days` outcome。该 writer 只在完成项目实际/计划工期能结构化映射到模板节点、且产生可行动 reference-days 差异时写入；样本数达到保守门槛时写 `accepted`，不足门槛但已有真实完成样本时只写 `weak`。`wbs-template-governance` 路由已把当前公司 scope 传入 feedback 服务，避免 outcome 证据落成无公司隔离的孤儿样本。写入行同样固定 `writes_runtime_directly=false`、`writes_fact_directly=false`，只作为 `network_outcomes:<id>` 生产样本证据，不改模板、不改任务、不改 baseline/monthly plan，也不发布 WBS runtime artifact。

第三条 plan-network outcome 业务 writer 补充：`constructionDependencyReplayCalibrationService:collectAndPersistConstructionDependencyReplayCalibrationCandidates(...)` 已把真实 `task_dependencies + tasks.actual_*` 回放形成的 L3 / L4 依赖规则候选写入 `duration_plan_network_outcomes` 的 `dependency_rule_candidate` outcome。该 writer 只消费 replay calibration queue 中已有实际前后置兑现样本的候选；样本达到保守门槛且无冲突时写 `accepted`，样本不足或存在冲突 / 弱证据时只写 `weak`。写入行固定 `writes_runtime_directly=false`、`writes_fact_directly=false`，只作为 `network_outcomes:<id>` 生产样本证据，不写 `task_dependencies`、不改用户确认依赖、不改 L3/L4 seed、不改 baseline/monthly plan，也不发布 dependency runtime artifact。

第四条 plan-network outcome 业务 writer 补充：`projectCriticalPathService:recalculateProjectCriticalPath(...)` 已在项目实际完工且 CPM snapshot 为 fresh、具备 `criticalPathInputHash / criticalSetHash` 时写入 `duration_plan_network_outcomes` 的 `critical_path_rule_candidate` outcome。该 writer 用项目实际起止跨度校验关键路径 CPM 项目工期预测：误差不超过 `max(2天, 预测工期20%)` 且存在自动关键任务时写 `accepted`，否则仅写 `weak`。写入行固定 `writes_runtime_directly=false`、`writes_fact_directly=false`，只作为 `network_outcomes:<id>` 生产样本证据，不写任务日期、不改用户确认关键路径、不写 `task_critical_overrides`、不改 baseline/monthly plan，也不发布 critical-path runtime artifact。

至此，`special_work_duration_seed / wbs_reference_days / dependency_rule_candidate / critical_path_rule_candidate` 四类计划网络资产均已具备各自的业务 outcome writer。该结论只表示 plan-network production sample evidence 入口齐备；最终 live 自升级口径仍必须继续通过 runtime consumer observation、runtime-call、真实业务路径接入、publication execution、impact monitoring、rollback drill 与 accuracy evidence 的全门禁，不能把四条 writer 齐备偷换为“四类计划网络资产已经全部 live 自学习”。

runtime consumer runtime-call 写入补充：`durationRuntimeConsumerObservationService.ts` 新增 `recordDurationRuntimeConsumerRuntimeCall(...)`，只允许声明过的 duration runtime consumer 写入 `runtime_consumer_runtime_calls`，并阻断 `writesRuntimeDirectly / writesFactDirectly`。写入时同时校验 `runtimeEntryRef` 必须等于该 consumer 的 canonical facade-backed runtime path，错误 entry ref 会被阻断，避免同一 consumer 名称下的其它函数冒充真实预测/排程入口。`durationRuntimeConsumerObservationAdapterService.ts` 的六个命名 facade 已在记录 artifact observations 前自动记录 runtime-call evidence，并返回 `runtimeCallResult`；这证明 facade 入口具备统一写入能力，但仍不等同于六个真实业务服务的所有运行路径已经全部调用 facade。

runtime consumer 真实业务路径接入审计补充：新增 `durationRuntimeConsumerBusinessPathIntegrationAuditService.ts`，固定六个 facade-backed consumer 与业务源码路径、facade 函数、runtime entry ref 的对应关系。`buildDurationLiveLearningProductionClaimAudit(...)` 已纳入 `runtimeConsumerBusinessPathIntegrationCoverage`；即使 facade、生产观测行、runtime-call 生产行全部齐全，只要业务源码没有证明 `durationSuggestionService / taskDurationForecastService / projectRemainingDurationForecastService / wbsTemplateGenerationService / scheduleAccelerationService / scheduleAccelerationRuntimeService` 调用了对应 facade，最终 claim 仍降级为 `duration_live_learning_production_claim_not_ready`。这道门禁专门防止“有入口、有表、有测试行，但真实业务路径未接入”的假闭环。

真实源码路径自动验收补充：`durationRuntimeConsumerBusinessPathIntegrationAuditService.ts` 新增 `loadDurationRuntimeConsumerBusinessPathSourceFiles(...)`，按固定六个业务服务路径只读加载当前仓库源码；`buildDurationLiveLearningProductionClaimAuditFromDb(...)` 固定使用该 loader，不再接受调用方手工传入的 `runtimeConsumerBusinessPathSourceFiles`。最终 DB reader 因此不依赖测试或人工调用方手工塞 source text，也不能在真实源码未调用 facade 时给出 production claim ready。业务路径审计要求声明的 `runtimeEntryRef` 对应函数体内出现具体 facade 调用表达式；单纯 import `record*ConsumedArtifacts`、只在文件中出现函数名、或只在旁路 helper 中调用 facade 都不算真实业务路径接入。

第一条真实业务 consumer 接入补充：`projectRemainingDurationForecastService.ts` 新增 `recordProjectRemainingDurationForecastRuntimeConsumption(...)` 与 `buildProjectRemainingDurationForecastConsumedArtifacts(...)`，把 `forecast_residual_overlay / wbs_reference_days / critical_path_rule_candidate` 三类已发布或金丝雀 runtime artifact 转成 `recordProjectRemainingDurationForecastConsumedArtifacts(...)` 的统一 facade 调用。该服务保持原 `buildProjectRemainingDurationForecast(...)` 同步 forecast 返回不变；当调用方传入 `runtimeConsumerObservationQueryExec` 与可追溯 `runtimeArtifactPublications` 时，运行入口本体会直接记录 `runtime_consumer_runtime_calls` 与 `runtime_consumer_observations` 生产证据。缺 publication key/status、candidate/report-only、或不属于 remaining forecast consumer 的 artifact 不会被记录，避免把 dependency rule 等其它 consumer 资产冒领到项目剩余工期预测入口。

第二条真实业务 consumer 接入补充：`scheduleAccelerationRuntimeService.ts` 新增 `recordScheduleAccelerationRuntimeConsumption(...)` 与 `buildScheduleAccelerationRuntimeConsumedArtifacts(...)`，把 `critical_path_rule_candidate` 的 published / canary / runtime_published artifact 转成 `recordScheduleAccelerationRuntimeConsumedArtifacts(...)` 的统一 facade 调用。`evaluateRuntimeScheduleAcceleration` 运行入口已在传入 `runtimeConsumerObservationQueryExec` 与可追溯 `runtimeArtifactPublications` 时直接记录 runtime-call / observation 证据；该 consumer 只记录 critical-path rule consumption，不把 dependency rule 混入 runtime service 证据。同时修正 E4 project remaining forecast 对 E2 P80 confidence band 的使用边界：单链/无 merge bias 时 P80 可扩宽最终 forecast finish；多条近关键链已经由 merge bias 表达 spread，不再重复叠加最大 P80 finish。

第三条真实业务 consumer 接入补充：`scheduleAccelerationService.ts` 新增 `recordScheduleAccelerationRuntimeConsumption(...)` 与 `buildScheduleAccelerationConsumedArtifacts(...)`，把 `dependency_rule_candidate` 的 published / canary / runtime_published artifact 转成 `recordScheduleAccelerationConsumedArtifacts(...)` 的统一 facade 调用。`evaluateRuntimeDelayRecoveryWithCriticalPath` 运行入口已在传入 `runtimeConsumerObservationQueryExec` 与可追溯 `runtimeArtifactPublications` 时直接记录 runtime-call / observation 证据；该 consumer 只记录 dependency-rule consumption，不把 critical-path artifact 混入 scheduleAccelerationService 的 consumer 证据，避免跨 consumer 冒领。

第四条真实业务 consumer 接入补充：`durationSuggestionService.ts` 新增 `recordDurationSuggestionRuntimeConsumption(...)` 与 `buildDurationSuggestionConsumedArtifacts(...)`，把 `base_duration_benchmark / duration_cold_start_baseline / standard_work_duration_seed / special_work_duration_seed` 四类 runtime artifact 转成 `recordDurationSuggestionConsumedArtifacts(...)` 的统一 facade 调用。该 helper 不记录 forecast residual、confidence weight、dependency 或 critical-path artifact，避免把其它 consumer 的学习资产冒领到工期建议入口。

第四条运行路径接入补充：`durationSuggestionService:getTaskDurationSuggestion` 已在标准工序 seed 分支内直接调用 `recordDurationSuggestionConsumedArtifacts(...)` facade。当前只记录真实已消费且具备 publication key/status 的 artifact：`duration.benchmark_blend_weight` 对应的 `base_duration_benchmark` runtime publication、shared cold-start baseline 的 published/canary publication、以及 `__resolverSource=active_seed` 且带 seed version 的 active seed version；缺 publication key、rolled-back/candidate/report-only artifact、TS fallback seed 或 company/project override 时不写 runtime consumer evidence。这一步证明工期建议入口本体已经从 helper-only 推进到真实业务路径写入 `runtime_consumer_runtime_calls` 与 `runtime_consumer_observations`，但只覆盖 duration suggestion consumer，不替代 task forecast、remaining forecast、WBS、dependency 或 critical-path consumer 的接入证明。

第五条真实业务 consumer 接入补充：`taskDurationForecastService.ts` 新增 `recordTaskDurationForecastRuntimeConsumption(...)` 与 `buildTaskDurationForecastConsumedArtifacts(...)`，把 `forecast_residual_overlay / forecast_confidence_weight` 两类 runtime artifact 转成 `recordTaskDurationForecastConsumedArtifacts(...)` 的统一 facade 调用。该 helper 绑定 task forecast 的 task/project/finish/remaining/confidence 上下文，只记录任务剩余工期预测真正消费的 residual overlay 与 confidence weight，不把 WBS reference、seed 或 dependency artifact 混入任务预测入口。

第五条运行路径接入补充：`taskDurationForecastService:forecastTaskDuration` 已在返回任务剩余工期 forecast 前直接调用 `recordTaskDurationForecastConsumedArtifacts(...)` facade。当前运行路径会把已实际应用的 `forecast.confidence_weight_multiplier` runtime publication 转成 `forecast_confidence_weight` consumer observation；`forecast_residual_overlay` 只有在 residual overlay 自身携带可归属的 runtime publication key/status 时才记录，普通 overlay key、candidate、rolled-back 或缺 publication key 的 overlay 不会被当成 live artifact。二次收口：新增 `209_v14225_forecast_residual_overlay_publication_key.sql` 与 `duration_forecast_residual_overlays.publication_key`，`persistAlgorithmAssetForecastResidualOverlayEvaluation(...)` 会为可发布 overlay 写入 `forecast_residual_overlay_runtime:*` lineage，`forecastTaskDuration` 会把该 key 透传到 `forecastSources.residualOverlay` 并写出 `runtime_consumer_runtime_calls / runtime_consumer_observations`。这一步证明 task forecast 入口本体已经从 helper-only 推进到真实业务路径写入 residual overlay / confidence weight 的 runtime-call / observation 证据；但最终 `forecast_residual_overlay` live 闭环仍必须同时满足 production publication、impact monitoring、rollback、accuracy 与 runtime consumer 覆盖门禁，不能用单条 observation 替代全门禁。

第六条真实业务 consumer 接入补充：`wbsTemplateGenerationService.ts` 新增 `recordWbsTemplateGenerationRuntimeConsumption(...)` 与 `buildWbsTemplateGenerationConsumedArtifacts(...)`，把 `special_work_duration_seed / wbs_reference_days / dependency_rule_candidate` 三类 runtime artifact 转成 `recordWbsTemplateGenerationConsumedArtifacts(...)` 的统一 facade 调用。`generateWbsTemplateRows` 运行入口已在传入 `runtimeConsumerObservationQueryExec` 与可追溯 `runtimeArtifactPublications` 时直接记录 runtime-call / observation 证据，并绑定 generationBatchId、templateId/templateIds、generationDepth 与 rowCount；不记录 residual overlay、confidence weight 或 critical-path artifact，且 `special_work_duration_seed` 必须使用 `wbs_template_runtime:*` 这类可归属发布键，避免跨 consumer 或错来源冒领。

runtime consumer 覆盖补充：`durationLiveLearningProductionEvidenceGateService.ts` 新增 `listDurationLiveLearningExpectedRuntimeConsumerObservations(...)` 与 `evaluateDurationRuntimeConsumerObservationCoverage(...)`，从 manifest 的 `implementationAnchors.runtimeConsumers` 生成逐资产 consumer 清单，并要求每个声明 consumer 都有 `runtime_consumer_observations.consumer_key` 观测。最终 production claim 不再只接受“每个资产一条 consumer ref”，而是必须满足全部 consumer 覆盖；缺任一 consumer 时即使 production refs 齐全，也降级为 `duration_live_learning_production_claim_not_ready`。

生产证据 DB reader 补充：新增 `durationLiveLearningProductionEvidenceReaderService.ts`，提供 `loadDurationLiveLearningProductionEvidenceSourceRows(...)` 与 `buildDurationLiveLearningProductionClaimAuditFromDb(...)`。该 reader 只按 `listDurationLiveLearningProductionEvidenceSourcePlan(...)` 的 canonical source tables 做只读查询，包括参数类 publication 表、标准工序 `algorithm_seed_versions` 当前 active seed version、`duration_plan_network_outcomes` 计划网络 outcome 表、WBS 模板 runtime publication/event 表、依赖/关键路径规则 runtime publication/event 表、`runtime_consumer_observations` 和 `runtime_consumer_runtime_calls`，再把 source rows 交给 adapter / collector / production gate；SQL 不触碰 seed record 写入表、task、baseline、monthly plan 或 runtime writer 表，避免调用方手工拼 rows 绕过生产证据门禁。

DB completion audit 派生补充：`buildDurationLiveLearningProductionClaimAuditFromDb(...)` 不再把调用方传入的 `completionAudit` 当作生产证明。DB reader 会先从 canonical source rows 经 source row adapter / collector 生成 production evidence refs，再把这些 refs 转成 `buildDurationLiveLearningCompletionAudit(...)` 可消费的 evidence overrides；外部 typed records 仍只允许补充 plan-network 资产的 `production_sample / network_outcomes:`。因此，即使调用方传入伪造、过期或测试夹具生成的 ready completion audit，只要生产 source rows / network outcome records 不足，最终仍必须降级为 `duration_live_learning_production_claim_not_ready`；反过来，即使调用方传入 not-ready audit，只要 DB 生产证据足够，reader 也以生产证据派生的 audit 为准。

最终入口补充：同一服务新增 `buildDurationLiveLearningProductionClaimAudit(...)`，低层组合函数仍接收 completion audit 与 production source rows；typed production records 在最终 claim 中只允许补充 plan-network 的 `network_outcomes:` 样本。该入口内部固定执行 source row adapter -> collector -> production evidence gate，避免业务侧绕过生产证据门禁直接使用 completion audit 的 ready 结果对外宣传。生产 DB 入口的权威路径是 `buildDurationLiveLearningProductionClaimAuditFromDb(...)`：它必须自己派生 completion audit，不接受调用方 completion audit 作为 final production claim 的证明。

该门禁要求每个 learnable asset 同时具备：

| 生产证据 | 说明 |
| --- | --- |
| `productionSampleEvidenceRef` | 指向真实 accepted/weak 样本或 outcome 证据，不接受合成测试样本替代 |
| `publicationExecutionRef` | 指向 v1.4.22.3 release execution / published version 记录 |
| `runtimeConsumerObservationRef` | 指向 runtime consumer 已消费 published / canary artifact 的观测记录 |
| `impactMonitoringEvidenceRef` | 指向发布后 impact monitoring armed / running / passed 证据 |
| `rollbackDrillEvidenceRef` | 指向 rollback target 已演练或可执行证据 |
| `accuracyEvidenceRef` | 指向 v1.4.22.4 MAE / Bias / overcompensation 等精度证据 |

返回结果：

- completion audit 未 ready 或任一生产证据缺失时：`status=duration_live_learning_production_evidence_not_ready`，并返回缺失资产与原因；
- completion audit ready 且九类可学习资产全部具备生产证据引用时：`status=duration_live_learning_production_evidence_ready`，才允许沿用 `all_learnable_duration_prediction_and_network_assets_are_live_self_learning;facts_and_commitments_remain_locked`；
- 禁止口径 `all_duration_assets_are_live_self_learning` 仍然保留，事实与承诺层不因生产证据门禁存在而变成 writer。

边界：production evidence gate 仍是只读验收门禁，不写 seed、不写 task、不写 baseline/monthly plan、不发布 runtime artifact。它只防止用 readiness package 或测试夹具冒充真实生产闭环。
