# v1.4.22.5 可学习工期资产 live 自升级闭环专项方案

> 本文件承接 v1.4.22.4 的收口结论。
> v1.4.22.4 证明“工期精度闭环边界、核心 runtime 校准、事实锁定、验收矩阵已收口”。
> v1.4.22.5 专门证明“所有可学习的工期预测与计划网络资产，是否真的进入 live 自学习 / 自升级闭环”。

## 0. 结论口径

允许达成的目标口径：

> 所有可学习的工期预测与计划网络资产，都已进入 live 自学习 / 自升级闭环；事实与承诺层保持锁定，只作为样本、验收和回放依据。

禁止达成或宣传的口径：

> 所有工期相关资产都已经 live 自学习、自我升级。

禁止原因：actual start/end、completion status、baseline、monthly plan、progress snapshot、用户确认依赖、用户确认关键路径等是事实或承诺，不是学习系统的 runtime writer。

## 1. 与 v1.4.22.3 / v1.4.22.4 的关系

| 文件 | 职责 | 不能替代 |
| --- | --- | --- |
| v1.4.22.3 | 治理控制面：资产登记、release-exit、rollback、冲突清理、发布锚点 | 不能证明数字更准 |
| v1.4.22.4 | 精度执行面：prediction event、actual outcome、MAE / Bias / overcompensation、runtime consumption、事实锁定验收 | 不能证明所有可学习资产都已 live 消费 |
| v1.4.22.5 | live 自升级闭环：逐项判定可学习资产是否具备五项闭环条件 | 不能把事实层改造成学习 writer |

代码边界：

- `durationPrecisionGovernanceBoundaryService.ts`：区分 v1.4.22.3 governance control、v1.4.22.4 precision learning、business fact lock。
- `durationLiveLearningClosureService.ts`：区分可学习资产、计划网络资产、事实锁定资产，并判定 live 自学习声明是否成立。

## 2. 五项强制闭环条件

每个可学习工期资产要被声明为 live 自学习，必须同时满足以下五项条件：

| 条件 | 代码字段 | 验收证据 |
| --- | --- | --- |
| 资产分级强约束 | `assetClass / canLearn / factLocked / selfLearningPublishAllowed / runtimeWriterRequired / publishGateRequired` | `durationLiveLearningClosureService.ts` 登记，测试覆盖标准工序 seed、专项工序 seed、依赖、关键路径、事实层 |
| prediction / outcome 事件 | `predictionEventRecorded / actualOutcomeEventRecorded` | 每次预测有输入、资产版本、输出影响；完工后有 actual outcome 与样本健康 |
| global / industry / company / project 分层学习 | `tieredLearningPolicyRegistered / enabledLearningScopes` | 冷启动可回退到全局/行业，公司和项目样本成熟后逐层覆盖 |
| runtime consumer 消费 published / canary artifact | `runtimeConsumerUsesPublishedArtifact` | 业务预测、剩余工期、赶工、依赖/WBS/关键路径消费者不再只看 report-only |
| 发布门禁与回滚闭环 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` | v1.4.22.3 release-exit 通过，v1.4.22.4 精度指标证明 MAE/Bias/过度补偿受控 |

任一条件缺失，只能说 `not_ready`、`candidate`、`evidence-only` 或 `shadow/report-only`，不得说 live 自学习。

## 3. 资产分级

| 资产族 | 资产键 | 分类 | 是否可学习 | runtime 声明边界 |
| --- | --- | --- | --- | --- |
| base 工期与冷启动 | `base_duration_benchmark`、`duration_cold_start_baseline` | `learnable_duration_prediction` | 是 | 可以在五项齐备后参与 runtime 预测，不写回事实 |
| 残差与置信度 | `forecast_residual_overlay`、`forecast_confidence_weight` | `learnable_duration_prediction` | 是 | published / canary 才能影响 runtime，shadow 只能留证据 |
| 标准工序 seed | `standard_work_duration_seed` | `learnable_duration_prediction` | 是 | replay 只能产候选，seed rewrite 需要专门 writer |
| 专项工序 seed / WBS / 依赖 / 关键路径 | `special_work_duration_seed`、`wbs_reference_days`、`dependency_rule_candidate`、`critical_path_rule_candidate` | `learnable_plan_network` | 是 | 先产网络 prediction/outcome，再通过 release-exit 与专项 writer |
| baseline / monthly / actual / progress | `baseline_commitment`、`monthly_plan_commitment`、`actual_duration_outcome`、`progress_snapshot` | `business_fact_lock` | 否 | 只做样本、验收、回放依据，不允许自学习改写 |

## 4. 分层学习规则

live 自升级不是单一公司桶学习，必须按以下顺序收敛：

1. `global`：全局默认基线，托底冷启动。
2. `industry`：行业/业态/项目类型基线，解决小账号长尾冻结。
3. `company`：公司级经验，样本达到门槛后覆盖上层。
4. `project`：项目内高频工序或局部节奏 overlay，样本足够时短周期校准。

下层样本不足时必须 shrink 到上层，不得用少量项目样本直接覆盖公司或全局经验。

## 5. 验收矩阵

| 验收项 | 通过条件 | 失败时口径 |
| --- | --- | --- |
| 单资产 live 自学习 | `evaluateDurationLiveLearningAsset(...).status = live_self_learning_ready` | `not_ready` |
| 全组合口径 | 所有可学习资产 ready，所有事实资产 `fact_locked_closed` | `not_ready_for_live_self_learning_claim` |
| 事实层保护 | 所有事实资产 `selfLearningPublishAllowed=false` | 阻断并返回 `duration_fact_auto_rewrite_blocked` |
| 禁止宣传语 | `prohibitedClaim = all_duration_assets_are_live_self_learning` 永远存在 | 不允许删除 |
| 正确宣传语 | `allowedClaim = all_learnable_duration_prediction_and_network_assets_are_live_self_learning;facts_and_commitments_remain_locked` | 未满足前不得使用 |

## 6. 实施顺序

1. 用 `durationLiveLearningClosureService.ts` 作为统一判定口，先拒绝错误宣传和事实层改写。
2. 给各可学习资产补 prediction event / actual outcome event。
3. 给冷启动、标准工序、专项工序、依赖/WBS/关键路径补 global / industry / company / project 分层策略。
4. 让 runtime consumer 逐个消费 published / canary artifact，并记录 consumer evidence。
5. 将 v1.4.22.3 release-exit、impact monitoring、rollback target 和 v1.4.22.4 accuracy metrics 绑定为统一发布门禁。
6. 每个资产族通过后，逐项从 `candidate/evidence-only` 升级为 `live_self_learning_ready`。

## 7. 当前状态

本文件创建时，代码已具备口径判定与阻断能力：

- `durationLiveLearningClosureService.ts` 已登记可学习资产和事实锁定资产；
- `durationLiveLearningClosureService.test.ts` 已覆盖资产分级、五项闭环、事实锁定、组合口径和缺口阻断；
- 这不等于所有业务 consumer 已经 live 消费所有资产，只说明系统已有统一判定口，后续实现必须以该判定口为准。

因此，当前可声明：

> live 自升级口径的代码级判定口已建立。

当前不可声明：

> 所有可学习工期资产已经全部 live 自学习。

### 7.1 第一批 A 类资产 manifest

已落地补充：`durationLiveLearningClosureService.ts` 新增 `duration_prediction_core_a` manifest，覆盖以下四个核心 A 类资产：

| 资产 | 当前 manifest 结论 | 已有锚点 | 主要缺口 |
| --- | --- | --- | --- |
| `base_duration_benchmark` | `manifest_not_ready` | `durationSuggestionService.ts`、`duration_prediction_events`、`durationExperienceService.ts`、`projectProductivityCalibrationService.ts`、参数 runtime consumption / release execution | 需要证明 global / industry / company / project 完整分层链路，并把 impact monitoring 绑定到 base duration runtime publication |
| `duration_cold_start_baseline` | `manifest_not_ready` | `algorithmAssetColdStartBaselineService.ts`、`durationSuggestionService.ts`、`duration_prediction_events` | 需要补 cold-start actual outcome，对接 company/project shrinkage，并通过 release-exit / rollback |
| `forecast_residual_overlay` | `manifest_not_ready` | `algorithmAssetForecastResidualOverlayService.ts`、`taskDurationForecastService.ts`、`projectRemainingDurationForecastService.ts`、`durationAlgorithmAccuracyService.ts`、release execution | 需要补 global / industry residual overlay fallback，或明确 residual overlay 只允许 company/project 范围的例外规则 |
| `forecast_confidence_weight` | `manifest_not_ready` | `algorithmAssetLearnableParameterRuntimeConsumptionService.ts`、`taskDurationForecastService.ts`、`durationAlgorithmAccuracyService.ts`、release execution | 需要补 global / industry confidence weight fallback，或明确该参数只允许 company/project 范围的例外规则 |

代码验收：

- `listDurationLiveLearningManifests('duration_prediction_core_a')` 必须返回上述四个资产；
- `evaluateDurationLiveLearningManifest('duration_prediction_core_a')` 当前必须返回 `manifest_not_ready`，且 `prohibitedClaim=all_duration_assets_are_live_self_learning`；
- 第一批未 ready 前，允许声明只能是 `first_batch_manifest_established_not_ready_for_live_self_learning_claim`。

范围归一化补充：`resolveDurationLearningScopeCoverage(...)` 已把 runtime 层常见命名归一到 v1.4.22.5 的四层学习口径：

| runtime / baseline 命名 | v1.4.22.5 归一层级 |
| --- | --- |
| `system` | `global` |
| `industry_baseline` | `industry` |
| `segment_baseline` | `industry` |
| `company` | `company` |
| `project` | `project` |

未知 scope 必须进入 `unknownScopes`，不得被默认当作已覆盖。

### 7.2 第二批 B 类计划网络 manifest

已落地补充：`durationLiveLearningClosureService.ts` 新增 `plan_network_core_b` manifest，覆盖以下五个计划网络资产：

| 资产 | 当前 manifest 结论 | 已有锚点 | 主要缺口 |
| --- | --- | --- | --- |
| `standard_work_duration_seed` | `manifest_not_ready` | `standardWorkDurationSeedReplayService.ts`、`evaluateStandardWorkDurationSeedLiveLearningEvidence(...)`、`standardWorkDurationSeedReplayCandidateBridgeService.ts`、`buildStandardWorkDurationSeedPublicationReadiness(...)`、`durationSuggestionService.ts`、`duration_prediction_events`、`durationExperienceService.ts` | live evidence 门禁与 approved candidate publication readiness 包已补；仍需要真实 seed version 发布执行与 runtime consumer 证据，并把 replay accuracy 绑定到 release-exit / rollback |
| `special_work_duration_seed` | `manifest_not_ready` | `wbsTemplateGenerationService.ts`、`wbsTemplateCandidateEventService.ts`、`evaluateSpecialWorkDurationSeedLiveLearningEvidence(...)` | live evidence 门禁已补；仍需要专项 seed publication writer、seed version lineage、runtime consumer 证据，并把用户 outcome / template feedback 绑定到 release-exit / rollback |
| `wbs_reference_days` | `manifest_not_ready` | `wbsTemplateGoldenBenchmarkReplayService.ts`、`wbsTemplateGoldenBenchmarkGateService.ts`、`evaluateWbsReferenceDaysLiveLearningEvidence(...)`、`durationAccuracyReplayAcceptanceService.ts` | live evidence 门禁已补；仍需要 WBS reference days publication writer、lineage、runtime consumer 证据，并避免 feedback 静默改 seed |
| `dependency_rule_candidate` | `manifest_not_ready` | `constructionDependencyReplayCalibrationService.ts`、`evaluateConstructionDependencyRuleCandidateLiveLearningEvidence(...)`、`wbsTemplateGenerationService.ts`、`scheduleAccelerationService.ts` | live evidence 门禁已补；仍需要 dependency rule publication writer、lineage、runtime consumer 证据，并把实际前后置兑现 outcome 绑定到 release-exit / rollback |
| `critical_path_rule_candidate` | `manifest_not_ready` | `projectCriticalPathService.ts`、`evaluateCriticalPathRuleCandidateLiveLearningEvidence(...)`、`projectRemainingDurationForecastService.ts`、`scheduleAccelerationRuntimeService.ts` | live evidence 门禁已补；仍需要 critical path rule publication writer、lineage、runtime consumer 证据，并把 critical path outcome / float outcome 绑定到 release-exit / rollback |

代码验收：

- `listDurationLiveLearningManifests('plan_network_core_b')` 必须返回上述五个资产；
- `evaluateDurationLiveLearningManifest('plan_network_core_b')` 当前必须返回 `manifest_not_ready`；
- 第二批未 ready 前，允许声明只能是 `batch_manifest_established_not_ready_for_live_self_learning_claim`。

### 7.3 执行 gate 与下一步推荐资产

已落地补充：`durationLiveLearningClosureService.ts` 新增 `evaluateDurationLiveLearningExecutionPlan(...)`，把 A/B 两批 manifest 的缺口统一归入五个可执行 gate：

| gate | 作用 | 失败时必须做的事 |
| --- | --- | --- |
| `prediction_and_outcome_events` | 检查每次 runtime prediction 是否有 prediction event，是否有 actual outcome / network outcome | 补预测事件和实际结果事件 |
| `tiered_learning_scope` | 检查 global / industry / company / project 四层学习或明确例外 | 补分层策略，低样本层 shrink 到上层 |
| `runtime_consumer_publication` | 检查 consumer 是否真的消费 published / canary artifact | 补 runtime consumer，candidate / shadow 仍只作证据 |
| `release_monitoring_rollback` | 检查 release-exit、impact monitoring、rollback target | 补 v1.4.22.3 发布门禁和回滚绑定 |
| `accuracy_metrics` | 检查 MAE / Bias / overcompensation 是否绑定到 live artifact | 补精度指标出口 |

当前 `evaluateDurationLiveLearningExecutionPlan(['duration_prediction_core_a', 'plan_network_core_b'])` 必须返回：

- `status=execution_plan_not_ready`；
- `prohibitedClaim=all_duration_assets_are_live_self_learning`；
- `nextRecommendedAssetKeys=['duration_cold_start_baseline','standard_work_duration_seed','special_work_duration_seed']`。

补充落地：`evaluateDurationLiveLearningManifest(...)` 与 `evaluateDurationLiveLearningExecutionPlan(...)` 已支持按资产传入 runtime evidence override。默认 manifest 仍保持静态保守口径；只有当 `duration_cold_start_baseline` 传入由 `evaluateAlgorithmAssetColdStartLiveLearningEvidence(...)` 生成的完整 evidence 时，统一执行计划才允许把它从阻塞资产中移除，并把下一批推荐推进为 `standard_work_duration_seed / special_work_duration_seed / wbs_reference_days`。

因此，后续执行顺序固定为：

1. 先补 `duration_cold_start_baseline` 的 actual outcome、company/project shrinkage、release / rollback；
2. 再补 `standard_work_duration_seed` 的 seed publication writer、seed version lineage、runtime consumer 与 replay accuracy release-exit；当前先落 live evidence 门禁，不把 replay 报告误判为 live 发布；
3. 再补 `special_work_duration_seed` 的专项 seed publication writer、seed version lineage、runtime consumer 与 template feedback accuracy release-exit；当前先落 live evidence 门禁，不把 template candidate event 误判为 live 发布。

### 7.4 `duration_cold_start_baseline` live evidence 入口

已落地补充：`algorithmAssetColdStartBaselineService.ts` 新增 `evaluateAlgorithmAssetColdStartLiveLearningEvidence(...)`，把冷启动 runtime decision 转换为 v1.4.22.5 可验收证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| actual outcome | `actualOutcomeRecorded=true` 且 `actualSampleHealth=accepted` |
| 分层学习 | runtime source 至少包含 `system_seed` 与 `industry_baseline / segment_baseline`，company accepted sample 达到 override 门槛，project accepted sample 达到 overlay 门槛 |
| runtime 消费 | runtime decision 为 `shared_baseline_reference` 或 `company_override`，且 `runtimeConsumable=true` |
| 发布安全 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady` 均为 true |
| 精度指标 | `accuracyMetricsAvailable=true` |

返回结果：

- 满足全部条件时：`status=cold_start_live_learning_ready`；
- 任一条件缺失时：`status=cold_start_live_learning_not_ready`，并返回缺口原因，例如 `accepted_actual_outcome_required`、`company_scope_samples_required_for_shrinkage`、`release_exit_required`。

统一闭环补充：该入口的 `liveLearningEvidence` 已可作为 `duration_cold_start_baseline` 的 runtime evidence override 传入 `durationLiveLearningClosureService.ts`。传入后只影响 manifest / execution plan 的 ready/not-ready 判定和 next recommended asset 排序，不改静态 manifest 默认值。

边界：该入口只生成 live-learning evidence，不写 actual、不写 company override、不写 shared baseline。事实与发布仍由各自业务链路和 v1.4.22.3 release-exit 管。

### 7.5 `standard_work_duration_seed` live evidence / publication writer gate

已落地补充：`standardWorkDurationSeedReplayService.ts` 新增 `evaluateStandardWorkDurationSeedLiveLearningEvidence(...)`，把标准工序 seed replay report 转换为 v1.4.22.5 可验收证据。

二次补充：`standardWorkDurationSeedReplayCandidateBridgeService.ts` 新增 `buildStandardWorkDurationSeedPublicationReadiness(...)`，把 report-only replay、candidate-only bridge、approved candidate、seed version id、runtime publication key、rollback target、release-exit 与 accuracy evidence 合成为一个 approved publication readiness package。该包可把 `liveLearningEvidence` 交给 `durationLiveLearningClosureService.ts` 的 runtime evidence override，但自身不写 seed、不发布 version。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| replay prediction / outcome | replay report 存在、`evaluatedCodeCount>0`，且外部确认 `actualOutcomeEventRecorded=true` |
| 候选可发布性 | 存在 trusted replay 或 review / missing-seed candidate，且 `approvedReplayCandidateRecorded=true` |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| replay 安全边界 | replay 保持 `report_only`，所有 queue item 保持 `seedWritePolicy=never_write_seed_from_replay` |
| 专项 writer 与 lineage | `seedPublicationWriterReady=true`，`seedVersionLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，不能只停留在 candidate / report-only |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true |

返回结果：

- 满足全部条件时：`status=standard_work_seed_live_learning_ready`；
- 任一条件缺失时：`status=standard_work_seed_live_learning_not_ready`，并返回缺口原因，例如 `approved_replay_candidate_required`、`seed_publication_writer_required`、`seed_version_lineage_required`、`runtime_consumer_publication_required`、`global_industry_company_project_learning_scopes_required`。

边界：这些入口只证明“标准工序 seed 已具备可发布 live-learning evidence / readiness package”。它们不直接写 `algorithm_seed_versions`，不改系统 seed，不绕过 replay candidate review，不把 `standardWorkDurationSeedReplayService` 的 report-only 阶段升级成 runtime writer。真正 runtime 生效仍必须通过专门 seed publication writer、seed version lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

### 7.6 `special_work_duration_seed` live evidence / publication writer gate

已落地补充：`wbsTemplateCandidateEventService.ts` 新增 `evaluateSpecialWorkDurationSeedLiveLearningEvidence(...)`，把专项工序 seed / 模板候选的 generated / retained / rejected / pending outcome 转换为 v1.4.22.5 可验收证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| network prediction | `networkPredictionEventRecorded=true`，且 `generatedRowCount>0` |
| 用户 outcome | `templateFeedbackOutcomeRecorded=true`，retained + rejected 覆盖全部 generated rows，`pendingRowCount=0` |
| 候选可发布性 | `approvedSpecialSeedCandidateRecorded=true`，不能只停留在 template candidate event |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| candidate 安全边界 | `governedCandidateOnlyBoundaryPreserved=true`；候选事件仍保持 candidate-only，不直接写 runtime |
| 专项 writer 与 lineage | `specialSeedPublicationWriterReady=true`，`seedVersionLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，模板生成 / 工期消费者消费 published / canary artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true |

返回结果：

- 满足全部条件时：`status=special_work_seed_live_learning_ready`；
- 任一条件缺失时：`status=special_work_seed_live_learning_not_ready`，并返回缺口原因，例如 `network_prediction_event_required`、`network_outcome_event_required`、`all_candidate_rows_must_have_user_outcome`、`special_seed_publication_writer_required`、`runtime_consumer_publication_required`。

边界：该入口只证明“专项工序 seed 已具备可发布 live-learning evidence”。它不直接修改 `wbs_templates`、任务、baseline、monthly plan 或 `algorithm_seed_versions`，不把用户确认的计划网络事实改成学习 writer。真正 runtime 生效仍必须通过专项 seed publication writer、seed version lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

### 7.7 `wbs_reference_days` live evidence / benchmark gate

已落地补充：`wbsTemplateGoldenBenchmarkGateService.ts` 新增 `evaluateWbsReferenceDaysLiveLearningEvidence(...)`，把 WBS golden benchmark runtime gate 与 reference-days 发布证据合成为 v1.4.22.5 可验收证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| benchmark replay | `evaluateWbsTemplateGoldenBenchmarkRunGate(...).status=pass`，13 个真实项目场景通过 coverage、deep coverage、duration deviation、dependency pass、row-count、duration output evidence |
| template reference-days outcome | `templateReferenceDaysOutcomeRecorded=true`，必须有 template feedback / actual schedule outcome，不只看 synthetic replay |
| 候选可发布性 | `approvedReferenceDaysCandidateRecorded=true`，不能只停留在 benchmark finding |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| 专项 writer 与 lineage | `referenceDaysPublicationWriterReady=true`，`referenceDaysLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，WBS 生成 / 剩余工期消费者消费 published / canary reference-days artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true，且 accuracy 必须建立在通过的 benchmark gate 上 |

返回结果：

- 满足全部条件时：`status=wbs_reference_days_live_learning_ready`；
- 任一条件缺失时：`status=wbs_reference_days_live_learning_not_ready`，并返回缺口原因，例如 `wbs_reference_days_benchmark_gate_required`、`template_reference_days_outcome_required`、`wbs_reference_days_publication_writer_required`、`wbs_reference_days_lineage_required`。

边界：该入口只证明“WBS reference days 已具备可发布 live-learning evidence”。它不直接修改模板、任务、baseline、monthly plan、依赖边或关键路径事实。golden benchmark replay 只是 release / accuracy 证据，不是 runtime writer；真正 runtime 生效仍必须通过 reference-days publication writer、lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

### 7.8 `dependency_rule_candidate` live evidence / replay calibration gate

已落地补充：`constructionDependencyReplayCalibrationService.ts` 新增 `evaluateConstructionDependencyRuleCandidateLiveLearningEvidence(...)`，把 L3 / L4 dependency replay calibration report 转换为 v1.4.22.5 可验收证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| dependency replay prediction | `reportCode=construction_dependency_replay_calibration`，且 `inputDependencyCount>0` |
| actual dependency outcome | `dependencyOutcomeEventRecorded=true`，且 `comparableActualDateCount>0` |
| 候选可发布性 | 存在 L3 lag calibration 或 L4 conflict quarantine candidate，且 `approvedDependencyRuleCandidateRecorded=true` |
| replay 安全边界 | replay 保持 `report_only`，`seedWritePolicy=never_write_seed_from_replay`，`taskDependencyWritePolicy=never_write_task_dependencies_from_replay` |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| 专项 writer 与 lineage | `dependencyRulePublicationWriterReady=true`，`dependencyRuleLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，WBS 生成 / 赶工 / 依赖建议消费者消费 published / canary dependency artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true |

返回结果：

- 满足全部条件时：`status=dependency_rule_candidate_live_learning_ready`；
- 任一条件缺失时：`status=dependency_rule_candidate_live_learning_not_ready`，并返回缺口原因，例如 `dependency_replay_report_required`、`dependency_actual_outcome_required`、`dependency_rule_publication_writer_required`、`dependency_rule_lineage_required`。

边界：该入口只证明“依赖规则候选已具备可发布 live-learning evidence”。它不直接改 L3 / L4 seed、不写 `task_dependencies`、不改用户确认依赖、baseline、monthly plan 或关键路径事实。真正 runtime 生效仍必须通过 dependency rule publication writer、lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。

### 7.9 `critical_path_rule_candidate` live evidence / CPM projection gate

已落地补充：`projectCriticalPathService.ts` 新增 `evaluateCriticalPathRuleCandidateLiveLearningEvidence(...)`，把 CPM critical path snapshot、network lineage、float projection 与关键路径规则候选发布证据合成为 v1.4.22.5 可验收证据。

该入口检查：

| 检查项 | 通过条件 |
| --- | --- |
| critical path prediction | `criticalPathSnapshot.networkLineage.criticalPathInputHash / criticalSetHash` 存在，且自动关键路径 / 展示关键路径 / task snapshot 至少有一类证据 |
| float / projection evidence | snapshot 中存在关键任务集合，且 task snapshot 有可计算 `floatDays` |
| actual critical path outcome | `criticalPathOutcomeEventRecorded=true`，且 `projectDurationDays>0`；后续可接项目实际工期、关键任务 float 偏差或关键链兑现 outcome |
| 候选可发布性 | `approvedCriticalPathRuleCandidateRecorded=true`，不能只停留在 CPM snapshot 或人工关注标记 |
| 分层学习 | `system/global`、`industry/industry_baseline/segment_baseline`、`company`、`project` 四层归一后齐备 |
| 专项 writer 与 lineage | `criticalPathRulePublicationWriterReady=true`，`criticalPathRuleLineageRecorded=true` |
| runtime 消费 | `runtimeConsumerUsesPublishedArtifact=true`，剩余工期 / 赶工 / critical path 消费者消费 published / canary critical-path-rule artifact |
| 发布安全与精度 | `releaseExitApproved / impactMonitoringReady / rollbackTargetReady / accuracyMetricsAvailable` 均为 true |

返回结果：

- 满足全部条件时：`status=critical_path_rule_candidate_live_learning_ready`；
- 任一条件缺失时：`status=critical_path_rule_candidate_live_learning_not_ready`，并返回缺口原因，例如 `critical_path_prediction_snapshot_required`、`critical_path_actual_outcome_required`、`critical_path_rule_publication_writer_required`、`critical_path_rule_lineage_required`。

边界：该入口只证明“关键路径规则候选已具备可发布 live-learning evidence”。它不直接改任务日期、不改 `task_critical_overrides`、不改用户确认关键路径、baseline、monthly plan 或 progress snapshot；`is_critical / total_float_days / free_float_days / criticality_weight` 仍是 CPM runtime projection，不是学习 writer。真正 runtime 自升级仍必须通过 critical path rule publication writer、lineage、v1.4.22.3 release-exit / rollback 和 v1.4.22.4 accuracy metrics。
