# v1 结束阶段最终执行方案

> 更新时间：2026-04-06  
> 合并来源：`C:\Users\jjj64\WorkBuddy\20260406140227\前端UI改造项确认清单_20260406.md` + `C:\Users\jjj64\.claude\plans\v1收尾完成后蓝图剩余缺口分析_20260406.md`  
> 前置条件：`v1收尾阶段后续专项方案_20260406.md` 的 `8.x` 已稳定，或至少当前会话不再与 `8.x` 混做  
> 用途：作为 v1 收尾阶段的总 gate / 总索引文档；计划编制与页面职责等新增口径，后续将先并入《业务流程优化清单》，再形成最终执行方案，最后回补本文件
> **执行顺序**：`9.x` 完成后，先执行 `业务流程优化清单.md` §十 全部步骤（`10.1 / 10.2a-e / 10.8a-c / 10.9 / 10.10`），再执行本文第 10 章专项施工步骤（`10.0 / 10.3-10.7`），最后按 `11.x -> 12.x -> 13.x` 顺序推进

---

## 0. 使用规则

### 0.1 适用前提

- 本文件只处理 `v1 收尾阶段后续专项方案_20260406.md` 之后的剩余事项
- 若 `EXECUTION_PROGRESS.json` 中仍存在 `8.x` 的 `pending / in_progress` 步骤，则先回到 `v1收尾阶段后续专项方案_20260406.md`，不要提前启动 `9.x`
- 凡与 `v1 收尾阶段执行清单` 重叠且已完成的项，一律按“已完成”处理，不回填
- 若当前代码现状与本文判断不一致，先更新本文，再继续执行，不要边做边改口径
- 当前仍处于过渡讨论阶段：计划编制、页面职责与侧边栏收口等新增口径，先在《计划编制专项讨论稿》中收敛；后续将并入《业务流程优化清单》，再形成最终执行方案，并作为补充回写本文件

### 0.2 进度文件口径

- 本文件与 `施工图纸实施专项方案_20260408.md`、`关键路径改造专项方案_20260407.md`、`前期证照实施专项方案_20260408.md`、`验收流程轴实施专项方案_20260408.md`、`业务流程优化清单.md` 统一对应以下步骤组：
  - `9.0-9.9`：UI 收口专项（已完成）
  - `10.1 / 10.2a-e / 10.8a-c / 10.9 / 10.10`：业务流程优化，详细执行以 `业务流程优化清单.md` §十 为准，**先于本文第 10 章执行**
  - `10.0 / 10.3-10.7`：专项施工步骤（图纸真实环境验证、独立图纸页、证照管理信息架构收口），详细执行以本文第 10 章 + 施工图纸专项为准
  - `11.0-11.6`：关键路径专项
  - `12.0-12.8`：前期证照专项
  - `13.0-13.8`：验收流程轴专项
- 只有当 `8.x` 已无需要继续执行的 `pending / in_progress` 步骤时，才允许把 `9.0` 作为当前执行入口
- 开始 `9.x` 时，将 `current_phase` 改为 `9`
- 开始 `10.x` 时，将 `current_phase` 改为 `10`
- 开始 `11.x` 时，将 `current_phase` 改为 `11`
- 开始 `12.x` 时，将 `current_phase` 改为 `12`
- 开始 `13.x` 时，将 `current_phase` 改为 `13`
- 每完成一个子步骤，必须立即回写进度；未通过验证不得写 `completed`

### 0.3 范围边界

- `9.x` 默认只改前端页面、组件、样式、轻量前端状态，不改后端口径
- `Milestones` 的“点击后详情”允许新增前端状态与详情面板逻辑，但不默认引入新接口
- `10.x` 允许改后端路由、迁移、前后端数据模型与 store
- `G1` 默认是环境验证项，不先按代码缺口处理

### 0.4 上下文管理规则

- 单个源文件超过 `2000` 行时，首轮只允许锚点搜索 + 分段读取，不全文通读
- 单个子步骤只做一个页面或一个业务域，不跨步推进
- 同一步完成“读锚点 + 编辑 + 测试 + 回写进度”后主动停止，不顺手进入下一步
- 若某步需要新增表或新增主路由，而现场约束与本文假设冲突，必须先停下更新本文

### 0.4A 执行纪律（跨步骤通用）

> 步骤级纪律（`10.2a / 10.2b / 10.2c / 10.2d`）已内联到 `业务流程优化清单.md` 对应步骤；`PM.4` 内联到前期证照专项；`AT.5` 内联到验收流程轴专项。本节只保留跨步骤通用规则。

- 高风险步骤（涉及 3 个以上文件或 4 个服务联动）一律先跑基线测试记录当前行为，再改代码
- 单文件超过 150-200 行改动时，默认拆成”后端轮 / 前端轮”两次提交思路推进
- 新 UI 一律先对齐现有系统视觉语言，不先追求复杂动画或重布局创新
- `10.7` 对 `AcceptanceTimeline` 的最小 UI 收口只允许做页壳统一：
  - 收口 `PageHeader / Breadcrumb / 页面级 loading`
  - 保留按钮级 `Loader2` 作为操作反馈
  - 不提前改 `ForceDirectedGraph` 主体业务逻辑
- 若 `Reports`、`TaskSummary`、`Drawings` 在后续步骤中新增大区块，必须继续遵守 `9.x` 已收口的页面语言：
  - `PageHeader` 只保留标题
  - 不恢复说明卡 / 副标题 / 大段引导文案
  - 卡片统一沿用 `card-base / shell-surface / border-slate-200 / bg-white` 风格
- `Reports` 布局口径以后续《计划编制专项讨论稿》为准：统一分析壳、顶部模块切换、无独立承接面、不保留页面底部“分析切换”卡片；不新增说明卡、不恢复副标题、不做三栏瀑布流
- 侧边栏最终结构、一级入口边界、二级入口归属统一以后续《计划编制专项讨论稿》“侧边栏收口口径”章节为准；本文件仅记录实施顺序与验证点，不再重复定义导航架构
- 导航显示名收口采用“统一导航元数据”方式处理，核心受影响文件固定包括：
  - 代码：`client/src/App.tsx`、`client/src/components/layout/Sidebar.tsx`、`client/src/components/layout/Header.tsx`、`client/src/hooks/useKeyboardShortcuts.tsx`
  - 页面：`client/src/pages/GanttView.tsx`、`client/src/pages/Milestones.tsx`、`client/src/pages/PreMilestones.tsx`、`client/src/pages/AcceptanceTimeline.tsx`、`client/src/pages/Notifications.tsx`、`client/src/pages/Reports.tsx`、`client/src/pages/RiskManagement.tsx`、`client/src/pages/TaskSummary.tsx`、`client/src/pages/WBSTemplates.tsx`
  - 测试：`client/src/pages/__tests__/LicenseManagement.test.tsx`、`client/src/pages/__tests__/Milestones.test.tsx`、`client/src/pages/__tests__/Notifications.test.tsx`、`client/src/pages/__tests__/Reports.test.tsx`、`client/src/pages/__tests__/systemMainChains.test.tsx`、`client/src/pages/__tests__/TaskSummary.test.tsx`、`client/src/pages/__tests__/WBSTemplates.test.tsx`
  - 专项文档：`证照管理三子页统一执行入口_20260409.md`、`前期证照实施专项方案_20260408.md`、`施工图纸实施专项方案_20260408.md`、`验收流程轴实施专项方案_20260408.md`
- 页面内失效分析跳转的处理原则也已收口：
  - `client/src/pages/GanttView.tsx` 中的 `WBS完成度分析` 按钮后续直接删除
  - `client/src/pages/PreMilestones.tsx` 中的 `证照状态分析` 按钮后续统一取消
  - `client/src/pages/AcceptanceTimeline.tsx` 中的 `验收进度分析` 按钮后续统一取消
  - `client/src/pages/WBSTemplates.tsx` 页头与说明文案后续统一改成 `计划编制 / WBS 模板`
- 迁移编号继续按”确认当前最大纯数字编号后顺延”的规则执行；`037b` 这类历史变体不复用、不覆盖，只作为历史存在
- `task_progress_snapshots` 的真实写入链必须挂在任务进度/状态更新路由中，按”任务 + 日期”写快照保证幂等，不允许继续只读快照表再回退到 `tasks` 作为长期实现

### 0.5 全局 UI 规则

- 当前应用页面中的说明性文案、说明卡、承接规则、引导文字默认一并删除，只保留标题
- 不为了保留 `overdue / soon` 逻辑新增弹窗或独立窗口
- 风险等级、预警等级、关键路径等语义色保留，不做“全部灰底标签化”

### 0.6 已知后置项

- `getProgressColor` / `getProgressColorClass` 仍散落在 `AssigneeProgressCard.tsx`、`DashboardHealthCard.tsx`、`UnitProgressCard.tsx`、`RecentTasksCard.tsx`
- 该项属于原 Phase 6 的低优先级共享工具抽取，不纳入本轮 `9.x / 10.x` 主链
- 后续如需处理，应单独作为“已知后置技术债”开步，不要在当前 UI 收口过程中顺手扩写

---

## 1. 总体执行顺序

> **重要**：`9.x` 已全部完成。后续执行顺序为：先完成 `业务流程优化清单.md` §十 全部步骤，再执行本文第 10 章专项施工步骤，最后按 `11.x -> 12.x -> 13.x` 顺序推进。

### 业务流程优化步骤（先执行，详见 `业务流程优化清单.md` §十）

1. `10.1` G3.1 问题域模型与 API 基线（含 risks 表前置迁移）
2. `10.2a` G3.2a issues 来源链路与 RiskManagement 前端切换
3. `10.2b` G3.2b delay_requests / change_logs / task_progress_snapshots 共享底座
4. `10.2c` G3.2c 预警/通知统一持久化与 warningService 职责收口
5. `10.2d` G3.2d GanttView 延期审批 UI 与关键路径影响展示
6. `10.2e` G3.2e Reports 变更记录视图
7. `10.8a` G9a 参建单位主数据台账与 CRUD
8. `10.8b` G9b 业务表单接入参建单位 + Dashboard 快照
9. `10.8c` G9c TaskSummary 责任主体分析面板
10. `10.9` G10 项目范围维度底座与挂接统一
11. `10.10` G11 跨页面主数据承载与状态一致性治理

### 本文专项施工步骤（后执行）

12. `10.0` 建立专项施工基线
13. `10.3` G1 图纸真实环境验证
14. `10.4` G2.1 Drawings 独立页面与主路由
15. `10.5` G2.2 Sidebar 入口与 PreMilestones 图纸收口
16. `10.6` G7 图纸与任务条件联动
17. `10.7` G8 证照管理三子页联动与信息架构收口

### 后续专项

18. `11.0-11.6` 关键路径专项（详见 `关键路径改造专项方案_20260407.md`）
19. `12.0-12.8` 前期证照专项（详见 `前期证照实施专项方案_20260408.md`）
20. `13.0-13.8` 验收流程轴专项（详见 `验收流程轴实施专项方案_20260408.md`）

补充说明：

- `业务流程优化清单.md` §十 的步骤顺序、前置依赖和通过标准以该文档为准，本文不重复展开
- 业务流程优化步骤全部完成后，才进入本文第 10 章专项施工步骤
- `10.3 blocked` 时只阻断 `10.6-10.7`（联动与收口），`10.4-10.5`（页面壳与路由）可先行

---

## 2. UI 收口专项（9.x）

### 2.1 `9.0` 建立 UI 收口基线

- **目标文件**
  - `EXECUTION_PROGRESS.JSON`
  - `client/src/pages/Dashboard.tsx`
  - `client/src/pages/CompanyCockpit.tsx`
  - `client/src/pages/Reports.tsx`
  - `client/src/pages/Milestones.tsx`
  - `client/src/pages/RiskManagement.tsx`
  - `client/src/pages/TaskSummary.tsx`
  - `client/src/pages/PreMilestones.tsx`
  - `client/src/pages/GanttView.tsx`
- **开始前定位命令**
  ```powershell
  (Get-Content client/src/pages/Dashboard.tsx | Measure-Object -Line).Lines
  (Get-Content client/src/pages/Reports.tsx | Measure-Object -Line).Lines
  (Get-Content client/src/pages/Milestones.tsx | Measure-Object -Line).Lines
  (Get-Content client/src/pages/RiskManagement.tsx | Measure-Object -Line).Lines
  (Get-Content client/src/pages/TaskSummary.tsx | Measure-Object -Line).Lines
  (Get-Content client/src/pages/PreMilestones.tsx | Measure-Object -Line).Lines
  (Get-Content client/src/pages/GanttView.tsx | Measure-Object -Line).Lines
  rg --line-number "DashboardSupport|DashboardCompareCard|ProjectInfoCard" client/src/pages/Dashboard.tsx
  rg --line-number "CompanyHero|CompanyInsightSection|MilestoneSummarySection|ProjectRanking|DeliveryCountdown" client/src/pages/CompanyCockpit.tsx client/src/pages/CompanyCockpit/components/*.tsx
  rg --line-number "AnalysisEntryCard|renderProgressDetail|renderRiskDetail|切换其他分析" client/src/pages/Reports.tsx
  rg --line-number "PageHeader|重点关注|里程碑主列表|TabsTrigger" client/src/pages/Milestones.tsx
  Test-Path client/src/pages/__tests__/Reports.test.tsx
  Test-Path client/src/pages/__tests__/Milestones.test.tsx
  Test-Path client/src/pages/__tests__/RiskManagement.test.tsx
  Test-Path client/src/pages/__tests__/TaskSummary.test.tsx
  Test-Path client/src/pages/__tests__/GanttView.test.tsx
  Test-Path client/src/pages/__tests__/sharedSummary.test.tsx
  Test-Path client/src/pages/__tests__/systemMainChains.test.tsx
  Test-Path client/src/pages/__tests__/preMilestonesLifecycle.test.ts
  ```
- **具体动作**
  1. 确认 8 个页面当前行数、现有测试文件、主要锚点块
  2. 确认 `EXECUTION_PROGRESS.JSON` 中已预建 `9.0-9.9 / 10.0-10.10 / 11.0-11.6`
  3. 不改业务代码，只建立本阶段基线
- **不要做的事**
  - 不在本步顺手改页面
  - 不提前开启 `10.x`
- **验证方式**
  - 确认进度文件中已出现 `9.x / 10.x / 11.x` 步骤键
- **通过标准**
  - 后续会话可以直接从 `9.1` 继续
  - `9.x` 所需现有测试文件已确认存在；若缺失，已在本步 note 里记录
- **进度回写示例**
  - `9.0 completed: 已确认 9.x/10.x/11.x 预建步骤存在，记录 Dashboard/Reports/Milestones/RiskManagement/TaskSummary/PreMilestones/GanttView 行数与锚点，并确认 9.x 现有测试文件存在`

### 2.2 `9.1` Dashboard

- **目标文件**
  - `client/src/pages/Dashboard.tsx`
  - `client/src/components/DashboardCompareCard.tsx`
  - `client/src/components/ProjectInfoCard.tsx`
- **开始前定位命令**
  ```powershell
  rg --line-number "DashboardCompareCard|DashboardSupport|ProjectInfoCard|quickLinks|专项准备度|PageHeader" client/src/pages/Dashboard.tsx
  ```
- **具体动作**
  1. 将当前位于页面底部独立区的 `DashboardCompareCard` 移入首屏 Hero 区，放到左侧三卡正下方
  2. 删除 `DashboardSupport`
  3. 删除 `quickLinks`
  4. 删除“专项准备度”
  5. 保留 `ProjectInfoCard`，并上收至 Dashboard 页头区域，不再独立铺满一行
  6. 删除页面副标题与其他说明性文案中的“专项准备度 / 快速跳转”等表述，只保留标题
- **不要做的事**
  - 不重做项目健康、关键节点、风险口径
  - 不新增弹窗或右侧辅助面板
  - 不改 `DashboardApiService.getProjectSummary` 的数据来源
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/sharedSummary.test.tsx
  npx vitest run --root client src/pages/__tests__/systemMainChains.test.tsx
  ```
- **通过标准**
  - `DashboardSupport` 不再渲染
  - `ProjectInfoCard` 已上收至页头区域
  - `DashboardCompareCard` 已进入 Hero 区
  - 页面只保留标题级信息，不保留说明性大段文案
- **进度回写示例**
  - `9.1 completed: DashboardCompareCard 已移入 Hero；DashboardSupport/quickLinks/专项准备度 已删除；ProjectInfoCard 已上收至页头区域`

### 2.3 `9.2` CompanyCockpit

- **目标文件**
  - `client/src/pages/CompanyCockpit.tsx`
  - `client/src/pages/CompanyCockpit/components/CompanyHero.tsx`
  - `client/src/pages/CompanyCockpit/components/CompanyInsightSection.tsx`
  - `client/src/pages/CompanyCockpit/components/MilestoneSummarySection.tsx`
- **开始前定位命令**
  ```powershell
  rg --line-number "CompanyHero|CompanyInsightSection|MilestoneSummarySection|ProjectRanking|DeliveryCountdown|PageHeader" client/src/pages/CompanyCockpit.tsx
  ```
- **具体动作**
  1. 将趋势总览从 `CompanyInsightSection` 内部移到 `CompanyHero` 左侧三张指标卡下方
  2. 删除“优先跟进清单”
  3. 删除“交付窗口 / 交付倒计时”
  4. 删除 `ProjectRanking`
  5. 删除 `MilestoneSummarySection`
  6. 将 `CompanyInsightSection` 调整为 `2x2` 等宽网格
  7. 删除 Hero 与洞察区中的说明性长文案，只保留标题
  8. 明确把 `ProjectRanking / MilestoneSummarySection / DeliveryCountdown` 视为最新 UI 收口方案中的“有意识撤销项”，不是误删蓝图产物
- **必须保持不动**
  - `refreshData`
  - 共享摘要与风险数据读取链
  - 新建 / 删除项目行为
- **不要做的事**
  - 不改项目排序、搜索和 tab 过滤逻辑
  - 不重做公司级数据口径
  - 不把已收口为轻量页面壳的 `CompanyCockpit` 重新打散
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/sharedSummary.test.tsx
  npx vitest run --root client src/pages/__tests__/systemMainChains.test.tsx
  ```
- **通过标准**
  - 趋势总览已从 `CompanyInsightSection` 迁到 `CompanyHero`
  - `优先跟进清单 / 交付窗口 / ProjectRanking / MilestoneSummarySection` 不再渲染
  - `InsightSection` 只保留 `2x2` 主洞察块
- **进度回写示例**
  - `9.2 completed: 趋势总览已移入 CompanyHero；优先跟进/交付窗口/ProjectRanking/MilestoneSummarySection 已删除；InsightSection 已收口为 2x2`

### 2.4 `9.3` Reports

- **目标文件**
  - `client/src/pages/Reports.tsx`
- **开始前定位命令**
  ```powershell
  (Get-Content client/src/pages/Reports.tsx | Measure-Object -Line).Lines
  rg --line-number "AnalysisEntryCard|renderProgressDetail|renderRiskDetail|切换其他分析|PageHeader|overview" client/src/pages/Reports.tsx
  ```
- **具体动作**
  1. 删除 overview 页的“信息说明 + 承接规则”
  2. 删除非 overview 视图里的说明卡
  3. 将 `AnalysisEntryCard` 改成紧凑卡片
  4. 将详情区统一成 `3+1` 统计 + 左右两列 + 建议区
  5. 底部“切换其他分析”改成紧凑卡片网格
  6. 统一详情外壳与节奏，但不同视图保留不同内容形态
- **不要做的事**
  - 不强制所有明细统一成固定 `5` 列 table
  - 不把页面改回只剩“分析承接说明”的空页
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/Reports.test.tsx
  ```
- **通过标准**
  - `Reports` 页面不再存在大段承接说明
  - 入口卡、详情区、底部切换区三处风格统一
  - `risk / progress / license / acceptance / wbs` 仍能按各自适配的卡片、时间轴或分组列表展示
- **进度回写示例**
  - `9.3 completed: Reports 已删除说明卡；AnalysisEntryCard 紧凑化；详情区改为 3+1 + 双列 + 建议区；底部切换区改为紧凑网格`

### 2.5 `9.4` Milestones

- **目标文件**
  - `client/src/pages/Milestones.tsx`
- **开始前定位命令**
  ```powershell
  rg --line-number "PageHeader|重点关注|milestoneOverview|MilestoneTreeList|parent_id|wbs_code|TabsTrigger|overdue|soon" client/src/pages/Milestones.tsx
  ```
- **具体动作**
  1. 删除当前固定右列“重点关注”模块
  2. 新增 `selectedMilestone` 之类的前端状态，让右侧改为“点击里程碑后显示详情”
  3. 默认未选中时，左侧主列表优先占据页面注意力；右侧默认不常驻
  4. 保留 `overdue / soon` 逻辑，但改由左侧列表排序、状态 badge、Tabs 筛选和右侧详情字段承接
  5. 删除副标题、筛选说明、层级说明、空状态说明等说明性文案，只保留标题
  6. 首轮默认保留当前 `milestoneOverview.items` 的树状主列表实现，不在没有明确证据时切换数据源
  7. 只有当现场确认“当前树层级与任务列表层级明显不一致”时，才允许开启前端重组分支，并在进度 note 中记录触发原因
  8. 若开启了前端重组分支，只允许按任务列表层级重组仅里程碑节点的展示树，且必须保留原层级缩进，不额外重算层级
- **不要做的事**
  - 不新增弹窗
  - 不新增独立窗口
  - 不把页面改回老式固定“右侧重点关注”结构
  - 不默认改后端接口
  - 不在没有截图或现场证据的情况下自行切换 `Milestones` 的数据来源
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/Milestones.test.tsx
  ```
- **通过标准**
  - 固定“重点关注”模块已移除
  - 点击里程碑后右侧才出现详情
  - `overdue / soon` 逻辑仍保留，但没有新增弹窗
  - 层级关系和缩进仍可辨识
- **进度回写示例**
  - `9.4 completed: Milestones 固定右栏重点关注已删除；右侧改为点击后详情；overdue/soon 改由列表排序+badge+tabs+详情承接`

### 2.6 `9.5` RiskManagement

- **目标文件**
  - `client/src/pages/RiskManagement.tsx`
- **开始前定位命令**
  ```powershell
  rg --line-number "getRiskSourceBadge|risk_source|自动来源|source.*badge|PageHeader|Badge|自动检测|手动补录" client/src/pages/RiskManagement.tsx
  ```
- **具体动作**
  1. 重排风险卡片的信息分区
  2. 收敛 badge 样式
  3. 按 `risk_source` 归一展示来源 badge；若字段缺失，再按 `source / is_auto / created_by` 兜底映射
  4. 删除副标题与说明性文案，只保留标题
- **不要做的事**
  - 不抹平风险等级语义色
  - 不改风险 / 问题 / 预警的业务口径
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/RiskManagement.test.tsx
  ```
- **通过标准**
  - 风险项描述、来源、等级、状态层次更清晰
  - `risk_source` 已优先作为来源 badge 展示依据，缺失时才走兜底映射
  - 来源 badge 不能继续写死为“自动来源”这类硬编码字符串，必须优先从字段读取
  - 等级语义色保留
- **进度回写示例**
  - `9.5 completed: RiskManagement 卡片分区重排完成，badge 已收敛；risk_source 来源展示已补齐；等级语义色保留`

### 2.7 `9.6` TaskSummary

- **目标文件**
  - `client/src/pages/TaskSummary.tsx`
- **具体动作**
  1. 将 `card-v4` 收敛到 `card-base`
  2. 优化骨架屏中的筛选栏
  3. 删除副标题和说明性文案，只保留标题
- **不要做的事**
  - 不改任务总结的数据口径
  - 不把 `TaskSummary` 从任务管理子页改回独立平级模块
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/TaskSummary.test.tsx
  ```
- **通过标准**
  - 页面不再依赖 `card-v4`
  - 筛选骨架更接近真实布局
- **进度回写示例**
  - `9.6 completed: TaskSummary 完成 card-base 收敛与筛选骨架优化`

### 2.8 `9.7` PreMilestones

- **目标文件**
  - `client/src/pages/PreMilestones.tsx`
  - `client/src/pages/preMilestonesLifecycle.ts`
- **具体动作**
  1. 将 `Loader2` loading 替换为骨架屏
  2. 将 `card-v4` 收敛到 `card-base`
  3. 统一 tab 样式
  4. 删除副标题和说明性文案，只保留标题
- **不要做的事**
  - 不在本步新建独立图纸页
  - 不改前期证照与施工图纸的业务口径
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/preMilestonesLifecycle.test.ts
  ```
- **通过标准**
  - 两个 loading 区都不再使用 `Loader2`
  - 页面不再使用 `card-v4` 作为主壳
- **进度回写示例**
  - `9.7 completed: PreMilestones 已完成 skeleton/card-base/tab 收口；说明性文案已删除`

### 2.9 `9.8` GanttView

- **目标文件**
  - `client/src/pages/GanttView.tsx`
  - `client/src/pages/GanttViewComponents.tsx`
- **开始前定位命令**
  ```powershell
  rg --line-number "BatchActionBar|关键路径|统计|StatCard|PageHeader" client/src/pages/GanttView.tsx client/src/pages/GanttViewComponents.tsx
  ```
- **具体动作**
  1. 保持整体结构，不做大范围布局重写
  2. 只处理顶部统计卡所在块，将顶部统计卡收敛为统一 `StatCard`
  3. 删除页面副标题和说明性文案，只保留标题
- **必须保持不动**
  - `BatchActionBar`
  - `WBS` 结构表达
  - `关键路径` badge 与筛选
  - 当前条件 / 阻碍交互链
- **不要做的事**
  - 不顺手改关键路径算法
  - 不把当前任务列表页重做成独立甘特图页
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/GanttView.test.tsx
  ```
- **通过标准**
  - 顶部统计卡风格统一
  - 页面语义仍是“任务管理 / 任务列表”
  - 关键路径、WBS、批量操作与筛选结构保持稳定
- **进度回写示例**
  - `9.8 completed: GanttView 顶部统计卡已统一为 StatCard；标题保留，说明性文案已删除`

### 2.10 `9.9` UI 总验收与收口

- **目标文件**
  - `client/src/pages/Dashboard.tsx`
  - `client/src/pages/CompanyCockpit.tsx`
  - `client/src/pages/Reports.tsx`
  - `client/src/pages/Milestones.tsx`
  - `client/src/pages/RiskManagement.tsx`
  - `client/src/pages/TaskSummary.tsx`
  - `client/src/pages/PreMilestones.tsx`
  - `client/src/pages/GanttView.tsx`
- **具体动作**
  1. 统一复查标题、副标题、说明卡、空状态说明是否符合“只留标题”规则
  2. 统一复查页面首屏空白是否已收口
  3. 统一复查 UI 改造没有误伤原有入口与测试
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/Reports.test.tsx
  npx vitest run --root client src/pages/__tests__/Milestones.test.tsx
  npx vitest run --root client src/pages/__tests__/RiskManagement.test.tsx
  npx vitest run --root client src/pages/__tests__/TaskSummary.test.tsx
  npx vitest run --root client src/pages/__tests__/GanttView.test.tsx
  npx vitest run --root client src/pages/__tests__/sharedSummary.test.tsx
  npx vitest run --root client src/pages/__tests__/systemMainChains.test.tsx
  ```
- **通过标准**
  - `9.1-9.8` 相关页面的 UI 回归全部通过
  - 没有残留的大段说明性文案
- **进度回写示例**
  - `9.9 completed: UI 收口总验收通过，8 个页面文案/布局/入口已统一复查`

---

## 3. 专项施工步骤（10.x）

> 本章为专项施工步骤的纯索引层，只记录步骤编号、前置条件、gate 定义和通过判定，不含具体实施动作。
> 所有具体实施动作已整体委托给 `施工图纸实施专项方案_20260408.md`（DW.x 步骤），本章每步均注明对应的 DW.x 映射。
> 业务流程优化步骤（`10.1 / 10.2a-e / 10.8a-c / 10.9 / 10.10`）已整体移入 `业务流程优化清单.md` §十，**必须在本章之前完成**。
> 跨域公共底座统一要求见 `业务流程优化清单.md` §零。
> AcceptanceTimeline 最小 UI 收口（PageHeader/骨架屏）和 Reports 侧边栏入口两项残留动作，已并入 `施工图纸实施专项方案_20260408.md` 的 `DW.9a` 步骤。

### 3.1 `10.0` 建立专项施工基线

- **索引映射**：本步为施工图纸专项 `DW.0` 的前置基线步骤
- **前置步骤**：`业务流程优化清单.md` §十 全部步骤已 `completed`
- **gate 定义**：确认业务优化步骤全部完成、迁移编号基线、Drawings 文件就绪状态
- **通过判定**：进度文件键齐全，业务优化步骤全部 `completed`，后续专项施工步骤能直接续跑
- **详细动作**：以 `施工图纸实施专项方案_20260408.md` `DW.0` 为准
- **进度回写示例**：`10.0 completed: 业务优化步骤已确认全部完成；已建立专项施工基线`

### 3.2 `10.3` G1 图纸真实环境验证

- **索引映射**：同 `施工图纸实施专项方案_20260408.md` `DW.G1`
- **gate 定义**：真实环境 `construction_drawings` 表校验与图纸创建实测
- **阻断级联**：`10.3 blocked` 时仅阻断 `10.6-10.7`（联动与收口），`10.4-10.5`（页面壳与路由）可先行
- **通过判定**：拿到真实环境命令输出、接口响应或表校验记录时才允许写 `completed`
- **详细动作**：以 `施工图纸实施专项方案_20260408.md` `DW.G1` 为准

### 3.3 `10.4` G2.1 Drawings 独立页面与主路由

- **索引映射**：同 `施工图纸实施专项方案_20260408.md` `DW.3`
- **gate 定义**：`Drawings` 独立页面、`/projects/:id/drawings` 路由、`Sidebar` 入口
- **9.x UI 约束**：新建页面必须遵循 `9.x` 收口后的统一 UI 规则（PageHeader 只标题、card-base、骨架屏、中文空态）
- **通过判定**：路由可达、页面壳符合 9.x 规则
- **详细动作**：以 `施工图纸实施专项方案_20260408.md` `DW.3` 为准

### 3.4 `10.5` G2.2 Sidebar 入口与 PreMilestones 图纸收口

- **索引映射**：同 `施工图纸实施专项方案_20260408.md` `DW.3`（Sidebar 入口）+ `DW.4`（图纸主视图收口）
- **gate 定义**：侧边栏有 `Drawings` 入口，`PreMilestones` 不再是图纸主承载页
- **通过判定**：Sidebar 入口可见，PreMilestones 图纸区已收口为轻量入口
- **详细动作**：以 `施工图纸实施专项方案_20260408.md` `DW.3 / DW.4` 为准

### 3.5 `10.6` G7 图纸与任务条件联动

- **索引映射**：同 `施工图纸实施专项方案_20260408.md` `DW.8`
- **gate 定义**：施工执行类图纸映射任务条件、竣工归档类图纸映射验收前置
- **通过判定**：联动口径已确定，详细验收以施工图纸专项 `DW.8` 为准
- **详细动作**：以 `施工图纸实施专项方案_20260408.md` `DW.8` 为准

### 3.6 `10.7` G8 证照管理三子页联动与信息架构收口

> 导航口径说明：侧边栏父级最终显示名统一为 `专项管理`；本节保留的 `证照管理` 仅用于沿用既有专项命名。

- **索引映射**：综合步骤，涉及施工图纸专项 `DW.9` + 本步两项残留动作（已并入 `DW.9a`）
- **gate 定义**：`前期证照 / 施工图纸 / 验收时间轴` 三子页结构、导航关系和摘要跳转已收口
- **残留动作委托**：以下两项原属 10.7 的动作已并入 `施工图纸实施专项方案_20260408.md` 新增的 `DW.9a` 步骤：
  - AcceptanceTimeline 最小 UI 收口（PageHeader 统一、页面级 Loader2 改骨架屏，不改业务逻辑）
  - Reports 侧边栏入口（Sidebar 增加 Reports 独立入口，移除页面内散落分析跳转按钮）
- **通过判定**：三子页结构收口、AcceptanceTimeline 页壳统一、Reports Sidebar 入口已建立
- **详细动作**：以 `施工图纸实施专项方案_20260408.md` `DW.9` + `DW.9a` 为准

### 3.7 本章全局约束（适用于 10.0-10.7 全部步骤）

- 本章所有步骤除各自显式通过判定外，还必须同时满足《业务流程优化清单.md》中适用的 §零 跨域公共底座统一要求、§九 数据完整性要求、§十四 用户理解与操作效率、§十六 跨页面统一口径。
- 本章为纯索引层，不含具体实施动作；如需查看具体实施步骤、子步骤拆分或 review 规则，应打开对应的专项方案文件。
- 各步骤的专项方案内部必须遵守统一的执行方法规范（全局约束、test→impl→review 子步骤拆分、gaps 结构、需求摘录限制），该规范定义在各专项方案自身的「全局约束」和「执行方法补充」章节中。

---

## 4. `11.x / 12.x / 13.x` 专项接入共通规则

- 本文对 `11.x / 12.x / 13.x` 只保留执行顺序、gate、入口和收口边界；详细实现不在主文档重复展开。
- 若主文档与专项文档出现细节层冲突，以对应专项文档为准；主文档只负责“先做什么、做到哪算过、与谁衔接”。
- 执行时明确要求：主文档只看 `gate`、编号和边界，落地细节一律以对应专项文档为准。
- `12.x / 13.x` 以及 `10.4-10.7` 涉及证照管理三子页时，统一入口固定为：`证照管理三子页统一执行入口_20260409.md`。
- 除关键路径外，新增专项文档默认按 `产品改造稿 -> 字段模型/状态稿 -> 页面原型稿 -> 实施专项方案` 顺序阅读；不要跳读实施方案后再反推数据口径。

---

## 5. `11.x` 关键路径专项接入说明

- `11.0-11.6` 已正式预建到 `EXECUTION_PROGRESS.json`
- `11.x` 的详细执行内容以 `关键路径改造专项方案_20260407.md` 为准，不在本文件重复展开
- 关键路径专项在任务列表中的落点统一收口为：关键路径摘要、筛选/高亮、延期影响提示、查看弹窗；不在任务列表主区新增关键链分析面板、多维分布或复杂解释区
- 默认自动顺序是 `10.7 completed -> 11.0`（即业务优化清单全部完成 + 本文专项施工步骤全部完成后）
- 若人工明确调整优先级，至少要求 `9.9 completed` 且 `业务流程优化清单.md` §十 全部 `completed`，且当前不存在 `10.x` 的 `in_progress`

---

## 6. `12.x` 前期证照专项接入说明

- `12.0-12.8` 已作为前期证照正式专项接入执行体系
- `12.x` 的详细执行内容以 `前期证照实施专项方案_20260408.md` 为准，不在本文件重复展开
- 前期证照相关文档参照顺序固定为：`前期证照产品改造稿_20260408.md -> 前期证照字段模型+状态流转稿_20260409.md -> 前期证照页面原型稿_20260409.md -> 前期证照实施专项方案_20260408.md`
- 默认自动顺序建议为 `11.6 completed -> 12.0`
- 若人工明确调整优先级，至少要求 `10.7 completed`，且当前不存在证照管理相关 `in_progress`
- **共享底座前置要求**：`12.6`（前期证照预警/风险/问题联动）要求 `10.2a completed`（issues 来源链路可用）且 `10.2c completed`（预警持久化链路可用）；若 `10.2` 子步骤仅部分完成，`12.6` 必须标记为 `blocked` 并在 note 中注明缺失的底座

---

## 7. `13.x` 验收流程轴专项接入说明

- `13.0-13.8` 已作为验收流程轴正式专项接入执行体系
- `13.x` 的详细执行内容以 `验收流程轴实施专项方案_20260408.md` 为准，不在本文件重复展开
- 验收流程轴相关文档参照顺序固定为：`验收流程轴产品改造稿_20260408.md -> 验收流程轴字段模型+状态机稿_20260408.md -> 验收流程轴页面原型稿_20260408.md -> 验收流程轴实施专项方案_20260408.md`
- 默认自动顺序建议为 `12.8 completed -> 13.0`
- `10.7` 只负责证照管理三子页信息架构收口，不再承担验收流程轴重构本体
- **共享底座前置要求**：`13.7`（验收联动预警接入）要求 `10.2a completed`（issues 来源链路可用）且 `10.2c completed`（预警持久化链路可用）；若 `10.2` 子步骤仅部分完成，`13.7` 必须标记为 `blocked` 并在 note 中注明缺失的底座

---

## 8. 一句话结论

执行本文件时，`9.x UI 收口` 已完成；先执行 `业务流程优化清单.md` §十 全部步骤，再推进本文第 10 章专项施工步骤（`10.0 / 10.3-10.7`），随后按正式专项继续进入 `11.x 关键路径`、`12.x 前期证照`、`13.x 验收流程轴`；其中 `G1` 先验证后定性，`10.7` 负责证照管理三子页信息架构收口与 Reports 侧边栏入口，后续专项详细执行分别以对应实施专项方案为准。
