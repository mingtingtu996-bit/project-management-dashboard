# 全应用测试未完全完成清单

更新时间：2026-04-20

判定口径：

- 本清单基于 `docs/plans/全应用测试执行方案_20260419.md` 与 `docs/plans/全应用测试执行结果台账_20260419.md` 当前主账状态整理。
- “明确未形成闭环”指当前主台账中未见正式执行记录，或未达到可用于发布准入的章节闭环。
- “部分覆盖但未完成”指已有零散台账行，但距离方案要求的章节级覆盖仍有明显缺口。
- 本清单只用于补测、补账和收口，不替代主台账本身。

## 1. 明确未形成闭环

### 1.1 §3.20 完整业务生命周期 E2E

- 项目全生命周期：创建项目、配置基础信息、导入 WBS 模板、生成任务、设置里程碑、邀请团队、执行、偏差分析、验收、归档
- 任务全状态流转：`todo -> in_progress -> completed`
- 风险全生命周期：系统预警、升级为风险、缓解、关闭或升级为问题、问题关闭
- 延期申请全流程：提交、命中冲突边界、审批通过/驳回、撤回、重新提交
- 验收全流程：`draft -> preparing -> ready_to_submit -> submitted -> inspecting -> rectifying -> passed -> archived`
- 前期证照全流程：创建事项、添加工作项、关联依赖、逐项完成、整体通过、验收关联检查
- 月度计划周期：创建基线、确认锁定、创建月度计划、月度确认、次月滚动、偏差分析闭环
- 图纸管理全流程：模板创建图纸包、上传版本、送审、审查通过、设为当前有效版、信号检测、问题升级
- WBS 模板应用全链路：创建模板、维护节点、应用到项目、生成带 `wbs_code` 的任务树
- 数据导入导出往返：项目 JSON 导出后在另一浏览器导入恢复；Excel 导出/导入后任务数据完整

### 1.2 §6.1 浏览器与分辨率

- `Edge` 主回归
- `Chrome` 正式主流程回归
- `1920x1080 / 1366x768 / 移动端模拟` 三档分辨率检查

### 1.3 §6.2 性能指标

- Dashboard 首屏 `<= 2s`
- Gantt `1000` 任务加载 `<= 5s`
- 月度计划生成 `<= 3s`
- CPM 重算 `<= 2s`
- 报表查询 `<= 3s`
- 批量操作 `100+` 条 `<= 5s`
- 无持续白屏、长时间主线程卡死、明显掉帧

### 1.4 §6.3 可访问性基础

- `SkipLink` 存在且可用
- 所有可交互元素可通过 `Tab` 聚焦
- 弹窗打开后焦点锁定在弹窗内
- 弹窗关闭后焦点恢复到触发元素

### 1.5 §7.1 日志检查

- 后端日志无批量 `ERROR`
- 前端控制台无红错
- 网络面板无异常 `4xx/5xx` 洪流
- 如已接监控平台，无新增高频错误

### 1.6 §7.2 数据库检查

- 无孤立记录
- 无状态机非法状态
- 无重复快照
- 无断链，`chain_id` 完整
- 关键表外键与删除保护逻辑符合预期

### 1.7 §8 发布准入

- 尚未形成正式发布准入评审结论
- 尚未在主台账上给出“准入通过 / 不通过”的统一判定

## 2. 部分覆盖但未完成

### 2.1 §3.11 里程碑 / 任务汇总

已覆盖：

- `Milestones` 首屏基础渲染
- `TaskSummary` 首屏与结果摘要区
- `Milestones -> Gantt -> TaskSummary` 部分跳转

仍未完成：

- 里程碑 `CRUD`
- 里程碑与任务关联
- 里程碑完成度统计
- 任务执行分析、对比分析
- 导出功能
- 空项目空态
- 四状态筛选：`completed / soon / overdue / pending`
- 统计面板准确性：`total / pending / completed / overdue / upcomingSoon / completionRate`
- `wbs_code`
- `mapping_pending`
- `merged_into`
- `parent_id` 层级缩进与折叠

### 2.2 §3.15 Hooks / 前端交互体验

已覆盖：

- `useDataSync`
- `useRealtimeConnection`
- `useConflictDetection`

仍未完成：

- `useUnsavedChangesGuard` 的通用离页/切项目闭环
- `usePlanningKeyboard`
- `usePlanningSelection`
- `useTabPersist`
- `useAuditLog`

### 2.3 §3.16 通用弹窗与守卫

已覆盖：

- 删除保护弹窗
- 普通确认弹窗的局部链路
- 个人资料弹窗
- 冲突弹窗的静态/局部核查

仍未完成：

- 条件预警弹窗
- 行动守卫弹窗
- 登录弹窗
- 标准工序抽屉
- 统一权限守卫矩阵
- 通用未保存变更守卫矩阵

### 2.4 §3.17 其他业务页

已覆盖：

- `ResponsibilityView` 基础切换与搜索
- `ProjectTeamManagementPanel` 邀请码创建/撤销
- `JoinProject` 无效码与成功加入

仍未完成：

- `ResponsibilityView` 三态颜色与文案
- `ResponsibilityView` 关注状态流转
- `ResponsibilityView` 15 项指标真值核对
- `ResponsibilityView` 下钻到 Gantt
- `ResponsibilityView` `risk_pressure` 公式
- 移除成员后即时失权
- 角色变更立即生效
- 过期邀请码不可用
- `max_uses` 用尽自动失效
- `canManageTeam` 权限控制
- `page / drawer` 双布局覆盖
- 待关联责任人批量关联链
- 负责人转让链
- `JoinProject` 过期码
- `JoinProject` 已加入提示
- `JoinProject` 未登录引导

### 2.5 §3.18 多人实时协作 E2E

已覆盖：

- WebSocket 建链与 polling 回退恢复
- 月计划锁冲突类局部静态核查

仍未完成：

- 双人同时编辑同一任务 `409 + 冲突弹窗`
- A 修改任务后 B 无刷新同步
- 高风险通知跨账号实时推送
- 延期审批后他人 Gantt 即时刷新
- `owner / editor / viewer` 三角色协作
- 成员移除后即时失权
- 离线编辑恢复同步
- 并发创建同名资源
- 断线期间变更补齐

### 2.6 §3.19 跨模块业务联动 E2E

已覆盖：

- 部分 B 域、C 域、D 域联动点

仍未完成：

- 任务完成自动满足后置条件并解除阻碍
- 预警 -> 风险 -> 问题三级链路全追溯
- 延期审批 -> 任务日期 -> `change_logs` -> 报表偏差
- 图纸缺漏 -> 问题 -> 通知 -> 甘特受阻
- 基线锁定 -> 月计划 -> 报表三主线
- 前期证照完成 -> 验收前置要求满足
- 里程碑完成 -> Dashboard / 报表统计同步
- 材料到场 + 图纸通过 -> 施工条件自动满足
- 责任人变更 -> 责任视图 + 任务分配通知同步
- 删除项目后的级联清理

### 2.7 §6.4 发布阻塞项：基础设施与边界

已覆盖：

- `clearAppStorage` 局部核查
- 离线写阻断
- `useDataSync` 局部核查
- `useProjectInit not_found`
- `websocket -> polling`

仍未完成：

- `QuotaExceededError / NS_ERROR_DOM_QUOTA_REACHED`
- 存储告警绑定防重复监听
- `safeStorageGet`
- `safeJsonParse`
- `listAppStorageKeys`
- SSR 安全降级
- `LOCAL / SYNC / READONLY` 三种存储模式
- 最大重试与失败态矩阵
- 同步状态监听器矩阵
- 六类实体冲突检测全覆盖
- 项目初始化降级回填矩阵
- 极端边界值与兼容矩阵

### 2.8 §6.5 工程观测项：性能与实现质量

仍未完成：

- `CompanyCockpit` 冷启动可交互时间
- 路由懒加载阻塞评估
- Gantt `300` 任务首渲染
- `setInterval / WebSocket / AbortController` 清理检查
- `Reports` 重算风暴检查
- 并发请求取消与竞态控制
- 包体回退检查
- 打印场景检查

### 2.9 §6.6 全页面系统性体验检查

仍未完成：

- 10 个关键页面骨架屏矩阵
- 10 个关键页面错误面板矩阵
- 10 个关键页面空态矩阵
- 页面切换后请求取消与卸载后不 `setState`
- CRUD 成功/失败 toast 一致性
- `DialogDescription + ESC`
- 必填校验矩阵
- 大列表分页或等效兜底矩阵

### 2.10 §7.3 安全与权限检查

已覆盖：

- 局部 `authenticate`
- 局部权限/RLS 问题修复链

仍未完成：

- 白名单路由全集检查
- 受保护业务路由全集检查
- 写权限矩阵
- `/api/auth/*` 限流
- `/api/*` 全局限流
- `XSS`
- `SQL` 注入
- `CSRF`
- `JWT` 过期统一验证

## 3. 按补充复核新增纳入

### 3.0 §3.0 Header

已覆盖：

- 个人资料保存
- 修改密码入口
- 团队管理入口
- 邀请码创建与撤销
- 实时连接状态降级与恢复

仍未完成：

- 复制邀请链接写入剪贴板 + 成功 toast + 冷却恢复
- 邀请链接仅在项目页且存在主邀请码时显示
- 退出登录清除 token 并跳转登录页
- 页面标题 / 上下文标签随 pathname 动态切换

### 3.1 §3.1 登录与权限

已覆盖：

- 登录入口、Header 用户菜单入口
- 个人资料保存
- 邀请码加入项目

仍未完成：

- 注册新账号
- 修改密码的真实提交闭环
- 重置密码
- 项目所有权转移
- `viewer` 角色按钮可见性矩阵
- 接口越权拦截矩阵
- 未登录访问项目页时的引导登录链

### 3.2 §3.2 项目管理基础

仍未完成：

- 创建项目
- 编辑项目信息
- 归档项目
- 激活项目
- 参建单位 `CRUD`
- 责任单位绑定
- 范围维度设置：楼栋、专业、阶段、区域
- 项目归档后不可编辑
- 激活后恢复可编辑
- 删除项目级联处理

### 3.3 §3.4 监控面板 / 数据质量 / 健康评分

已覆盖：

- `/monitoring` 页面打开
- 指标卡基础渲染
- 部分真实运行态指标卡

仍未完成：

- 数据质量评分真值核对
- 项目健康评分真值核对
- 计划治理状态联动
- 反馈弹窗
- 错误日志展示矩阵
- 性能监控指标章节级闭环

### 3.4 §3.5 Gantt / 任务链 / 共享真相层

已覆盖：

- 任务主链增改查
- 条件、阻碍、延期申请主链
- 进度恢复与共享真值主链

仍未完成：

- 右键菜单 11 项操作的完整 UI 验证
- 任务详情面板中审批/驳回延期申请的 UI 交互
- 横道图时间轴缩放
- 依赖线渲染
- 拖拽排序与层级调整交互
- 基线双线对比展示
- `lagging` 三档状态 chip：`偏慢 / 慢 / 严重滞后`
- 任务新建/编辑表单高级区展开/折叠与 `localStorage` 记忆
- 屏幕宽 `<1024px` 时横道图降级提示
- 阻碍类型“设计” -> “查看相关图纸”跳转链接

### 3.5 §3.7 风险 / 问题 / 预警链

已覆盖：

- 风险/问题/预警主链
- Warning -> Risk -> Issue 的部分升级链

仍未完成：

- 自动优先级分数展示：来源权重 × 严重性权重 × 时间衰减
- 已锁定 issue 在 `change_logs` 中写入 `field_name='priority'`
- `pendingManualClose` 筛选
- 同一任务同周期通知去重：`critical_path_stagnation + progress_trend_delay`
- 延期审批通知去重：`delay_request_reminder` 仅在 `submitted` 且已读未处理时发送

### 3.6 §3.8 前期证照

已覆盖：

- 前期证照状态口径
- 前期证照状态机转换矩阵

仍未完成：

- 证照依赖关系 `CRUD`
- 依赖环检测
- 办理事项联动：看板 / 台账 / 详情三路刷新
- 解锁施工功能
- 生成 WBS 功能
- `CertificateSharedRibbon` hover 高亮联动
- `LifecycleSummaryCards` 点击切换筛选行为

### 3.7 §3.9 施工图纸

已覆盖：

- 图纸基础页与部分状态更新链
- 图纸包列表路由恢复
- 图纸乐观锁 409

仍未完成：

- 图纸版本管理：创建版本 / 设置当前版本 / 快照
- 图纸包应有项清单与完整度计算
- 送审规则判定：`mandatory / optional / none`
- 版本更新通知
- `DrawingReadinessSummary` 就绪度汇总

### 3.8 §3.10 验收流程轴

已覆盖：

- 验收状态机转换矩阵

仍未完成：

- 验收节点拖拽
- 验收要求 `is_required / is_satisfied` CRUD
- `parallel_group_id` 设置
- 依赖关系环检测
- 视图模式持久化
- 时间轴缩放

### 3.9 §3.12 计划编制

已覆盖：

- 关键路径主链
- 基线 / 月计划 / 关账 / WBS 的部分首屏与主流程

仍未完成：

- 基线撤销/重做：最多 `60` 个快照
- Excel 导入基线：多列名格式、Excel 日期、里程碑识别
- `buildDiffItems` 四类差异对比
- 月度五路并发加载与月份切换
- 月度草稿“从基线” vs “从任务排期”双来源切换
- 关账梯度升级：`3日 / 5日 / 7日`
- `buildCloseoutItems` 三类建议项：`done / needsCarry / manual`
- `DataConfidenceBreakdown` 置信度展示

### 3.10 §3.13 材料管控

已覆盖：

- 材料首屏
- 材料详情弹窗
- 页面级编辑、筛选与报表跳转

仍未完成：

- 材料 `CRUD`
- 批量导入
- 模板创建模式
- 批量草稿行编辑
- `actual_arrival_date` 设置后的状态切换
- `requires_sample_confirmation / requires_inspection` 按钮显示与切换
- AI 工期估算
- AI 排程建议
- AI 结果采纳
- 材料到货提醒
- 周报摘要

### 3.11 §3.14 报表与通知

已覆盖：

- 报表与通知主页面的部分首屏和主链

仍未完成：

- 延期统计子视图
- 延期与阻碍关联分析子视图
- 执行散点图
- `S` 曲线图
- 周度简报 `WeeklyDigest` 6 个区块完整展示
- `snapshot_gap` 不重复推送验证
- 预警底层条件恢复后通知自动消除验证
- 侧边栏“专项管理”标签文案验证

### 3.12 §3.15 Hooks / 前端交互体验补充缺口

仍未完成：

- `useAuditLog` 的章节级专门执行记录

### 3.13 §4.2 延期审批边界

仍未完成：

- 延期审批超时第 `3` 天提醒
- 延期审批超时第 `5` 天升级
- 关键路径任务延期通知级别升级

### 3.14 §4.3 条件 / 阻碍边界

仍未完成：

- 管理员强制解除条件
- 障碍超时 `severity` 升级

### 3.15 §4.4 通用异常

已覆盖：

- 部分错误边界与权限异常链

仍未完成：

- 标签页长时间休眠后恢复
- 普通成员尝试审批/关闭/导出的权限拦截矩阵

### 3.16 §4.10 定时任务

已覆盖：

- `§4.10.1 / §4.10.2 / §4.10.8 / §4.10.9 / §4.10.10 / §4.10.11 / §4.10.14 / §4.10.15` 的静态分析台账
- Jobs API、Monitoring、`operationalNotificationJob` 的局部真实环境记录

仍未完成：

- `§4.10.3 conditionAlertJob`：4 路并发单路失败不阻断其他分支、自动升级链等 6 项
- `§4.10.4 riskStatisticsJob`：每日风险快照、`job_execution_logs` 写入、`getStatus()` 字段等 6 项
- `§4.10.5 responsibilityAlertJob`：`scanned / abnormalSubjects` 统计、`getNextRunTime()` 跨日计算等 6 项
- `§4.10.6 delayRequestReminderJob`：`reminder / escalation` 两类通知生成、`initialDelay` 对齐 `09:00` 等 6 项
- `§4.10.7 dataQualityJob`：`confidence.flag='low'` 统计、`initialDelay` 对齐 `02:30` 等 5 项
- `§4.10.12 healthHistorySnapshotJob`：月度健康快照记录 3 项
- `§4.10.13 weeklyDigestJob / materialArrivalReminderJob`：每周一 `09:00` 对齐、材料到货提醒数量等 4 项
- `§4.10` 顶层项的真实环境闭环：基线有效性扫描触发 `pending_realign`
- `§4.10` 顶层项的真实环境闭环：data-quality 报表置信度面板同步
- `§4.10` 顶层项的真实环境闭环：健康度定时计算后 Dashboard 刷新

### 3.17 §5.1 快照一致性

已覆盖：

- 延期审批后 `task_progress_snapshots` 双快照写入链
- 条件恢复推进链的快照写入与同日去重

仍未完成：

- 跨月重开写入 `cross_month_reopened` 事件
- 快照触发 `projectHealthService` 更新

### 3.18 §5.2 change_logs 完整性

已覆盖：

- 基线确认写入 `change_logs`
- 月计划确认 / 草稿 / 强制关闭写入 `change_logs`
- 延期申请与 `task_conditions` 状态变更写入 `change_logs` 的静态核查

仍未完成：

- 任务字段变更写入 `change_logs` 的真实环境验证
- 里程碑一致性检测写入 `change_logs`（`M3 / M4`）

### 3.19 §5.4 其他一致性

已覆盖：

- 月度计划与基线 `baseline_version_id` 关联写入
- 关键路径快照主链与人工覆盖主链

仍未完成：

- 关键路径快照与任务 `is_critical` 一致性
- 删除保护替代动作不会造成孤立记录

### 3.20 §5.5 乐观锁一致性

已覆盖：

- `tasks` 乐观锁冲突 `409 + VERSION_MISMATCH`
- 施工图纸 `lock_version` 冲突 `409`

仍未完成：

- 风险实体的 version 冲突 `409` 验证
- 问题实体的 version 冲突 `409` 验证
- 参建单位相关实体的 version 冲突 `409` 验证

### 3.21 §5.8 审计日志完整性

仍未完成：

- `useAuditLog` 前端调用闭环
- `auditLogger` 后端拦截
- 创建操作写入审计日志
- 编辑操作写入审计日志
- 删除操作写入审计日志
- 审批操作写入审计日志

### 3.22 §5.10 状态机转换矩阵补充缺口

已覆盖：

- 延期申请状态机
- `task_conditions` 状态流转
- `task_obstacles` 状态流转
- 前期证照状态机
- 验收状态机

仍未完成：

- 任务状态机：`todo -> in_progress -> completed`
- 任务重开：`completed -> in_progress`
- 任务阻塞：进入 `blocked`
- 非法转换拒绝矩阵

### 3.23 §5.11 进度推进 8 步链端到端验证

已覆盖：

- B1-LIVE 系列中的部分进度推进主链

仍未完成：

- `first_progress_at / actual_start_date` 首次推进补写
- `actual_end_date` 完成时设置
- 完成后自动满足依赖条件
- 重开后清空 `actual_end_date`
- 跨月重开 `cross_month_reopened` 事件
- 针对完整 8 步链的专门台账闭环

### 3.24 §5.12 升级链完整数据链路

已覆盖：

- `chain_id` 与 `escalated` 的静态核查

仍未完成：

- `Warning -> Risk -> Issue` 自动升级全链真实环境验证
- `chain_id` 从 warning 到 issue 的全程可追溯验证
- 删除已升级记录返回 `422` 的真实环境验证

### 3.25 §5.14 localDb 数据完整性

已覆盖：

- `exportAllData / importBackup / getAutoBackupList`
- `clearAppStorage / getStorageUsage / 7表导出` 的静态核查

仍未完成：

- `projectDb.delete` 级联清除
- `taskDb.update` 版本冲突与“保留本地”选项
- `replaceByProject` 跨项目隔离
- `upsert` 幂等性
- `generateId` 无重复

## 4. 额外缺口

### 4.1 §4.5 WebSocket 实时通信

已覆盖：

- WebSocket 建链
- `polling -> 实时同步` 恢复

仍未完成：

- 跨账号通知实时推送
- 项目频道任务更新同步
- `projectId / userId` 切换订阅
- `token` 过期拒连
- 心跳保活
- 异常消息静默丢弃
- 组件卸载时连接与定时器清理

### 4.2 §4.8 本地存储与数据备份

已覆盖：

- JSON 导出/导入
- 本地存储核心 API 局部核查

仍未完成：

- 清空浏览器缓存后无崩溃并重新拉取
- Excel 导出
- 完整恢复验证

### 4.3 §4.11 导出 / 下载 / 附件

已覆盖：

- WBS 导出链

仍未完成：

- 项目数据导出
- 任务列表导出
- 报表导出与下载
- 图纸附件上传与下载
- WBS 模板导入真实执行

### 4.4 §4.12 降级与恢复场景

已覆盖：

- 离线写阻断
- `5xx` toast 聚合
- `401` 处理
- `polling` 回退

仍未完成：

- 后端宕机时前端只读缓存态
- 后端恢复后的统一补推
- `429` 提示
- 跨标签页 token 失效感知

## 5. 使用建议

- 本清单只表示“当前还未完成收口”的项，不代表这些项完全未做。
- 如某项实际上已经执行，但主台账未及时落行，应优先补主台账，而不是只改本清单。
- 如某项已有历史执行证据但受共享层改动影响，应在主台账中标记 `待回归`，不能直接算 `通过`。
