# 业务流程优化实施专项方案

> 本文是业务流程优化域的唯一执行入口，只包含实施步骤，不包含产品需求定义。
> 产品需求真值来源：《业务流程优化清单.md》全文；执行步骤已整体迁出，不再保留在需求文件中。
> 本文步骤中涉及的字段定义、业务规则、页面规格，以需求文件对应章节为准，本文不重复定义。
> 若本文与需求文件表述不一致，以《业务流程优化清单.md》为准；若需求文件存在歧义，必须先确认后执行。
> 需求文件不含实施步骤，不作为执行入口。

> 步骤编号与 EXECUTION_PROGRESS.json 保持一致
> 执行顺序：10.1 → 10.2a → 10.2b → 10.2c → 10.2d → 10.2e → 10.2f → 10.2g → 10.8a → 10.8b → 10.8c → 10.9 → 10.10 → 10.10a
> 每步包含：当前判断、产品需求真值引用、目标文件、具体动作、通过标准、进度回写模板

## 全局约束

- 本文所有步骤除各自显式要求外，还必须同时满足《业务流程优化清单.md》中适用于本步骤的 §零 跨域公共底座统一要求、§九 数据完整性要求、§十四 用户理解与操作效率、§十六 跨页面统一口径（20260412 确认）。
- 判定步骤完成时，必须同时满足：功能动作完成、验证通过、且未违反上述适用全局约束。
- 若步骤以服务端或共享底座为主，优先检查 §零 / §九；若步骤以页面、交互或文案为主，优先检查 §十四 / §十六；若同时涉及两类，则四章都应检查适用项。

## 执行方法补充

- 正文任一步骤开始前，必须先对照《三份专项方案执行前检查清单.md》筛出本步适用的 `blocked` 条件；若命中任一阻断项，当前步骤不得直接进入实现。
- 下列步骤已在正文内明确拆成子步骤：`10.2a / 10.2b / 10.2c / 10.2d / 10.2f / 10.2g / 10.10 / 10.10a`；执行时不再依赖模型自行判断是否拆分。
- 子步骤只允许内联本步必需的需求摘录，推荐控制在 `20-80` 行，并同时保留来源锚点 `业务流程优化清单.md:xxxx`；不得全文复制需求章节，避免形成第二份真值。
- 数据模型 / API / 状态机类步骤统一采用 `test -> impl -> review`；页面壳层 / 组件类步骤统一采用 `contract-test -> impl -> review`；纯视觉一致性统一采用 `story/e2e -> review`。
- `review` 步骤只做检查，不写新功能；若发现缺口，当前父步骤必须保持 `blocked`，不得直接记为 `completed`。
- `review` 发现的问题写回 `EXECUTION_PROGRESS.json` 时，必须使用统一 `gaps` 结构，至少包含 `id / source / rule / finding / fix_in` 五个字段，例如：

```json
{
  "step": "10.2c-review",
  "status": "blocked",
  "gaps": [
    {
      "id": "GAP-10.2c-01",
      "source": "业务流程优化清单.md:xxxx",
      "rule": "规则原文摘录",
      "finding": "当前实现缺了什么",
      "fix_in": "10.2c-fix-01"
    }
  ]
}
```

- 三份专项方案的实际交错顺序不是“先做完 10.x 再做 15.x”；`10.1 completed` 与 `10.2b completed` 之后，即可插入《计划编制实施专项方案.md》的 `15.0 / 15.0a`。

## 实施步骤

> 本章包含业务流程优化所需的全部实施步骤，步骤编号与 `EXECUTION_PROGRESS.json` 保持一致。
> 跨域公共底座统一要求见《业务流程优化清单.md》 §零。
> 产品需求真值见《业务流程优化清单.md》 §一～§九。
> 步骤执行顺序：`10.1 → 10.2a → 10.2b → 10.2c → 10.2d → 10.2e → 10.2f → 10.2g → 10.8a → 10.8b → 10.8c → 10.9 → 10.10 → 10.10a`

### 10.1 G3.1 问题域模型与 API 基线（含 risks 前置迁移）

- **当前判断**
  - 当前"问题"只是 `task_obstacles` 的另一种展示，不是独立 issue 域
  - 当前 `Risk` 接口（`server/src/types/db.ts`）不含 `source_type / source_id / chain_id`，必须先补齐 risks 表迁移，否则后续 chain_id 升级链无法传播
- **产品需求真值**
  - 字段定义以《业务流程优化清单.md》 §1.3 + §六 为准，本步不重复列举完整字段
  - `chain_id` 生成与传播规则以《业务流程优化清单.md》 §1.4 为准
- **目标文件**
  - `server/migrations/<next>_alter_risks_add_source_fields.sql`（risks 前置迁移）
  - `server/migrations/<next+1>_create_issues_table.sql`
  - `server/src/types/db.ts`
  - `server/src/routes/issues.ts`
  - `server/src/index.ts`
  - `server/src/__tests__/issues-domain.test.ts`
  - `client/src/lib/supabase.ts`
- **开始前定位命令**
  ```powershell
  Get-ChildItem -LiteralPath server/migrations -File | Where-Object { $_.Name -match '^(\d{3})_' } | Sort-Object Name | Select-Object -ExpandProperty Name | Select-Object -Last 10
  Test-Path server/src/__tests__/issues-domain.test.ts
  rg "source_type|source_id|chain_id" server/src/types/db.ts
  ```
- **具体动作**
  1. 确认当前最大迁移编号，使用下两个可用三位编号（一个给 risks 迁移，一个给 issues 建表）；禁止硬编码覆盖
  2. **risks 前置迁移**：`ALTER TABLE risks ADD COLUMN source_type, source_id, chain_id, pending_manual_close, linked_issue_id, closed_reason, closed_at`；移除 `occurred` 状态枚举值；同步删除 `mitigation_plan` 列；建立 `(source_id, source_type)` 复合索引；更新 `db.ts` 的 Risk 接口
  3. 新增独立 `issues` 表，字段按《业务流程优化清单.md》 §六 `issues` 行定义（含可选 `task_id`）；`source_type` 枚举包含 `manual / risk_converted / risk_auto_escalated / obstacle_escalated / condition_expired`；`source_deleted` 不作为初始创建来源；`status` 枚举：`open / investigating / resolved / closed`（流转规则见《业务流程优化清单.md》 §1.3）；建立 `(source_id, source_type)` 复合索引
  4. 新增 `/api/issues` 基础 CRUD
  5. 新建 `issues-domain.test.ts`，至少覆盖：CRUD 可用、`source_type` 合法值校验、issues 与 task_obstacles 是独立记录、risks 已有 `source_type / chain_id` 字段
  6. 本步只建模型与路由，不实现优先级自动排列、来源解除联动和自动升级逻辑（这些归 10.2a）
- **不要做的事**
  - 不继续把 `task_obstacles` 直接当问题表
  - 不把 issue 混回 `risks` 表
  - 不在本步实现 warningService 的预警→风险→问题自动升级链
- **验证方式**
  ```powershell
  npm run build --workspace=server
  npx tsc -p client/tsconfig.json --noEmit
  npm run test --workspace=server -- --run src/__tests__/issues-domain.test.ts
  ```
- **通过标准**
  - risks 迁移已执行，`db.ts` Risk 接口含 `source_type / source_id / chain_id / pending_manual_close / linked_issue_id / closed_reason / closed_at`，`occurred` 已移除
  - `issues` 迁移存在
  - `/api/issues` 可读写
  - issues 含 `source_type / source_id / task_id(可选) / severity / priority / pending_manual_close / chain_id`
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.1 completed: risks 前置迁移已落地（source_type/source_id/chain_id + 删 mitigation_plan）；issues 表与 /api/issues 基线已建立`

### 10.2a G3.2a issues 来源链路与 RiskManagement 前端切换

- **前置步骤**：`10.1 completed`
- **目标文件**
  - `server/migrations/<next>_create_issues_table.sql`
  - `server/migrations/<next>_create_change_logs_table.sql`（change_logs 建表前移，解决管理员强制解除写入依赖）
  - `server/src/services/changeLogs.ts`（基础写入服务，本步只建骨架 + force_satisfy 写入）
  - `server/src/routes/issues.ts`
  - `server/src/routes/risks.ts`
  - `server/src/routes/task-obstacles.ts`
  - `server/src/routes/tasks.ts`
  - `server/src/types/db.ts`
  - `client/src/pages/RiskManagement.tsx`
  - `client/src/pages/GanttView.tsx`
  - `server/src/__tests__/issues-domain.test.ts`
- **子步骤执行结构（必须按以下顺序推进）**
  1. `10.2a-a-impl`
     - 只实现 `issues` 表、`/api/issues`、`change_logs` 基础骨架、升级链保护和接口字段对齐，不切前端默认数据源
  2. `10.2a-b-impl`
     - 只实现 `RiskManagement` 切换、双视图、badge、趋势图、弹窗和前端引导；阶段二正式切换动作只允许放在本子步骤
  3. `10.2a-review`
     - 只读检查 `阶段二切换条件 / 删除保护 Dialog / 409 冲突回退提示 / priority_score 人工锁定`
     - 若发现缺口，必须写 `gaps` 并保持父步骤 `blocked`
- **具体动作**
  0. **change_logs 建表前移**：新建 `change_logs` 表（字段按《业务流程优化清单.md》 §六定义）+ 基础写入服务骨架（`changeLogs.ts`），本步只实现 `writeLog(entity_type, entity_id, field_name, old_value, new_value, change_reason, changed_by)` 方法；管理员强制解除（动作 #12）和乐观锁状态变更（动作 #14）立即接入；其余路由（tasks/risks/issues 状态变更）的 change_logs 写入留给 10.2b 统一接线
  0.5. **issues 建表与读写基线**：当前代码库不存在 `issues` 独立实体，本步必须先新建 `issues` 表、类型定义和 `/api/issues` 路由，再谈前端切换；字段按《业务流程优化清单.md》 §六定义，不允许继续把 `TaskObstacle` 当成问题实体替身
  1. 在风险页增加"风险转问题"按钮，调用 `/api/issues` 创建 `source_type: risk_converted`，回写 `chain_id`；源风险同步执行 `status → closed`，回写 `linked_issue_id / closed_reason = converted_to_issue / closed_at`（与自动升级路径行为一致，见 §1.2 升级/转化统一原则与 §1.4）
  2. 在阻碍链中增加"阻碍上卷问题"，调用 `/api/issues` 创建 `source_type: obstacle_escalated`
  3. 补齐人工直接登记问题入口（`source_type: manual`）
  4. `RiskManagement` 的问题列表按“两阶段迁移”推进：\n     - 阶段一：保留现有 `problems` 旧入口继续读取 `/api/task-obstacles`，同时补一个内部可切换的新问题数据源读取 `/api/issues`，用于并行验收与数据核对；\n     - 阶段二：正式切换动作只允许在 `10.2a-b-impl` 内执行，且前提必须同时满足：`issues-domain.test.ts` 已通过、`/api/issues` 三类来源链可写可读、并行验收未发现阻断级 `gaps`；满足后再将对外 `问题` tab 切到 `/api/issues`，并移除旧问题入口；\n     - 交付态只保留新版 `问题` tab，不允许长期双真值并存
  5. UI 上展示三类来源 badge
  6. 附带清理：`task_obstacles` 移除"无法解决"状态值（服务端类型 + 客户端 `supabase.ts` + `useProjectInit.ts` + `taskBusinessStatus.ts`）
  7. 在阻碍弹窗中增加 `resolution_notes`（解决措施）文本编辑区域和 `expected_resolution_date`（预计解决日期）日期选择器（障碍域前端收口）
  8. 在 RiskManagement 概览 tab 补入风险趋势图（按时间展示风险数量变化）
  9. 在 `tasks.ts` 进度更新路由增加条件全满足检查，条件未全部满足时返回 422；`GanttView` 进度输入框增加 disabled 状态 + tooltip 提示"请先满足所有开工条件"
  10. 实现 RiskManagement 链路双视图切换：①按任务节点归类（有关联任务挂任务下，无关联放"项目级"分组，用 `source_type` 标签区分自动/手动）②统一时间轴视图（所有预警/风险/问题按时间排列，用颜色/图标区分类型和来源）；支持按来源类型过滤
  11. `issues` 和 `risks` 的 DELETE 端点增加升级链删除保护校验（见 §9.2）：预警 `is_escalated = true`、风险 `linked_issue_id` 非空或 `closed_reason = converted_to_issue`、问题 `source_type` 为 `risk_converted / risk_auto_escalated` 时禁止物理删除，返回 422；**前端引导**：收到 422 时弹出 Dialog（见 §1.4 升级链删除保护前端引导），展示关联上下游记录摘要并提供"关闭此记录"主按钮替代删除
  12. GanttView 条件弹窗增加"管理员强制解除"按钮（仅 `owner / admin` 角色可见），点击后弹确认框要求填写强制解除原因（必填），执行后写入 `satisfied_reason: 'admin_force'` 并记录 `change_logs`（见 §2.1）
  13. GanttView 阻碍弹窗增加 `severity` 下拉选择器（随阻碍弹窗改造一并补入，见 §2.2）
  14. `risks` 和 `issues` 的状态变更端点（PUT）增加乐观锁校验：`WHERE updated_at = ?`，不匹配返回 409 + "该记录已被其他人修改，请刷新后重试"（见 §9.2 并发操作冲突）
  15. RiskManagement 风险 tab 增加独立"新建风险"按钮/对话框，创建 `source_type: manual` 的风险记录（见 §1.2 创建入口），表单字段：标题、描述、级别、关联任务（可选）；创建时生成独立 `chain_id`
  16. issues 列表加载时实现 `priority_score` 实时计算：`priority_score = source_weight × severity_weight × (1 + floor(未处理天数 / 7) × 0.1)`，封顶 50%；人工调整后锁定，时间衰减不再计算；权重数值见 §1.3（见 §1.3 优先级）
- **不要做的事**
  - 不在本步做预警/通知持久化收口（归 10.2c）
  - 不在本步建 `delay_requests` 表（归 10.2b）
- **执行纪律**
  - 默认按"两轮执行"推进：第一轮只做后端（issues 来源链、task_obstacles 状态清理、接口字段对齐），中间跑一轮后端测试；第二轮再做前端（RiskManagement 切 `/api/issues`、来源 badge、风险趋势图、阻碍弹窗编辑入口）
- **验证方式**
  ```powershell
  npm run build --workspace=server
  npx tsc -p client/tsconfig.json --noEmit
  npm run test --workspace=server -- --run src/__tests__/issues-domain.test.ts
  npx vitest run --root client src/pages/__tests__/RiskManagement.test.tsx
  npx vitest run --root client src/pages/__tests__/GanttView.test.tsx
  ```
- **通过标准**
  - `issues` 表、类型定义与 `/api/issues` 路由已建立
  - 问题列表不再是障碍物列表的替身
  - 手动 / 风险转入 / 阻碍上卷三类来源可见
  - "无法解决"状态已从服务端和客户端类型定义中移除
  - 阻碍弹窗已有 `resolution_notes`、`expected_resolution_date` 编辑入口和 `severity` 下拉选择器
  - 条件弹窗已有管理员强制解除按钮（仅 owner/admin 可见）
  - RiskManagement 概览 tab 已有风险趋势图
  - 条件未满足时进度填报被阻止，GanttView 进度输入框显示禁用提示
  - RiskManagement 链路双视图（任务归类 + 时间轴）可切换
  - 升级链删除保护返回 422 时，前端弹出引导 Dialog（非 toast）
  - risks/issues 状态变更端点已接入乐观锁（409 冲突响应）
  - RiskManagement 风险 tab 已有独立"新建风险"按钮，可创建 `source_type: manual` 风险
  - issues 列表加载时 `priority_score` 实时计算生效（含人工锁定后跳过时间衰减）
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.2a completed: issues 表与 /api/issues 基线已建立；RiskManagement 问题链已完成两阶段迁移切换；三类来源 badge 可见；task_obstacles 无法解决状态已清理；阻碍弹窗补齐 resolution_notes/expected_resolution_date/severity；条件弹窗管理员强制解除已落地；风险趋势图已补入；条件阻止填报已落地；链路双视图可切换；升级链删除保护前端 Dialog 已落地；乐观锁已接入；独立新建风险按钮已落地；issues priority_score 实时计算已落地`

### 10.2b G3.2b delay_requests / change_logs / task_progress_snapshots 共享底座

- **前置步骤**：`10.2a completed`
- **目标文件**
  - `server/migrations/<next>_rename_task_delay_history_to_delay_requests.sql`
  - `server/src/routes/delay-requests.ts`
  - `server/src/routes/task-delays.ts`（兼容旧入口，迁移期转发到新语义）
  - `server/src/services/changeLogs.ts`（10.2a 已建骨架，本步扩展全量接线）
  - `server/src/routes/tasks.ts`
  - `server/src/routes/task-summaries.ts`
  - `server/src/services/projectHealthService.ts`
  - `server/src/services/taskSummaryService.ts`
  - `server/src/services/schedulePredictor.ts`
  - `server/src/types/db.ts`
  - `server/src/index.ts`
  - `server/src/__tests__/shared-infrastructure.test.ts`
- **子步骤执行结构（必须按以下顺序推进）**
  1. `10.2b-a-test`
     - 需求摘录：`业务流程优化清单.md:2998`
     - 只锁 `delay_requests / change_logs / task_progress_snapshots` 的 contract 和 `actual_start_date / actual_end_date` 自动写入规则
  2. `10.2b-a-impl`
     - 只实现迁移、路由骨架、共享写入链和显式保存链自动写入，不扩到 warningService 与前端 UI
  3. `10.2b-review`
     - 只读检查 `显式保存才触发 actual_start/end / 审批通过副作用链同事务 / snapshots 不再静默降级`
     - 若发现缺口，必须写 `gaps` 并保持父步骤 `blocked`
- **具体动作**
  1. 确认当前最大迁移编号后顺延
  2. 延期数据不从零重建，按 **Rename + Extend** 迁移：将现有 `task_delay_history` 重命名为 `delay_requests`，补齐 `status / requested_by / requested_at / reviewed_by / reviewed_at / withdrawn_at / chain_id` 等字段；对存量数据统一回填 `status = approved`，并以原 `created_at / approved_at` 对齐历史审批语义
  3. 扩展 `change_logs` 写入服务（10.2a 已建表和骨架），接入任务排期/状态变更、风险/问题状态变更、延期记录等全量写入场景
  4. `task_progress_snapshots` 补齐真实写入链：任务进度/状态更新时自动写入快照；扩展字段 `status / conditions_met_count / conditions_total_count / obstacles_active_count / recorded_by`
  5. 在当前任务显式保存链（以现有 `PUT /api/tasks/:id` 为准，后续若拆 PATCH 仍必须沿用同一副作用入口）中落实 `actual_start_date / actual_end_date` 自动写入规则：①任务第一次从 `未开始` 进入 `进行中`，或第一次保存 `progress > 0` 且 `actual_start_date` 为空时，系统自动写入 `actual_start_date`；②任务第一次保存 `progress = 100%` 或状态首次进入 `已完成` 且 `actual_end_date` 为空时，系统自动写入 `actual_end_date`；③仅在显式保存时触发，不在任务创建、默认值初始化、批量导入、批量修正、拖动预览阶段触发；④任务被重新打开时默认保留第一次完成时间，不静默覆盖；若需修正，必须留 `change_logs`，并区分 `system_auto / manual_adjusted`
  6. 更新 `db.ts` 类型定义
  7. 新建 `shared-infrastructure.test.ts`，至少覆盖：delay_requests CRUD 与审批流转（pending→approved/rejected/withdrawn）、change_logs 写入在任务/风险/问题状态变更时被调用、task_progress_snapshots 在进度更新时有真实写入且扩展字段正确、`actual_start_date / actual_end_date` 仅在符合规则的显式保存链中自动写入
  8. `delay_requests` 审批路由增加业务规则：①**并发申请限制**：创建前查询同一 `task_id` 是否存在 `status = pending` 的记录，存在则返回 409；②**reason 去重校验**：被拒后重新提交时，校验 `reason` 与同一 `task_id` 最近一条 `rejected` 记录的 `reason` 不同，相同返回 422；③**withdrawn 状态**：增加撤回端点（仅申请人可操作，且仅 `status = pending` 时允许）；④**审批通过副作用链**（同一事务）：更新任务 `end_date` → 写 `change_logs` → 写 `task_progress_snapshots` → 重算 CPM → 触发 `warningService.evaluate(event)`（warningService 钩子在 10.2c 接入前先留空调用占位）
  9. 将当前仍直接查询 `task_delay_history` 的服务端代码同步切到 `delay_requests` 新语义：`projectHealthService / taskSummaryService / schedulePredictor / task-summaries / tasks` 至少这 5 处必须显式纳入本步修改范围；延期惩罚分与统计口径统一改为 `status = approved` 的记录，不把 `pending / rejected / withdrawn` 混入已生效延期
  10. 在 `scheduler.ts` 增加延期审批催促定时检查：`status = pending` 且 `created_at < now - 3d` → 提醒审批人；`created_at < now - 5d` → 升级通知项目 owner（见 §2.3 审批超时催促）
- **不要做的事**
  - 不在本步改 warningService / autoAlertService / 通知系统（归 10.2c）
  - 不在本步改前端 UI（延期审批 UI 归后续步骤）
- **执行纪律**
  - 只做共享底座三张表与写入链，不顺手扩写 warningService、通知系统或前端审批 UI
  - `delay_requests / change_logs / task_progress_snapshots` 必须有独立测试覆盖
  - `task_progress_snapshots` 写入按"任务 + 日期"保证幂等，避免重复更新产生脏记录
- **验证方式**
  ```powershell
  npm run build --workspace=server
  npm run test --workspace=server -- --run src/__tests__/shared-infrastructure.test.ts
  npm run test --workspace=server -- --run src/__tests__/issues-domain.test.ts
  ```
- **通过标准**
  - `task_delay_history -> delay_requests` 的 Rename + Extend 迁移已落地，存量记录已回填为 `status = approved`
  - `delay_requests` 表与审批路由可用，含 withdrawn 状态支持
  - 并发申请限制生效（同一任务同一时间仅一个 pending）
  - 被拒后重新提交 reason 去重校验生效（相同返回 422）
  - 审批通过副作用链在同一事务中完成（end_date + change_logs + snapshots + CPM）
  - 当前代码里硬查 `task_delay_history` 的 5 处服务/路由都已改到 `delay_requests`，且只统计 `approved`
  - 审批催促定时检查已挂接 scheduler（3天提醒 + 5天升级）
  - `change_logs` 写入服务被 tasks/risks/issues 路由调用
  - `task_progress_snapshots` 进度更新时有真实写入，不再只降级读取
  - `actual_start_date / actual_end_date` 自动写入规则已落地，只在显式保存链触发，不再用 `updated_at` 近似代替
  - `shared-infrastructure.test.ts` 全部通过
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.2b completed: task_delay_history 已 Rename+Extend 为 delay_requests；审批路由 + 业务规则（并发限制/reason去重/withdrawn/副作用链/催促定时）+ change_logs 写入服务 + task_progress_snapshots 写入链已建立；actual_start/end 自动写入规则已落地；旧查询点已切到 approved delay_requests；shared-infrastructure 测试通过`

### 10.2c G3.2c 预警/通知统一持久化与 warningService 职责收口

- **前置步骤**：`10.2b completed`
- **产品需求真值**
  - warningService / autoAlertService 职责分工以《业务流程优化清单.md》 §七为准
- **目标文件**
  - `server/migrations/<next>_alter_warnings_add_chain_fields.sql`
  - `server/src/services/warningService.ts`
  - `server/src/services/warningChainService.ts`
  - `server/src/services/autoAlertService.ts`
  - `server/src/utils/riskDetector.ts`
  - `server/src/scheduler.ts`
  - `server/src/routes/warnings.ts`
  - `server/src/routes/notifications.ts`
- **子步骤执行结构（必须按以下顺序推进）**
  1. `10.2c-a-test`
     - 需求摘录：`业务流程优化清单.md:19`、`业务流程优化清单.md:80`
     - 只锁通知持久化、去重、升级跳过规则和自然消除 contract
  2. `10.2c-a-impl`
     - 只实现 `warningService = 检测真值 / warningChainService = 升级与编排层 / autoAlertService = 调度壳` 的主从关系，不扩到前端页
  3. `10.2c-b-test`
     - 需求摘录：`业务流程优化清单.md:19`、`业务流程优化清单.md:80`
     - 只锁 `acknowledged_at / muted_until / pending 延期降级 / severity 自动升级` 的边界链路
  4. `10.2c-b-impl`
     - 只实现升级、跳过、降级和事件钩子接线
  5. `10.2c-review`
     - 只读检查 `双写是否清理 / 通知失败是否仍暴露 / 跳过规则是否完整 / 规则是否仍散落在多个服务`
     - 若发现缺口，必须写 `gaps` 并保持父步骤 `blocked`
- **具体动作**
  1. 明确职责分工：**warningService = 检测真值**（条件/阻碍/延期/验收等扫描规则与阈值）；**warningChainService = 编排层**（去重、升级、再激活、静音/已知悉跳过、写 warnings/notifications、链路回写）；**autoAlertService + scheduler = 调度壳**（触发频率、批次编排、重试、幂等），不再把升级链逻辑散在多个服务里
  2. `riskDetector` 阈值配置收归 warningService，riskDetector 降级为 warningService 内部检测策略
  3. 结合当前代码现状，预警链迁移分两层落地：①先 `ALTER warnings` 补齐 `chain_id / first_seen_at / muted_until`（`acknowledged_at` 若缺失才补列），把现有 warnings 表升级为可承载升级链与静音/已知悉计时的源表；②`notifications` 继续作为提醒中心展示表，但只能由 `warningChainService` 统一写入，不能再让 warningService 自己散写通知
  3.5. `warningChainService` 写入 `notifications` 时，`INSERT` 或 upsert 字段必须显式覆盖 `category / task_id / delay_request_id`；不允许只改调用方而遗漏这三列，避免 tab 分类、任务关联和延期审批导流失效
  4. 按 `warning_type + task_id + date` 去重
  5. 预警→风险自动升级（3 天未确认）、风险→问题自动升级（7 天 identified 未处理）：由 scheduler 定时触发 `warningChainService` 的升级检查方法；升级执行时必须同时完成源记录回写：预警升级回写 `escalated_to_risk_id / escalated_at / is_escalated`（见 §1.1），风险升级回写 `linked_issue_id / closed_reason / closed_at` 并置 `status = closed`（见 §1.2），统一遵循 §1.4 升级/转化统一原则；**升级检查跳过规则**：`acknowledged_at IS NOT NULL` 的预警跳过 3 天自动升级（已知悉暂停计时器）；`muted_until > now` 的预警跳过本轮检查（静音期内）；底层条件恶化再激活时清空 `acknowledged_at` 并重新开始 3 天倒计时（见 §1.1 再激活时字段处理）
  6. 条件过期自动创建问题：条件 `target_date < now` 且 `is_satisfied = false` 时，warningService 自动创建 `issues` 记录（`source_type: condition_expired`），不再继续触发预警（以《业务流程优化清单.md》 §1.1 为准）
  7. 通知列表、未读数、确认/已读状态统一走 notifications 表，不再允许双口径
  8. 在以下路由/服务的写入成功后接入 `warningService.evaluate(event)` 事件钩子：`tasks.ts`（进度更新、状态变更）、`task-obstacles.ts`（阻碍创建、状态变更）、`task-conditions.ts`（条件满足/过期）、`delay-requests.ts`（延期审批通过）；升级计时检查则统一挂到现有 `conditionAlertJob` 的每小时触发点，不新增第二套轮询框架
  9. 实现预警自然消除逻辑：按触发源映射消除条件（见 §1.1 预警自然消除），消除后标记为 `resolved`；兜底规则：任务 `status = completed` 时统一消除该任务关联的所有活跃预警
  10. 实现阻碍 severity 自动升级：warningService 的 `evaluate(event)` 和 `scanAll()` 中增加检测策略——阻碍 `expected_resolution_date < now` 且 `status != '已解决'` 时，自动将 severity 从 warning 升级为 critical；写入 `change_logs` 留痕；发送提醒通知；同一条阻碍同一方向升级最多触发一次（去重）（见 §2.2 / §七）
  11. 实现 pending 延期申请预警降级：warningService 检测到任务存在 `delay_requests.status = pending` 的延期申请时，该任务的延期类预警降级为 `info`（不参与自动升级），附注"延期审批中"；审批通过或驳回后重新评估预警级别（见 §2.3 pending 期间预警降级）
- **不要做的事**
  - 不删除 autoAlertService 文件本身（它作为调度壳保留），但不允许它定义检测规则
  - 不在本步改前端页面（预警/通知 UI 调整由后续步骤或专项承接）
- **执行纪律**
  - 本步是 10.x 风险最高的单步，执行顺序固定为：①先跑基线测试记录当前行为 → ②补或固化测试 → ③逐步迁移 warningService / autoAlertService / riskDetector / scheduler 逻辑 → ④清理重复职责
  - 不要直接重写 4 个服务文件；先定主从关系（warningService = 规则真值，autoAlertService = 调度壳，riskDetector = 检测策略，scheduler = 编排），再按主从关系逐个迁移
- **验证方式**
  ```powershell
  npm run build --workspace=server
  npm run test --workspace=server -- --run src/__tests__/riskNotificationChain.test.ts
  ```
- **通过标准**
  - warningService / warningChainService / autoAlertService 的职责边界已按“检测 / 编排 / 调度”拆清
  - 通知列表和未读数走同一来源
  - 同类预警/提醒不会因重复调度而双写或双显
  - 升级定时检查已挂接，且正确跳过 `acknowledged_at` 非空和 `muted_until` 未过期的预警
  - `warnings` 表已补齐 `chain_id / first_seen_at / acknowledged_at / muted_until` 所需列，`notifications` 只由统一编排层写入
  - 预警自然消除逻辑已实现（按触发源映射 + 任务完成兜底）
  - 阻碍 severity 自动升级已实现（expected_resolution_date 过期 + 未解决 → warning→critical，含去重和 change_logs 留痕）
  - pending 延期申请预警降级已实现（存在 pending 申请时延期类预警降为 info，审批结束后重新评估）
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.2c completed: warningService 已收口为检测真值，warningChainService 承接升级与编排，autoAlertService 为调度壳；warnings 链路字段已补齐；统一通知写入已挂接；升级定时检查已接入 conditionAlertJob；预警自然消除、阻碍 severity 自动升级和 pending 延期预警降级已落地`

### 10.2d G3.2d GanttView 延期审批 UI 与关键路径影响展示

- **前置步骤**：`10.2b completed`（delay_requests 表与审批路由可用）
- **当前判断**
  - 本步同时触及 `GanttView` 大页、延期申请交互和关键路径影响提示；如果还按单步直推，最容易顺手把拖拽、阻碍和预警逻辑一起打散
  - 本步属于高风险页面步骤，必须按 `contract-test -> impl -> review` 推进
- **目标文件**
  - `client/src/pages/GanttView.tsx`
  - `client/src/pages/GanttViewDialogs.tsx`
  - `client/src/lib/cpm.ts`
  - `client/src/lib/supabase.ts`
- **子步骤执行结构（必须按以下顺序推进）**
  1. `10.2d-a-contract-test`
     - 需求摘录：`业务流程优化清单.md:80`、`业务流程优化清单.md:3390`
     - 只锁延期申请表单、关键路径影响提示、申请状态 badge 和撤回动作 contract
  2. `10.2d-a-impl`
     - 只实现延期申请 UI、关键路径影响展示和状态可见，不扩到其他 Gantt 弹窗
  3. `10.2d-b-contract-test`
     - 需求摘录：`业务流程优化清单.md:80`
     - 只锁并发申请限制、reason 去重错误提示、withdrawn 流程和通知导流协议
  4. `10.2d-b-impl`
     - 只实现并发/重提/撤回交互与通知调用，不重写 CPM 算法
  5. `10.2d-review`
     - 只读检查 `是否仍直接改 end_date / 关键路径提示是否可见 / pending 是否禁提 / withdrawn 是否能回退重提`
     - 若发现缺口，必须写 `gaps` 并保持父步骤 `blocked`
- **开始前定位命令**
  ```powershell
  rg --line-number "end_date|delay_count|handleDelay|延期|calculateDelayImpact" client/src/pages/GanttView.tsx client/src/pages/GanttViewDialogs.tsx
  rg --line-number "calculateDelayImpact" client/src/lib/cpm.ts
  ```
- **具体动作**
  1. 将 GanttView 中当前"直接修改 `end_date` 触发延期"的交互替换为"提交延期申请"表单（调用 `POST /api/delay-requests`），表单字段：`delay_days`（延期天数）、`reason`（延期原因）
  2. 提交前自动调用 `cpm.ts` 的 `calculateDelayImpact`，在表单中展示对项目总工期的影响评估（关键路径上 / 消耗浮动时间 / 无影响）
  3. 关键路径任务（`is_critical=true`）延期申请额外显示醒目提示
  4. 延期申请提交后，任务 `end_date` 不立即变更，等待审批通过后由后端执行
  5. 在任务详情面板或卡片上展示当前延期申请状态（pending / approved / rejected / withdrawn）
  6. 审批通过后的 `end_date` 更新由后端 `delay-requests` 路由的 approve 端点完成（副作用链见 §七 `delayApproval` 行），前端只刷新任务数据
  7. 延期申请提交后，调用通知接口通知项目 owner/admin；关键路径任务（`is_critical=true`）延期申请额外在通知中标注"关键路径任务延期"醒目标识
  8. 前端增加"撤回申请"按钮（仅 `status = pending` 时可见），调用 `PUT /api/delay-requests/:id` 置 `status = withdrawn`；撤回后允许重新提交
  9. 提交时前端校验：同一任务存在 `pending` 申请时，提交按钮禁用 + tooltip 提示"存在待审批的延期申请，请先等待审批或撤回"（见 §2.3 并发申请限制）
  10. 被拒后重新提交时，前端提示"请修改延期原因后重新提交"（后端校验 reason 与上一条 rejected 记录不同，相同返回 422，见 §2.3）
- **不要做的事**
  - 不在本步改 warningService / 通知系统（归 10.2c）
  - 不改 CPM 算法本身
- **执行纪律**
  - `GanttView.tsx`（2046 行）超过 §0.4 的 2000 行上下文管理规则：禁止全文通读，只按 `rg` 锚点定位延期入口 / 对话框打开 / 提交处理 / `calculateDelayImpact` / `end_date`，然后分段读对应代码块
  - 补丁只落在 `GanttView.tsx` 和 `GanttViewDialogs.tsx` 的局部区域，不做顺手整理
  - 只承接"延期审批申请 + 关键路径影响展示 + 审批状态可见"，不顺手吞并阻碍弹窗、预警通知或共享底座后端逻辑
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/GanttView.test.tsx
  ```
- **通过标准**
  - GanttView 延期操作不再直接修改 `end_date`，改为提交审批申请
  - 提交前可见关键路径影响评估
  - 延期申请状态（含 withdrawn）在任务卡片/详情中可见
  - 延期申请提交后 owner/admin 收到通知，关键路径任务延期有醒目标识
  - 同一任务存在 pending 申请时，提交按钮禁用
  - 被拒后重新提交时 reason 不同校验生效（相同返回 422）
  - pending 申请可撤回（withdrawn），撤回后可重新提交
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.2d completed: GanttView 延期改为审批申请提交；关键路径影响评估已展示；申请状态可见（含 withdrawn）；延期通知已接入；并发申请限制 + reason 去重校验 + 撤回已落地`

### 10.2e G3.2e Reports 变更记录视图

- **前置步骤**：`10.2b completed`（change_logs 表与写入服务可用）
- **目标文件**
  - `client/src/pages/Reports.tsx`
  - `client/src/lib/supabase.ts`
- **开始前定位命令**
  ```powershell
  rg --line-number "change_logs|changeLogs|变更记录|AnalysisEntryCard" client/src/pages/Reports.tsx
  ```
- **具体动作**
  1. 在 Reports 页面新增"变更记录"分析视图入口卡片
  2. 变更记录视图读取 `change_logs` 表数据，按时间倒序展示
  3. 支持按 `entity_type`（任务 / 风险 / 问题 / 延期）筛选
  4. 每条记录展示：变更对象、字段、旧值→新值、变更原因、操作人、时间
  5. 视图收口为”轻摘要 + 筛选区 + 时间倒序记录列表”，强调系统审计留痕，不做工程签证/设计变更专区表达
- **不要做的事**
  - 不在本步改 change_logs 写入逻辑（归 10.2b）
  - 不在其他页面（GanttView / Milestones 等）内嵌变更记录组件
  - 不把本页做成工程变更审批、签证管理或商务变更专区
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/Reports.test.tsx
  ```
- **通过标准**
  - Reports 页面存在"变更记录"入口卡片
  - 变更记录视图能按 entity_type 筛选并展示核心变更字段
  - 视图风格与 Reports 现有分析视图统一
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.2e completed: Reports 变更记录视图已建立，支持按 entity_type 筛选，风格与现有分析视图统一`

### 10.2f G3.2f 提醒中心（Notifications）适配

- **前置步骤**：`10.2c completed`（notifications 持久化、acknowledged_at / muted_until 字段可用）
- **当前判断**
  - 提醒中心如果只改 tab 文案，不同步归组、已知悉、静音和导流模式，最后会变成“名字对了，行为还是旧的”
  - 本步属于高风险页面步骤，必须按 `contract-test -> impl -> review` 推进
- **产品需求真值**
  - 提醒中心适配以《业务流程优化清单.md》 §五 提醒中心行为准
- **目标文件**
  - `client/src/pages/Notifications.tsx`
  - `client/src/pages/__tests__/Notifications.test.tsx`
- **子步骤执行结构（必须按以下顺序推进）**
  1. `10.2f-a-contract-test`
     - 需求摘录：`业务流程优化清单.md:4906`、`业务流程优化清单.md:4922`
     - 只锁新 tab 分类、审批导流模式、通知归组和目标标签 contract
  2. `10.2f-a-impl`
     - 只实现 tab、归组和导流模式，不改后端持久化
  3. `10.2f-b-contract-test`
     - 需求摘录：`业务流程优化清单.md:4906`
     - 只锁 `已知悉 / 暂时静音 / 整组已知悉 / 全部已读` 的交互与状态回写协议
  4. `10.2f-b-impl`
     - 只实现抗疲劳动作和设置面板收口，不顺手追加审批表单
  5. `10.2f-review`
     - 只读检查 `审批类通知是否仍内嵌操作 / 归组是否可见 / acknowledged/mute 是否可回写 / resolveTarget 是否仍硬编码旧路由 / 计划编制来源是否能跳到 MonthlyPlanPage 与 CloseoutWorkbench`
     - 若发现缺口，必须写 `gaps` 并保持父步骤 `blocked`
- **具体动作**
  1. **tab 结构替换**：将现有 `all / unread / risk-issues / follow-up / system` 替换为 `全部 / 未读 / 业务预警 / 系统异常 / 流程催办`（与《业务流程优化清单.md》 §十五.19 分类口径一致）；更新 `TAB_OPTIONS` 常量和 `getReminderTab` 分类函数
  2. **审批导流模式**：延期审批等流程催办类通知只展示摘要 + "前往处理"按钮跳转到对应页面（GanttView / delay-requests），不在通知页内嵌审批操作
  3. **抗疲劳机制**：①同类通知自动归组（如"15 条快照断层"折叠为一条），归组后显示"最新触发时间 + 累计条数"；②最高严重级置顶；③支持"已知悉"操作（调用 `PUT /api/notifications/:id/acknowledge`，回写 `acknowledged_at`，降级灰显、不再置顶，但条件未消除前保留）；④支持"暂时静音"操作（调用 `PUT /api/notifications/:id/mute`，回写 `muted_until = now + 24h`，抑制同类 24 小时）
  4. **批量操作**：顶部增加"全部标记已读"按钮（已有 `markAllAsRead`，保留）；归组后的通知组支持"整组已知悉"操作
  5. 移除现有"提醒设置"面板中的数据导入导出功能（已下沉到各业务模块），设置面板仅保留提醒范围和同步模式切换
  6. 为计划编制接线预留新的来源类型解析：`source_entity_type` 至少支持 `planning / baseline / monthly_plan / closeout` 四个新值；在 `10.2f` 执行时，`Notifications.tsx` 先用页面内局部映射补齐这些来源到 `/projects/:id/planning/*` 相关路由的导流分支，确保 `MonthlyPlanPage / CloseoutWorkbench / BaselinePage` 可跳达；最终显示名和统一元数据读取留给 `14.5` 接管
- **不要做的事**
  - 不在本步改 notifications 后端持久化逻辑（归 10.2c）
  - 不在本步内嵌审批表单（审批操作统一导流到业务页面）
  - 不在本步提前依赖 `client/src/config/navigation.ts` 文件；该文件由 `14.5` 建立并统一接管提醒中心目标标签
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/Notifications.test.tsx
  ```
- **通过标准**
  - tab 结构已替换为 `全部 / 未读 / 业务预警 / 系统异常 / 流程催办`
  - 审批类通知只展示摘要 + 导流按钮，无内嵌审批操作
  - 同类通知归组显示，严重级置顶
  - "已知悉"和"暂时静音"操作可用
  - "全部标记已读"和"整组已知悉"批量操作可用
  - `planning / baseline / monthly_plan / closeout` 新来源类型已可被提醒中心识别并正确导流
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.2f completed: 提醒中心 tab 已替换；审批导流模式已落地；抗疲劳归组/已知悉/静音已实现；批量操作已补齐`

### 10.2g G3.2g 里程碑前置预警接线与待确认关闭交互补齐

- **前置步骤**：`10.2c completed`
- **当前判断**
  - `preMilestoneWarningService` 仍有较大概率游离在主扫描链之外，里程碑临近但前置未完成的预警容易漏到模块外
  - `pending_manual_close` 已经进入需求真值和数据结构，但 `RiskManagement` 还缺少醒目标记、确认关闭、保持处理中、快筛这套完整交互
  - 本步是典型跨后端规则 + 前端交互联动步骤，必须拆成服务端和前端两段推进，不能一把改完
- **产品需求真值**
  - 里程碑前置预警来源、阈值和去重规则以《业务流程优化清单.md》 §1.1、§七 warningService 行、§八 `RiskManagement` 行为准
  - `pending_manual_close` 的前端交互和自动闭合原则以《业务流程优化清单.md》 §1.2、§八 `RiskManagement` 行为准
- **目标文件**
  - `server/src/services/preMilestoneWarningService.ts`
  - `server/src/services/warningService.ts`
  - `server/src/scheduler.ts`
  - `client/src/pages/RiskManagement.tsx`
  - `client/src/pages/__tests__/RiskManagement.test.tsx`
- **子步骤执行结构（必须按以下顺序推进）**
  1. `10.2g-a-test`
     - 需求摘录：`业务流程优化清单.md:19`、`业务流程优化清单.md:4318`
     - 只锁 preMilestoneWarning 并入 warningService 主链、阈值、去重和通知写入 contract
  2. `10.2g-a-impl`
     - 只实现服务端接线、去重和补扫，不改 RiskManagement 页面
  3. `10.2g-b-contract-test`
     - 需求摘录：`业务流程优化清单.md:4318`
     - 只锁 `pending_manual_close` 标签、banner、确认关闭、保持处理中和快筛协议
  4. `10.2g-b-impl`
     - 只实现前端交互和 change_logs 留痕接线，不重做异常页整体结构
  5. `10.2g-review`
     - 只读检查 `里程碑预警是否仍游离主链 / 风险问题卡是否可见待确认关闭 / 两个动作是否写留痕 / 是否误新建额外 tab`
     - 若发现缺口，必须写 `gaps` 并保持父步骤 `blocked`
- **具体动作**
  1. 将 `preMilestoneWarningService` 正式并入 `warningService.evaluate(event)` / `warningService.scanAll()` 主链，不再作为游离服务单独判断
  1.5. 在 `scheduler.ts` 中移除 `preMilestoneWarningJob` 的独立实例和 `start()` 调用，改由 `conditionAlertJob` 的统一扫描节奏覆盖里程碑前置预警，避免同一扫描重复执行和通知双写
  1.6. `风险 -> 问题` 的自动升级 scheduler 归属仍在 `10.2c`；本步不新增第二套升级调度 job，只处理里程碑前置预警并入主链与 `pending_manual_close` 交互补齐
  2. 若当前仓库内没有稳定阈值常量，则按需求文件口径使用 `7` 天默认阈值，并在代码注释中注明来源，不再额外发明第二套规则
  3. 对同一 `task_id + pre_milestone_id + warning_date` 做去重，避免同一里程碑临近事件重复写入 `notifications`
  4. 在 `RiskManagement` 风险/问题卡片上补 `待确认关闭` 琥珀标签；详情抽屉顶部增加 banner：`来源已解除，是否确认关闭？`
  5. banner 提供两个动作：`确认关闭` 和 `保持处理中`
  6. `确认关闭`：写入 `status = closed`、`pending_manual_close = false`，并写 `change_logs`
  7. `保持处理中`：仅清除 `pending_manual_close`，保留原状态（`mitigating / investigating`），并写 `change_logs`
  8. 在 `RiskManagement` 视图筛选区补 `待确认关闭` 快筛，不额外新建 tab
- **不要做的事**
  - 不在本步重做 `RiskManagement` 整页信息架构
  - 不把里程碑预警单独做成第二套通知中心或第二套扫描服务
  - 不顺手改 `Reports / Dashboard` 的页面壳层
- **验证方式**
  ```powershell
  npm run build --workspace=server
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/RiskManagement.test.tsx
  ```
- **通过标准**
  - 里程碑前置预警已并入 `warningService` 主链，支持事件驱动和定时补扫
  - 同一里程碑临近事件不会重复生成通知
  - `pending_manual_close` 的标签、banner、确认关闭、保持处理中和快筛都已落地
  - 对应操作都能写入 `change_logs`
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.2g completed: 里程碑前置预警已并入 warningService 主链；RiskManagement 已补齐 pending_manual_close 标签、banner、确认关闭/保持处理中与快筛`

### 10.8a G9a 参建单位主数据台账与 CRUD

- **当前判断**
  - 当前仓库并不存在"参建单位独立业务页"基础；本步只建后端表、路由和 GanttView 弹窗，不动 TaskSummary。
- **目标文件**
  - `server/migrations/<next>_create_participant_units_table.sql`
  - `server/src/routes/participant-units.ts`
  - `server/src/types/db.ts`
  - `server/src/index.ts`
  - `server/src/__tests__/participant-units.test.ts`
  - `client/src/pages/GanttView.tsx`
  - `client/src/pages/GanttView/ParticipantUnitsDialog.tsx`
- **开始前定位命令**
  ```powershell
  Get-ChildItem -LiteralPath server/migrations -File | Where-Object { $_.Name -match '^(\d{3})_' } | Sort-Object Name | Select-Object -ExpandProperty Name | Select-Object -Last 10
  ```
- **具体动作**
  1. 确认当前最大迁移编号后顺延
  2. 建立参建单位主数据表与 CRUD 路由
  3. 维护字段只保留：`单位名称`、`单位类型`
  4. `单位类型` 同时支持快捷选择和可编辑补录，口径以 `园林 / 幕墙 / 土建` 这类专项工程类型为主
  5. 主数据维护入口固定挂在任务列表位置，使用 `GanttView` 内弹窗承载，不新建独立页面、不新增侧边栏路由
- **不要做的事**
  - 不新建独立 `ParticipantUnits` 页面
  - 不新增侧边栏入口或新路由
  - 不在本步改 Dashboard 或 TaskSummary
- **验证方式**
  ```powershell
  npm run build --workspace=server
  npm run test --workspace=server -- --run src/__tests__/participant-units.test.ts
  npx tsc -p client/tsconfig.json --noEmit
  ```
- **通过标准**
  - 参建单位表与 CRUD 路由可用
  - GanttView 弹窗可维护单位台账
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.8a completed: participant_units 表与 CRUD 路由已建立；GanttView 弹窗入口已挂接`

### 10.8b G9b 业务表单接入参建单位 + Dashboard 快照

- **前置步骤**：`10.8a completed`
- **目标文件**
  - `client/src/pages/GanttView.tsx`（任务表单中的责任单位字段）
  - `client/src/pages/PreMilestones.tsx`（证照表单中的责任单位字段）
  - `client/src/pages/Dashboard.tsx`
  - `server/migrations/<next>_alter_tasks_add_participant_unit_fk.sql`
  - `server/src/routes/tasks.ts`
  - `server/src/routes/task-summaries.ts`
  - `server/src/types/db.ts`
- **具体动作**
  1. 在 `tasks` 表上新增可空 `participant_unit_id` 外键，作为新的主引用；旧字段 `assignee_unit` 标记为 deprecated，`responsible_unit` 暂保留作为过渡兼容字段，不允许一次性硬切断旧数据
  2. 业务表单中的 `责任单位` 不再允许直接自由手填，改为从单位台账选择；写入优先级固定为 `participant_unit_id -> responsible_unit -> assignee_unit`
  3. 同一专项工程因标段划分存在多家单位时，单位展示按同一专项类型归组、按具体单位名称分开展示
  4. `Dashboard` 只保留责任单位快照摘要，不承接详情
  5. `Dashboard` 增加状态变化指标摘要卡片：新增/关闭的条件、阻碍、延期数量（基于 `task_progress_snapshots` 日对比计算）
  6. `ProjectInfoCard` 保留在 Dashboard 页头区域，继续作为项目级基础信息编辑入口；不下沉到 Dashboard 主体快照区；参建单位和范围维度主数据都不在此维护
- **不要做的事**
  - 不在本步改 TaskSummary（归 10.8c）
  - 不把责任单位详情放到 Reports
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/sharedSummary.test.tsx
  ```
- **通过标准**
  - `participant_unit_id` 已成为新的主引用，旧字段有清晰 fallback 链且未导致存量数据断读
  - 业务表单中的责任单位都改为"先维护、再选择"
  - Dashboard 保留责任单位快照
  - Dashboard 状态变化指标摘要可见
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.8b completed: tasks 已补 participant_unit_id 外键与 fallback 链；assignee_unit 标记 deprecated；业务表单责任单位已改为台账选择；Dashboard 保留快照摘要；状态变化指标已接入`

### 10.8c G9c TaskSummary 责任主体分析面板

- **前置步骤**：`10.8b completed`
- **口径来源**
  - `TaskSummary` 页面定位、承接范围、与 Dashboard / Reports 的边界，以《业务流程优化清单.md》为准
  - 本节只保留实现与验收要求，不再重复定义页面职责
- **9.x UI 规则约束**
  - 新增面板遵循 `9.x` 收口后的 card-base 风格，图表库如引入 recharts 等需与已有卡片风格一致
- **目标文件**
  - `client/src/pages/TaskSummary.tsx`
  - `client/src/pages/TaskSummary/components/ResponsibilityAnalysisPanel.tsx`
- **具体动作**
  1. 按《业务流程优化清单.md》已确认口径，在现有 `TaskSummary` 内实现责任主体分析承接
  2. 责任主体分析同时支持：
     - 责任人
     - 责任单位
  3. 至少补齐：任务总数、已完成数、延期数、按时完成率、当前在手任务数、风险/阻碍压力
  4. 提供图表 + 列表 + 下钻任务明细
- **不要做的事**
  - 不新增独立责任主体业务页
  - 不把责任主体详情放到 Reports
  - 不只做一个简单排行
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/TaskSummary.test.tsx
  ```
- **通过标准**
  - `TaskSummary` 以新增分析区方式承接责任人/责任单位统一详情
  - 责任主体分析已能为管理动作提供判断依据
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.8c completed: TaskSummary 已新增责任主体分析面板，含图表+列表+下钻明细`

### 10.9 G10 项目范围维度底座与挂接统一

- **当前判断**
  - 当前 `client/src/components/ProjectInfoCard.tsx` 已经具备项目级基础信息编辑能力；若本节不明确"谁负责主维护、谁只负责挂接"，大模型很容易额外发明 `ScopeDimensions` 页面或在多个页面各做一套编辑入口。
- **目标文件**
  - `server/migrations/<next>_create_scope_dimensions_tables.sql`
  - `server/migrations/<next+1>_drop_project_scope_columns.sql`
  - `server/src/routes/scope-dimensions.ts`
  - `server/src/index.ts`
  - `server/src/types/db.ts`
  - `client/src/components/ProjectInfoCard.tsx`
  - `client/src/pages/GanttView.tsx`
  - `client/src/pages/PreMilestones.tsx`
  - `client/src/pages/Drawings.tsx`
  - `client/src/pages/TaskSummary.tsx`
- **开始前定位命令**
  ```powershell
  Get-ChildItem -LiteralPath server/migrations -File | Where-Object { $_.Name -match '^(\d{3})_' } | Sort-Object Name | Select-Object -ExpandProperty Name | Select-Object -Last 10
  ```
- **具体动作**
  1. 执行前先确认当前最大迁移编号，使用下一个可用三位编号
  2. 第一个服务端动作必须先在 `server/src/index.ts` 挂接 `/api/scope-dimensions` 路由，避免新路由文件落地后接口入口仍返回 `404`
  3. 建立统一的范围维度字典与项目挂接表
  4. 一次性统一 `building / specialty / phase` 三类范围维度，不再分轮次推进
  5. 范围维度主维护入口固定放在 `TaskList / GanttView`，与参建单位统一维护；`ProjectInfoCard` 只做范围摘要只读展示
  6. `ProjectInfoCard` 在 Dashboard 页头采用“摘要条 + 页头下展开编辑区”形态，不再保留主体区大卡内联全量编辑
  7. 页头摘要条只展示项目计划开始/结束、总建筑面积、总投资、建筑数量、支护形式，编辑入口统一为 `编辑项目`
  8. 范围维度在 `TaskList / GanttView` 中一次到位支持全局字典治理和项目挂接，包括新增、改名、排序、挂接、取消挂接和受控选择统一值
  9. `v1` 直接完成全局字典治理，不拆“轻量版 / 后续版”
  10. 统一后续筛选、统计、责任主体分析和图纸/证照联动所依赖的范围口径
  11. 对旧字段清理必须先用明确判断语句决定分支：执行 `SELECT COUNT(*) FROM projects WHERE building_type IS NOT NULL OR structure_type IS NOT NULL`；若结果为 `0`，则直接删除旧字段；若结果大于 `0`，必须先完成旧字段到新范围维度底座的数据迁移，再执行删除
  12. 同步清理 `server/src/types/db.ts` 中 `Project` 接口的对应字段定义
- **不要做的事**
  - 不新建独立 `ScopeDimensions` 页面
  - 不在 `PreMilestones / Drawings / TaskSummary` 各自再做一套范围维度主编辑弹窗
  - 不把 G10 和 G9 混成一个表
- **验证方式**
  ```powershell
  npm run build --workspace=server
  npx tsc -p client/tsconfig.json --noEmit
  ```
- **通过标准**
  - 范围维度不再只是零散字段集合，已经形成统一字典、统一挂接和统一展示口径
  - `TaskList / GanttView` 成为范围维度与参建单位的主维护入口，`ProjectInfoCard` 与范围维度脱钩
  - `projects` 表已删除 `building_type / structure_type / project_type` 等冗余列，`db.ts` Project 接口已同步清理
  - 范围维度已完全改由新底座治理，不再保留旧字段兼容口径
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.9 completed: building/specialty/phase 范围维度已一次性统一到同一底座，现有页面已接入统一挂接口径`

### 10.10 G11 跨页面主数据承载与状态一致性治理

- **当前判断**
  - 当前 `client/src/hooks/useStore.ts` 仍是单文件平铺 store；若本节继续写成"新增多个 slice"，大模型极易把它误执行为"先全面重构 Zustand 架构"，从而偏离本节真正目标。
- **目标文件**
  - `client/src/hooks/useStore.ts`
  - `client/src/pages/PreMilestones.tsx`
  - `client/src/pages/TaskSummary.tsx`
  - `client/src/pages/Drawings.tsx`
  - `client/src/pages/GanttView.tsx`
  - `client/src/pages/AcceptanceTimeline.tsx`
  - `client/src/pages/Dashboard.tsx`
  - `server/src/routes/task-conditions.ts`
- **子步骤执行结构（必须按以下顺序推进）**
  1. `10.10x-contract-test`
     - 需求摘录：`业务流程优化清单.md:8`、`业务流程优化清单.md:6564`
     - 只锁 store 分区、关键 selector、跨页读取 contract 和“关键关系失败即暴露”规则
  2. `10.10x-impl`
     - 只实现 store 分区、页面接线和统一 selector，不顺手重构整个 Zustand 架构
  3. `10.10x-review`
     - 只读检查 `主数据是否仍有页面私有副本 / criticalPathSummary 是否被误当真值 / 缺表缺权限是否仍伪成功`
     - 若发现缺口，必须写 `gaps` 并保持父步骤 `blocked`
- **具体动作**
  1. 本节必须在 `10.8c completed` 与 `10.9 completed` 之后执行；未完成前，不提前做全局状态归并
  2. 在 store 中新增 `drawings / preMilestones / taskSummary / participantUnits / scopeDimensions / acceptanceTimeline / criticalPathSummary` 等命名状态分区；允许先继续放在同一 `useStore.ts` 文件内，不强制拆成多文件 slice 架构
  3. **store 复杂度阈值**：本步完成后，若 `useStore.ts` 满足以下**任一**条件，则必须在本步内完成按域拆文件：①单文件超过 **500 行**；②数据域超过 **15 个**（含 setter/selector）；③同一次改动涉及 **4 个以上**不相关数据域的 store 逻辑。拆分方式：按业务域拆文件（如 `stores/tasks.ts`），原 `useStore` 保留为聚合入口。若未触达阈值，每个新增分区用注释块标注域边界（如 `// ─── drawings ───`），对应 selector 紧跟数据域定义
  4. 其中 `criticalPathSummary` 只承载跨页面复用的关键路径摘要 / 读取缓存，关键路径真值仍以后端 `CriticalPathSnapshot` 为准，不在本节改成前端本地真值
  5. 将 `Dashboard / GanttView / TaskSummary / PreMilestones / Drawings / AcceptanceTimeline` 的主业务数据统一写入 store
  6. 页面保留本地弹窗与表单临时状态，但不再让主业务数据只停留在页面级 `useState`
  7. 保证责任单位、范围维度、图纸、证照、验收流程轴、任务总结等主数据在跨页面跳转后仍保持一致
  8. 保证 `Dashboard` 快照、`TaskSummary` 详情分析、`GanttView` 任务明细、`AcceptanceTimeline` 外部前置读取的是同一套责任主体与范围维度数据
  9. `notifications / issues / delay_requests / change_logs / task_progress_snapshots` 这些共享底座继续以服务端为真值；本节只统一前端读取口径、摘要缓存和跨页状态一致性，不允许再派生新的前端私有真值
  10. 本节只解决"主数据承载与跨页面一致性"，不借机重写路由、页面信息架构或 UI 组织方式
  11. 对前置任务关系、责任单位挂接、范围维度挂接等关键关系写入，禁止"缺表/缺迁移时返回 success 空结果"的静默降级；尤其 `task_preceding_relations` 缺失时，保存必须明确报错或标记 blocked，并留下可诊断信息
- **不要做的事**
  - 不把所有瞬时 UI 状态都塞进 store
  - 不把本节误执行成"先把 Zustand 完整重构成新架构再做业务接线"
  - 不在本节顺手新增业务路由或页面
  - 不把本步误写成新的独立业务模块
  - 不对关键关系写入保留"伪成功"的兼容返回
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/TaskSummary.test.tsx
  npx vitest run --root client src/pages/__tests__/preMilestonesLifecycle.test.ts
  npx vitest run --root client src/pages/__tests__/sharedSummary.test.tsx
  ```
- **通过标准**
  - 责任单位、范围维度、图纸、证照、验收流程轴、任务总结等主数据已有统一 store 承载，且允许仍由单文件 `useStore.ts` 先行承载
  - 关键路径跨页面复用已统一到 `criticalPathSummary` 摘要读取口径，但关键路径真值未被错误下放到前端本地状态
  - 页面不再重复各自维护孤立主数据副本
  - 跨页面跳转后，责任主体和范围维度口径保持一致，不再出现各页各算一套
  - 关键关系写入失败时能够明确暴露错误，不再出现缺表情况下返回 success 空结果
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.10 completed: 责任单位、范围维度、图纸、证照、任务总结主数据已统一归并到 store，跨页面口径保持一致；关键关系写入不再静默降级`

### 10.10a G11a 共享真值收口与静默降级清理

- **前置步骤**：`10.10 completed`
- **当前判断**
  - 当前 `10.10` 解决了主数据承载问题，但 `notifications / issues / delay_requests / change_logs / task_progress_snapshots` 这批共享真值仍可能在页面层保留 shadow state、局部 fallback 或“失败时返回空结果”的静默降级
  - 《业务流程优化清单.md》 §零 已经明确：页面只能消费共享真值，不允许各自再维护平行真值源
- **产品需求真值**
  - 共享真值对象、前端读取口径与归属边界以《业务流程优化清单.md》 §零 为准
  - 共享底座编排与服务端真值要求以《业务流程优化清单.md》 §七 为准
  - 乐观锁、删除保护、失败暴露和不允许静默成功的要求以《业务流程优化清单.md》 §九 为准
- **目标文件**
  - `client/src/hooks/useStore.ts`
  - `client/src/pages/Dashboard.tsx`
  - `client/src/pages/RiskManagement.tsx`
  - `client/src/pages/Notifications.tsx`
  - `client/src/pages/TaskSummary.tsx`
  - `client/src/pages/GanttView.tsx`
  - `server/src/routes/task-conditions.ts`
  - `client/src/pages/__tests__/sharedSummary.test.tsx`
- **子步骤执行结构（必须按以下顺序推进）**
  1. `10.10b-contract-test`
     - 需求摘录：`业务流程优化清单.md:8`、`业务流程优化清单.md:6564`
     - 只锁共享真值单一读取口径、`loading/empty/error` 三态和禁止静默降级 contract
  2. `10.10b-impl`
     - 只清理 shadow state、伪空态和页面私有 fallback，不重构导航与视觉结构
  3. `10.10b-review`
     - 只读检查 `notifications/issues/delay/change/snapshot 是否仍有平行真值 / 失败态是否仍被伪装成空态`
     - 若发现缺口，必须写 `gaps` 并保持父步骤 `blocked`
- **具体动作**
  1. 审计并清理页面内对 `notifications / issues / delay_requests / change_logs / task_progress_snapshots` 的私有副本、影子缓存和“先本地写一份再异步对齐”的平行真值写法
  2. 为共享底座数据统一 `loading / empty / error` 三态，不允许把请求失败伪装成空列表或零统计
  3. 对关键关系写入继续执行“失败即暴露”原则：缺路由、缺表、缺权限、乐观锁冲突都必须给出明确报错，不返回 success 空结果
  4. `Dashboard / RiskManagement / Notifications / TaskSummary / GanttView` 只允许保留摘要缓存和 UI 临时态，不允许二次派生共享对象的本地真值
  5. 把跨页面读取统一到同一批 selector / fetch 封装，不再由页面各自拼接同类数据的字段映射
- **不要做的事**
  - 不把本步误执行成再次全面重构 store 架构
  - 不把所有列表数据都改成重型全局缓存
  - 不在本步改页面视觉结构或导航层级
- **验证方式**
  ```powershell
  npx tsc -p client/tsconfig.json --noEmit
  npx vitest run --root client src/pages/__tests__/sharedSummary.test.tsx
  npx vitest run --root client src/pages/__tests__/TaskSummary.test.tsx
  ```
- **通过标准**
  - 共享底座对象只保留一套前端读取真值口径
  - 失败态、冲突态、缺表态不再静默降级为空结果
  - 各页面不再维护 `warning / risk / issue / delay / change / snapshot` 的私有平行真值
- 其余未单列项，仍须符合《业务流程优化清单.md》中适用于本步骤的 §零 / §九 / §十四 / §十六 约束。
- **进度回写示例**
  - `10.10a completed: 共享底座对象已统一到单一前端读取口径；页面私有 shadow state 与静默降级已清理`

